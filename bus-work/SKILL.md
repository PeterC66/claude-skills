---
name: bus-work
description: Show what the BusMaps.uk bus-map system is waiting on — one ranked worklist gathered from the portal (publish reviews, organisation applications, map requests approved and awaiting a build, proposed updates) and from the local map tree (towns whose services change soon per the BODS scan, stale engine renders, missing S6 verification) — then carry the chosen item all the way through: run the right map skill, gate it, and deliver it into the portal with the exact command. Use when asked "what needs doing on the buses / the portal", "what's next", "do the monthly bus refresh", "is there anything waiting in the portal", "build the map someone asked for", "work the bus worklist", or when picking a numbered item from a worklist this skill printed. The entry point for routine BusMaps.uk operations — it replaces reading runbooks R1–R4 to remember which script, which flags, which order.
---

# Work the BusMaps.uk worklist

**Names for the parts and the process.** `C:\u3a St Ives\Using AI\Buses\Documentation\README - Glossary of terms.md` is the shared vocabulary: sheet elements by callout code, and the stage / portal / repo / failure words this skill uses throughout. It also records which of them to translate before writing to a customer — `deliver`, `accept` and `publish` are three different acts, and only publish changes what the public sees.

**The point of this skill: Peter should never have to open a runbook to do the routine month.** Everything the system is waiting on appears in one ranked list, and every item carries its own procedure. If you find yourself telling him to "see R4", you have failed — inline the step instead.

**Where Peter reads about this in his own words.** `Buses\Documentation\README - How to publish a map to the portal.md` (written 2026-08-21) is his plain-English account of the whole lifecycle — the three approval gates, what the publish review is actually asking, deliver vs accept vs publish, and the monthly cycle — and it names this skill as the front door. It is command-free by design and defers to R1/R3/R4 on anything technical. Point him at it when he wants to understand a step rather than execute one; **when a procedure here changes, check whether it changed the story there**, and don't answer him out of R1/R3/R4 when that guide already says it in his register.

## The shape of the system (why the split exists)

| Zone | Where it happens | Why it can't move |
|---|---|---|
| **Know** what needs doing | the portal | it holds the customers, requests and queues |
| **Make** the map | this laptop, S1–S6 | needs live data, network and judgement — the portal is deliberately deterministic, no network, no AI at render time |
| **Deliver** into the portal | `import-map.mjs` / `propose-update.mjs` | writes the portal's SQLite + object store |

This skill is the spine across all three. It does not replace `make-bus-leaflet` / `make-place-bus-leaflet` — it *calls* them, then does everything either side.

## Asked about ONE map? Use `town_status.mjs`, not the worklist

The worklist answers *what needs doing across the estate*. **"What is the status of Ramsey?" is a different question**, and answering it from the worklist or the board gets it wrong: on 2026-09-10 `status.js` reported `Ramsey 3.9 · PASS PASS PASS · ok` about a map that was returning 404 to the public, had been invisible for thirteen days, and had a correspondent waiting seven days for a reply. Every column was honest — they answer *does the current engine reproduce the committed sheet*, which is not the same question.

```powershell
node "%BW%\town_status.mjs" "Ramsey"
```

**Any directory.** One argument, the map's name — a town, a nested place or a standalone place; it finds all three by looking for `manifest.json` rather than by a list it carries. It joins the four things nothing else crossed: the **build** (version, S6 age, hard/soft), the **live site** (`/api/public/maps` — *published* and *visible* are different facts, and a map with a published version stays hidden while `public_listed` is 0), the **people** (every `Correspondence/CORR-nnn/` thread that declares this map on its `**About:**` line, reported even when nothing is pending), and the **questions we asked a real person and never got back** (`local-decisions.json`).

**It leads with whatever is worst, not with the build.** If a map is invisible to the public, that is its status whatever the gates say.

Exit **0** nothing needs attention · **1** something does · **2** used wrongly, or a fact could not be established. `--offline` skips the site, `--json` gives the same facts without the ordering opinion, `--url` points at another portal.

**An unreachable portal is not an invisible map**, and the tool refuses to conflate them: a failed fetch exits 2 saying *"This is NOT a report that the map is missing"*, because the opposite would send somebody to re-publish a map that was never off. Its falsification harness is `prove-red-town-status.mjs` — nine cases, each negative paired with the positive that proves the check can fire at all, including the real bug that paid for it (a thread README naming another map's adviser in prose was reported as correspondence about that map). Run it after touching either file:

```powershell
node "%BW%\prove-red-town-status.mjs"
```

**It runs no byte gate and re-renders nothing** — for gates, vendoring drift and the quality ratchet, that is still `status.js`.

## Step 1 — Print the worklist (always start here)

Let `BW=C:\u3a St Ives\.claude\skills\bus-work\assets` (this skill lives in the bus skills repo alongside `make-bus-leaflet`, and is junctioned into `~\.claude\skills\` like the others).

```powershell
node "%BW%\worklist.mjs"
```

**Any directory.** Unlike everything in [references/playbooks.md](references/playbooks.md), this one does not care where you are: it is invoked by absolute path and reads nothing relative to the working directory. Every command in the playbooks does care, and each carries its own `cd` for that reason.

The same list is on the web at **`/app/admin` → To do** (the admin landing tab) — same ranking, same wording, because both call the portal's `src/worklist/index.js`. The terminal version adds what only this machine can see: failing gates, engine-stale renders, missing S6, and towns with a leaflet but no portal map.

Read-only, safe to run at any time, and safe while the dev server is running (the portal DB is WAL). Add `--json` when you need to act on the fields, `--gates` to also run the full byte-identical gate sweep (slow — a minute or two; only worth it after an engine change or before a batch of deliveries).

**Pass `--session <this session's name>` — the name `ListAgents` gives THIS session, `buses-29` rather than `Claude`.** It is the same name a backlog claim is made under, and without it the conditions block cannot tell your own claim from a neighbour's and says so. That is the one flag worth remembering; everything else has a default.

**`--self-id <uuid>` is the second one, and only a scheduled tick needs it** (buses-data OA-294, 2026-09-11). It is this session's own transcript id — the UUID on its scratchpad path, `…\Temp\claude\<project>\<uuid>\scratchpad`, which is the basename of its own `<uuid>.jsonl`. It buys one field, `conditions.peers.quiescentMin`: the age in minutes of the newest turn taken by any session **other than the caller**, which is the only form of the question a session taking a turn can usefully ask. Without the flag that field is `null`, and **`null` means *cannot tell*, never *quiet*** — so does an id matching no transcript, and so does a disk with no other session on it. It is a measurement and nothing here scores it; the rule that acts on it is the *Adopting an orphan* section of `loop/README.md` in the private `buses-data` repository, which is deliberately not linked from here — a relative link out of this repository resolves on one laptop and 404s for everyone else, and this one is public.

### Is it safe to do this right now? — the concurrency verdict

**Every row carries `SAFE NOW`, `CHECK FIRST` or `BETTER TO DELAY`, and a `CONDITIONS` block above the bands states each contended fact once.** Printing the list is read-only; carrying an item through writes into trees several sessions share, and sometimes deploys. `node "%BW%\worklist.mjs" --conditions` answers *can I do anything at all right now* without gathering a queue; `--safe-only` filters the list strictly, so a workable `CHECK FIRST` row is hidden by it. ([why](references/why-these-rules.md#is-it-safe-to-do-this-right-now--the-concurrency-verdict-oa-221-2026-09-01))

**A dirty tree is `CHECK FIRST`, not a delay** — `git status` cannot say whose files those are. **Only two things earn `BETTER TO DELAY`**: an estate-wide sweep while `Areas/`, `Places/` or `ci-reference/` is dirty, and a deliver or deploy while the portal checkout is off `main`. A modified `Correspondence/**/*.md` that a live `loop/your-move/` hold names is **accounted for** and subtracted, and the block prints the subtraction rather than hiding it.

**A count that could not be taken is never a count of zero.** `unpushed UNKNOWN` reads `CHECK FIRST`; a claim made before today is marked `EXPIRED, n day(s) old` — a statement about AGE and not about liveness, because this board cannot tell a dead session from an idle one — and an expired row is free: the board prints the `--claim` that takes it without `--force`, never a release (buses-data OA-400).

**A decision is never held back, and that is the most useful thing on the list.** A publish review, an application, a map-request decision, a chase and a drafted reply touch no working tree, so when everything else says wait, those are what to offer.

**A DETACHED portal checkout is measured now, because two things look alike there and only one of them clears itself** (buses-data OA-387, 2026-09-17). `portal-write` used to say of any checkout that was not on `main` that the branch was *somebody's live work*, and for two days in September 2026 it said that hourly about a portal whose HEAD the deploy procedure had detached while a finished worktree held the `main` branch name — so `git checkout main` in the primary checkout failed, nothing could undo it, and eleven consecutive ticks read the verdict, correctly declined to deliver, and none of them went and looked. A verdict that says somebody is mid-task reads as transient; residue is the opposite, and it is still there tomorrow. The rule now asks git two questions instead: **is the detached commit already on `origin/main`** — if it is, nothing is stranded and this is deploy residue, which reads `CHECK FIRST` with the worktree holding `main` named on the row and the `git -C "<portal>" checkout main` that clears it — and **is it not**, in which case somebody's commits are sitting on no branch at all and `BETTER TO DELAY` is still the right advice. A named feature branch is unchanged and still reads as somebody working, which is the one case the old sentence was right about. There is a third answer: no `origin/main` to compare against, or a git call that failed, reads `COULD NOT LOOK` in as many words and takes the **stricter** verdict, because a refusal read as an absence measures the instrument instead of the subject. The conditions block prints a `detached` line carrying the same finding, and prints nothing at all over a checkout that is on a branch.


**Falsified** from this folder, no placeholders: `node prove-red-concurrency.mjs`.

### The scheduled loop holds a lock, and this is where you see it

**A tick takes `loop/LOCK.d` before its unit of work, and `loop/` is gitignored, so no `git status` can ever see it.** It is the `loop-lock` resource: a live tick is `BETTER TO DELAY` on every row touching the shared trees. ([why](references/why-these-rules.md#the-scheduled-loop-holds-a-lock-and-this-is-where-you-see-it-oa-287-2026-09-08))

**Take the lock yourself before heavy work** — an S1–S6 build, an estate sweep or rollout, `quality_gate.js --accept`, or an engine change — and not for ordinary document or backlog work, which several sessions must go on doing at once. From the buses repository root, `C:\u3a St Ives\Using AI\Buses`, with no placeholders:

```bash
mkdir "loop/LOCK.d"
```

Non-zero means somebody holds it. Write `loop/LOCK.d/holder` with **your session name first** — the name `ListAgents` gives this session — then the time, and a second line `expires:` at most ninety minutes ahead. **Releasing is the part you owe**, because a tick never steals from a name that is not a tick's:

```bash
rm -rf loop/LOCK.d
```

Relative, not absolute: both allow rules are written relative, and the absolute form is a fresh permission prompt at the worst possible moment.

**Your own lock reads `SAFE NOW`**, and so does a tick's past its lease, because step 3 of the loop's prompt is where the steal rule lives. A person's lock past its lease is `CHECK FIRST`: nothing will clear it for you. **Falsified**: `node prove-red-loop-lock.mjs`.

### Whether the loop is DOING anything, which is a different question

**A HALTED loop holds no lock, which is exactly what a healthy idle loop looks like.** `loop_runs.mjs` raises one row, `loop-idle`, from the NAMES of the files in `loop/runs/` and nothing else: `-none` stopped before dispatch, `-around` did real work but never reached `buses-data`. ([why](references/why-these-rules.md#and-whether-the-loop-is-doing-anything-which-is-a-different-question-from-whether-it-is-running-oa-288-2026-09-09))

**The row says WHY, from the same `conditions` block printed above the bands**, so it can never contradict it. A forgotten `loop/STOP` or a dirty shared tree is rank 3 in `SOMEONE IS BLOCKED`; a lock held by a person stays at rank 8, because the loop is correctly deferring to somebody at the keyboard. **Two silences are deliberate**: a merely old newest run raises nothing, and the threshold is two consecutive, derived from the median gap between run files. **Falsified**: `node prove-red-loop-runs.mjs` — read its GREEN cases.

### Which portal you are looking at, and what the credential is

**`worklist.mjs` reads the LIVE site by default.** `BUSMAPS_URL` and `BUSMAPS_TOKEN` live in `C:\Claude\community-bus-maps\.env`, which it loads for itself; with neither set and no `--local` it prints the two lines to add and exits 2 rather than opening the dev SQLite. `--local` is the dev checkout and beats a configured `BUSMAPS_URL`; `--local --url` together is a refusal. **Say which portal it was in the first line of your answer to Peter.** ([why](references/why-these-rules.md#remote-portals--the-honest-state))

**Send the TOKEN, not a cookie.** `BUSMAPS_TOKEN` carries the portal's `OPERATOR_TOKEN` as a Bearer header and is read-only over three GET routes; a `cbm_session` value is a PERSON's live admin login and only four portal routes sit behind step-up. Rotate with `npm run rotate:secret -- OPERATOR_TOKEN` from `C:\Claude\community-bus-maps`, which never prints the new value — read it out of the host's `.env`.

**Demo rows are hidden unless `--demo`, and the test for a seeded record is the ADDRESS, never the name.** RFC 2606 and RFC 6761 reserve `example.com` and the `.example`, `.test`, `.invalid` and `.localhost` TLDs, so an applicant who cannot be emailed is nobody waiting. Absence of a demo label is not evidence of realness.

**The operator half is browser work.** Accepting a staged refresh, withdrawing a publish request and changing a map's outputs need a signed-in admin session; reading and delivery both work from this laptop.

## Step 2 — Pick one

- If the invocation already names one ("do the St Ives refresh", "build the Waitrose map", "/bus-work 3"), take it and skip the question.
- Otherwise show the list and ask which — **one message, one question**. Recommend the top item unless something lower is obviously more urgent, and say why in a sentence.
- **Prefer a `SAFE NOW` row when the top one is `BETTER TO DELAY`**, and say that is why. A held-back row is not dropped from the list and Peter can still choose it — the verdict is advice with its evidence attached, not a lock — but the recommendation should be something he can finish. If he takes a delayed row anyway, do the thing the reason names first: read the uncommitted files, or get the portal checkout back on `main`.
- If the list is empty, say so plainly and stop. Do not invent work.

## Step 3 — Do it

**First, unless this item builds or renders a map, do it in a worktree** — `EnterWorktree`, or `git worktree add .claude/worktrees/<name> -b work/<name>` from the repository root (OA-395, R2 of the 2026-09-17 process review: one writer per tree). A `gate` diagnosis, an approval-gate `review`, an `awaiting-customer` nudge and a `draft-unsubmitted` evidence pass all write only tracked files and belong in a worktree. A `build`, a `refresh`, a `refresh-local` and any `housekeeping` that re-renders a sheet write a map's untracked S4–S6 output, which a worktree cannot keep, so those stay in the main checkout and take `loop/LOCK.d` for their duration. The full convention is the *So: what belongs in a worktree* section of buses-data's `Documentation/README - Working in parallel.md` (named rather than linked, because a relative link across a repository boundary 404s for everyone but this laptop).

Load [references/playbooks.md](references/playbooks.md) and follow the playbook for that item's `type`. One item at a time, start to finish, before offering the next.

The types and where each ends up:

| `type` | What it is | Ends with |
|---|---|---|
| `gate` | the engine no longer reproduces a committed map | diagnosed; nothing else ships until it is |
| `review` · `application` · `request-decision` | one of the **three approval gates** | Peter decides in the portal UI — **never decide for him**. An AREA request carries the town's **complexity band** off its newest S2 run, or says UNSCORED with the S1→S2 step that scores it for free (buses-data OA-088); RED is the pipeline's one *not a single-sheet town* verdict, and approval is the quota gate, so the band belongs BEFORE the decision |
| `build` | an approved request with no map yet | map imported, v1.0 verified byte-identical |
| `refresh` | a portal map whose services are changing | proposed update staged for the customer |
| `refresh-local` | a town leaflet with no portal map | new S5 render + `_latest` refreshed |
| `housekeeping` | engine-stale renders -- **one `engine-rebuild-<map>` row per map since 2026-09-22** (buses-data OA-430), towns and places alike, because each map is gated against the engine recorded in its own reference and so being behind is a chore a tick can work one at a time -- missing S6, **S6 claims** — a red-team claim about a service with no home, or the queue of `service-facts.json` entries awaiting a decision (buses-data OA-273) — since 2026-09-17 a **deploy pending** on the portal (`deploy-pending`, buses-data OA-396), and since 2026-09-18 the **monthly BODS scan not having run** (`bods-scan-overdue`, buses-data OA-402, R9). None of these reddens the board any more: each is a chore, and this row is what chases it | rollout applied / S6 run / every claim has a home and the queue is worked one entry at a time / the portal deployed / the monthly refresh re-run |
| `awaiting-customer` | staged, they haven't accepted | a nudge, or nothing |
| `draft-unsubmitted` | a version saved and never sent for review | evidence prepared; Peter submits and approves |

## Step 4 — Close out

Every time, without being asked:

1. **Re-run the worklist** and confirm the item is gone. If it is still there, it is not done — say so.
2. Tell Peter what changed in one or two lines: the version number, the map, the customer, what the next actor is.
3. If anything about the *procedure* proved wrong or fiddly, fix it here (this file, the playbooks, or `worklist.mjs`) in the same session rather than leaving it for the next one.

## Before you push — `preflight.mjs` answers *what would go red if I pushed now*

**Run it once per ROUND, before the first push of a change that reaches map data, the engine or a workflow** (buses-data OA-343, from Peter on 2026-09-14: *I want us to avoid the system being in a BROKEN state*). Landing OA-338 left `buses-data` `main` red for about seven hours and met four independent blockers one at a time — byte-gate control diff, portal vendoring drift, the deployment row BEHIND, the portal's own `verify:area` on a stale committed fixture. Every one of them was true before the first push and answerable on this laptop in under two minutes, and three CI round trips were spent discovering them in sequence. From anywhere, with the repository written into the command rather than stated beside it, and with no placeholders:

```bash
node "C:/u3a St Ives/.claude/skills/bus-work/assets/preflight.mjs" --repo "C:/u3a St Ives/Using AI/Buses"
```

**It asks what the push CONTAINS first, and the answer decides how much of the rest to run.** `origin/main..HEAD` gives the paths; a push touching only the documentation folders `gates.yml` already treats that way runs the cheap tier, and anything reaching `Areas/`, `Places/`, `.github/` or a root dotfile earns the full one, which includes the unsuppressed board and the area fixture. That is not thrift for its own sake: hand-run before a docs-only push the cheap version yielded zero true findings and one false alarm, and a preflight whose false positives outnumber its true ones is one somebody stops running. `--all` forces the full tier, `--json` prints the machine-readable form, `--repo <path>` names a repository other than the one you are standing in.

**Four properties are load-bearing and each of them is a thing that went wrong.** It never stops at the first failure, because CI's job ordering is what hid three of the four blockers — the named shape *the blocker behind the blocker*. It suppresses nothing: no `--no-live`, no `--register-only`, because the deployment row is exactly what `--no-live` hides. A check whose tool is missing is UNANSWERED and takes the exit code with it, never a pass. And the report always ends with **what it did not ask** — whether the portal suite is green, anything needing the network, and, when the engine checkout is not clean and level with its own `origin/main`, the fact that a local byte-gate verdict was measured against an engine CI will not check out.

**Exit codes are this repository's: `0` nothing would go red, `1` something would, `2` it cannot tell you** — an undeclared repository, a branch with no upstream, or a check it could not run. A repository declares its own checks in a `.preflight.json` at its root, exactly as it declares its documentation scope; `buses-data` and `claude-skills` have built-in manifests, and a repository matching neither is a refusal rather than a clean bill of health. `npm run test:prove-red-preflight --prefix "C:/u3a St Ives/.claude/skills/bus-work"` is the harness.

## Rules that override convenience

- **Never decide an approval gate.** Organisation approval, map-request approval, and publish review are Peter's judgement and the system's integrity. Prepare the evidence, summarise it, open the URL — then stop.
- **Never hand-edit a rendered file.** Corrections go through a new version, always.
- **Never relax a gate to make it pass.** A `verify` that says DIFF is information, not an obstacle.
- **If you push something you already know will go red, BEGIN the commit subject with `[expected-red]`** (OA-251, 2026-09-05). `ci_state.mjs` reads it off the run's `displayTitle` — which is the commit subject — and ranks that repository's red row at 8 rather than 0 for six hours, so the next session can see somebody meant it. **At the START, not merely somewhere in the message**, or a commit whose body only *describes* the convention trips it — which is exactly what the first version did. **It changes nothing about the email, and do not try to make it:** GitHub titles a "Run failed" notification from the workflow's static `name:`, so nothing can vary one failure email's subject against another's — measured 2026-09-05 on a deliberate red (run `33970616137`), whose mail reads `[PeterC66/claude-skills] Run failed: Bus leaflet gates - main (5bb4752)`. A `run-name` expression was fitted to all six push-triggered workflows across the three repositories and **removed the same day**: it reached the email's BODY, and Peter's inbox list does not show the body. A line nobody sees is not a signal.
- **`npm run verify` skips silently** when its fixture dir is unset — a green run with no byte counts proves nothing. Require the word PASS *and* byte counts before calling an import verified.
- **PowerShell is the shell.** `FIXTURE_DIR=… npm run …` is bash and silently does nothing here; use `$env:FIXTURE_DIR = "…"; npm run …`. The worklist already emits the PowerShell form.
- **Stop the dev server before any command that writes directly** to a *local* portal (`import-map.mjs`, `propose-update.mjs`, `seed-demo.mjs` run in place). Reading — including this worklist — is fine either way. `npm run deliver` (below) is different: it runs against the live VPS over SSH and stops *that* service itself, briefly, as part of its own sequence — it doesn't touch your local dev server at all.
- **Delivering to the live site is `npm run deliver`, run from the laptop** (item 4, 2026-08-10 — see below). There is no longer a "must run on the machine that hosts the portal" restriction for this. Each call emails the customer and briefly restarts the portal, so a multi-map pass means one of each *per map* — warn them first.
- **The operator actions after delivery are still HTTP endpoints needing an admin `cbm_session` — withdraw a publish request, change a map's outputs, and a single accept/publish are still browser work.** But a *batch* of several already-reviewed maps has a script now: `community-bus-maps/scripts/accept-publish-batch.mjs` (`npm run accept-publish`, added 2026-08-19, PR #54) drives withdraw→accept→submit→approve→live-verify for a named list of staged maps in one run, and sends ONE grouped digest email per customer for the whole batch instead of one "is published" email per map (`suppressNotify` + `POST /api/admin/notify-published-batch` — a batch used to mean N emails to the same inbox, confirmed via `/health?deep=1`'s `customers` count on the 2026-08-19 pilot). **This does not relax the rule above it.** `--reviewed-by "<name>"` is mandatory and the script never looks at a rendered sheet itself — it only executes a decision that has already been made by someone who opened every sheet at full resolution first, same as this skill preparing evidence and stopping before a gate. Two orderings the portal enforces in code and will surprise you otherwise, whether run by hand or through the script: an open publish request makes the accept fail with a **409** until it is withdrawn, and an expert output (`internal_schematic`, `boarding_plan`) cannot be switched on until the map’s live `routes.json` carries its opt-in key — so that toggle is gated on the refresh landing first. **And a third, which the batch script does NOT handle: a map carrying a boarding plan needs a version SAVED between the accept and the submit**, because granting an output renders nothing and this is the one output with no other act to carry the render — run the script on such a map and it publishes it with the sheet missing. See `references/playbooks.md` §*Output toggles*. **For the full step-by-step — where to review (`Collected_latests`, not the portal), the exact command, what every flag means — see `references/playbooks.md` §*bulk-accept-publish*, not this bullet.**

## This worklist now has an UNATTENDED consumer, and two of its outputs are load-bearing to it

Since 2026-09-07 a scheduled task in `buses-data` — the design is `loop/README.md` there — works this worklist without a person, one row per timer tick, each tick a fresh session. It depends on two things this skill emits, so **check them before changing either shape**.

**`worklist.mjs --conditions --json`.** The loop reads the per-resource verdicts out of the JSON and stops unless every resource its intended work touches says `safe`. It never reads the exit code, because that command always exits 0. **An unattended run collapses `CHECK FIRST` into stop** — the three bands were written for somebody who can go and read whose uncommitted files those are, and there is nobody. So a verdict that becomes vaguer, or a resource key that is renamed, silently changes what an unattended run will refuse to do. **And the subtraction runs the other way**: since OA-301 the `buses-tree` verdict leaves out a held letter that a `loop/your-move/` hold names, so the JSON can say `safe` while `git status` shows a modified file — a tick that re-reads the tree by hand and treats that as a contradiction has misread the design. The file is in `conditions.repos.buses.accounted`, with the hold's ref, and it is never a tick's to touch. **Since OA-434 (2026-09-22) dirt inside one map or letter folder is fenced as well**: unstaged dirt under one `Areas/<X>/`, `Places/<X>/` or `Correspondence/CORR-nnn/` folder, outside `ci-reference/`, leaves `buses-tree` and is counted by `buses-maps`, which only work that writes into such a folder needs. The JSON's `resources` block gives one verdict per resource, and that block is what a tick reads.

**`rank`, and the fact that rank alone is not enough.** The loop takes the lowest-ranked row it can **finish without a human**, which is a filter this skill does not express: of the twelve rows standing that morning, six were blocked on Peter or on a third party and would have sat at rank 1 for ever — two publication reviews, two drafted-but-unsent replies, questions asked locally, an update waiting on a customer. It also learned the hard way that **a CI red belonging to another session's in-flight vendor inside its grace is not work to pick up.** If a future row type is added whose rank implies actionability it does not have, that is where it will go wrong.

Both rules are stated for the loop in `loop/README.md`; they are repeated here because this is the file somebody edits when they change the worklist, and the coupling is otherwise invisible from this side.

## What feeds the worklist

Nothing here is a new source of truth; it is a join over what already exists.

| Source | Gives |
|---|---|
| the portal's own `src/worklist/index.js` — **imported locally, fetched (`GET /api/admin/worklist`) when remote; never re-implemented** | ranks 1–6 and 9: publish reviews, applications, map requests, awaiting-build, refresh flags, proposed updates. The admin console's To-do tab renders the same call, so the two cannot show different lists |
| `_gtfs/upcoming/upcoming-report_<date>.md` | which towns have service changes coming, matched to maps by the *same rule* `check-upcoming-refreshes.mjs` uses |
| each map's `refresh-reviews.json` | which of those refresh rows have ALREADY been adjudicated against the current scan and found not to need a rebuild. Suppresses the row and says so; a newer scan brings it back |
| each town's `manifest.json` + `routes.json.engine` vs `engine_version.js` | which renders pre-date the current engine, and how stale S6 is |
| `status.js --json` (only under `--gates`, or as pushed by `push-status.mjs`) | the expensive proof: regenerate everything and diff |
| `Correspondence/CORR-nnn/` message headers, and each map's `local-decisions.json` | rank 2 a reply owed to a real person, rank 3 **a reply drafted and not sent**, rank 9 **a drafted reply deliberately HELD** and a question asked locally and never answered |
| each town's newest S2 run, `complexity.json` — read by `complexity_band.mjs` | the **complexity band** on an area map request or build row (rank 3/4): GREEN, AMBER, RED with the measure that tripped it, or UNSCORED. The portal cannot score a town (it holds no bus data); this machine can, without spending quota. Peter's guide had promised him this at gate two since 2026-08-21 and nothing did it until 2026-09-05 |
| each AREA map's landmark answer — `GET /api/maps/:id/poi-tiers` when remote (the operator token's third route), the store's `overrides.json` + pack `routes.json` when local — against the town's latest S3 and S4 `routes.json`, compared by the ENGINE's `poi_tiers_sync.js` (OA-233, 2026-09-05) | rank 7 **`landmark-owed-<slug>`**: the portal holds an answer the source lacks; rank 7 **`landmark-unbuilt-<slug>`**: the source carries an answer the latest build has not drawn — the row no byte gate can raise, because the gate reads the build's own config. A portal older than the route is SKIPPED and counted in the header. Playbook `landmarks` |
| `Development Docs/commitments.json` | rank 4 **a dated commitment now OVERDUE**, rank 7 one inside its warning window. Silent outside it |
| `BusMapsUK/bus-map-directory/link-check.json` — the dated record `directory.mjs --links` writes — read by `directory_links.mjs` (buses-data OA-308, 2026-09-11) | rank 7 **`directory-links-dead`**: the last sweep found URLs that are GONE, every one named with the directory rows citing it; rank 8 **`directory-links-due`**: that sweep is over a month old, or has never run; rank 8 **`directory-links-record`**: the record cannot be believed. Silent on a sweep inside the month with nothing dead, and silent in any tree that has no `bus-map-directory/` at all |
| `git status` / `git branch` in the three repositories, and the `selected:` line of every open action — read by `concurrency.mjs` | not a rank but a VERDICT on every row: what is safe to start right now, what to look at first, and what to leave until a neighbouring session has finished |
| `gh run list` on each of the three repositories' default branch — read by `ci_state.mjs` (OA-251, 2026-09-05) | rank 0 **`ci-red-<owner>/<repo>`**: that repository's last run in GitHub Actions is still failing, how long it has been failing, and the failing step names. Rank 8 instead, for six hours only, when the triggering commit's subject BEGINS `[expected-red]` — the marker buys grace, not amnesty. **Past the grace the rank returns to 0 and the row now SAYS the marker is there** (buses-data OA-341 hole C, 2026-09-14): the message used to branch on `excused` alone, so the moment the six hours expired it printed *NOTHING says anybody expected it* over a `predicted` that was still true, which sent a scheduled tick re-deriving a four-repository chain the head commit's own subject announced. Three states, one rank each: predicted-and-fresh at 8, predicted-and-stale at 0 **naming the marker**, unmarked at 0 saying so plainly. This is the one source whose only other channel was an email to Peter, and that channel was measured dead — see below |

**A refresh row you have answered can now be cleared without rebuilding the map (buses-data OA-205).** A `refresh` row is a JOIN against the newest scan report, so until 2026-08-31 nothing could clear one except doing the rebuild it asks for — and on 2026-08-31 all 40 High Wycombe items were worked to a conclusion, none of them needed a rebuild, and the row came back unchanged with the same 40 on it. If you adjudicate a scan and the sheet does not need to change, **record it**, from the `bus-work` assets folder (`C:\u3a St Ives\.claude\skills\bus-work\assets`); `--map` is the map's OWN folder, the one holding `manifest.json`, and `--scan` must name a report that exists under `_gtfs/upcoming/` or the command refuses:

```bash
node refresh_review.mjs --map "C:/u3a St Ives/Using AI/Buses/Areas/High Wycombe" --scan 2026-08-31 --verdict no-rebuild --by <this session's name> --note "what you found, in one sentence"
```

The row disappears and the worklist header says how many it suppressed and why — it is never silently dropped. **A newer scan brings it straight back**, because the match is on the scan date and nothing else, and `--verdict rebuild-needed` records the reading without silencing anything. **Adjudicating a town does NOT adjudicate a place inside it**: a place's frame draws a different set of services, so it keeps its own row and its own file.

**Re-adjudicating a scan you have already adjudicated REPLACES that scan's entry, and since 2026-09-09 it keeps the one it replaced (buses-data OA-290).** Replacing is deliberate — `worklist.mjs` suppresses on one review per scan, and two entries for the same scan with opposite verdicts would leave the board's behaviour decided by whichever a `.find()` reached first — but until that date the displaced entry was **dropped**, while the `_readme` the same script writes into the same file six lines above promised *APPEND, never rewrite: the history is the point*. It cost a real note: on 2026-09-09 a re-adjudication of Huntingdon removed 900 characters explaining why that row had stood for six days, and the only signal was the past participle in `replaced scan 2026-09-01 as no-rebuild`. The displaced entry now moves into `superseded[]` **on the entry that displaced it** — oldest first, flat rather than nested, so a three-step reversal keeps its middle — which leaves the board's join untouched and puts the prose back where a reader will find it. The tool also prints what it displaced, and `--list` shows a superseded entry indented under the one that replaced it. An adjudication note is not a status field: it is the evidence somebody assembled, and the reason a verdict was reversed is exactly what the next reader needs. Falsified in both directions by `assets/prove-red-refresh-review.mjs`, whose control asserts that a FIRST review of a scan carries no `superseded` key at all — without it, a tool stamping an empty array on every entry would pass.

**The last three sources are read from THIS LAPTOP and are identical in every mode.** Only the portal queues change between `--local` and `--url`: a stale render, an upcoming BODS change and an unsent letter are facts about this disk, not about a portal. So the same correspondence row appearing in a dev run and again in a live run is **correct, and not duplication** — there is one draft on one disk, and it belongs to neither portal. If it ever appeared twice inside a single run, that would be a bug.

**Ranks 0 and 8 also reach the portal itself** (item 3, 2026-08-08): `push-status.mjs` POSTs the same `status.js --json` output to `POST /api/admin/status`, the portal stores the latest snapshot, and `src/worklist/index.js` folds it into ranks 0/8 there too — so a failing gate or a stale engine shows on the admin console's To-do tab and to a remote reader, not only to whoever last ran `--gates` on this laptop. It is a snapshot, not a stream: stale until the next push. Rank 7 (a BODS-flagged town with no portal map) is not pushed — it still only comes from this tool reading `_gtfs/upcoming` directly.

**Why commitments are in here too, added 2026-08-31, and why they are not the same source.** Correspondence answers *is a real person waiting*. Commitments answer *did we do the thing we said we would*, and the difference is that nothing on disk changes when the answer is no. A letter WE chose to write — the OSMF licensing enquiry, the solicitor instruction — has no thread, no counterparty record and no artefact to interrogate; neither has a credential three months from expiry. `status.js` grew a Commitments section for exactly this and fails the board once a date passes, but a red daily CI run is only a reminder if somebody reads the email, and this project has already had `gates.yml` red for **167 runs across twelve days** without anyone noticing. This puts the same rows on the list you actually work from. **It is deliberately silent until the date is close**, because a worklist that prints an obligation eighty days out is one nobody finishes — the same rule the correspondence source follows when no reply is owed. Retire an entry by DELETING it from the JSON, exactly as a finished OA file is deleted; a list that keeps its dead rows stops being read. Falsified by `node assets/prove-red-commitments.mjs`, which pairs every case: make the state and see the row, clear it and see the row go.

**Why correspondence is in here at all, added 2026-08-31.** A reply to the first member of the public who ever wrote in was drafted on the 30th and was still unsent a day later, with eighty commits in between. The backlog work that same message raised was picked up promptly — another session read the open actions, claimed one and released half of it overnight — because the backlog is indexed, checked in CI and read by every session that starts. **The one step nothing could do for him had nothing watching it.** That is the asymmetry: everything Claude can do is picked up by the next session, so the only step with no reminder is the human one, and it is the only step that a person on the other end is actually waiting on. Rank 3 is deliberately in the *someone is blocked* band rather than *your move* — because they are.

**A thread may declare itself finished, and rank 2 then says nothing (2026-09-07).** *Is a reply owed* was answered from the direction of the last message alone, and a courtesy acknowledgement is inbound like any other: CORR-002 ended on *thank you, I have forwarded it*, and this tool called that a person waiting — for eighteen days, at rank 2, above every row an unattended run could actually finish — while the thread's own record said in prose that nothing was outstanding on either side. That is the named shape *the answer written in a key nothing reads*, and the fix is the move the outbound branch already made: **read the declaration rather than infer it.** A `CORR-nnn/README.md` silences its rank-2 row when **both** hold — its `**Status:**` begins with `dormant`, `closed` or `settled`, and the record's **docstamp is dated on or after that last inbound message**. The closed vocabulary means unrecognised prose keeps nagging rather than going quiet, and the stamp is what stops this becoming a reminder that never fires: when the correspondent writes again the stamp falls behind the new message by itself, with no upkeep, and the row comes back. **It suppresses rank 2 only.** An unsent draft at rank 3 is the one step nobody else can do, so no wording in a record we write ourselves can take it off the list — `prove-red-correspondence.mjs` asserts that directly, alongside the three refusals (stamped too early, unstamped, and a status reading `open`).

**One thing MAY move an unsent letter off rank 3, and it is a declaration rather than a wording (2026-09-17).** The immunity in the paragraph above is right and is unchanged, and it was also hiding a letter nobody was waiting for: CORR-004's reply sat in `SOMEONE IS BLOCKED` for ten days under *"until it goes, the person has heard nothing"*, which was true and was not a debt — **he never wrote to us**, having given his views to a fellow campaigner who asked for them, and that thread's own record had said so since the day it was written. So the message's header may carry `**Held:**` and `**Revisit when:**`, and with **both** present the row demotes to **rank 9, WAITING ON OTHERS** — which is what a letter waiting on a condition outside this project is. **The condition is the load-bearing half**: a `**Held:**` with no `**Revisit when:**` leaves the row exactly where it was and says why, because a hold with no route back is how a letter is quietly abandoned. It demotes rather than disappearing, deleting the two lines puts it back the same minute, and the green that matters most is that a leftover hold on a letter that has **gone** raises nothing at all — the rule is read inside the unsent branch for that reason. The convention is `Correspondence/README.md` in `buses-data`, deliberately not linked here because a relative link out of this repository resolves on one laptop only. Suppressed threads are named in `meta.correspondenceSettled` and printed to the console, for the reason `adjudicated` is: a suppression nobody can see is how a board starts lying.

**Both ends of one letter were wrong, and the loop found it by being dispatched to the false row (2026-09-08).** The reply to CORR-002 message 010 is filed in **CORR-005**, because he forwarded somebody else's suggestion and the answer was addressed to her and copied to him. This source reads a thread's own folder and nothing else, so it got the same letter wrong twice in opposite directions: **CORR-002 raised *a reply is owed and not drafted* at rank 2** an hour after it was drafted, and **CORR-005 raised nothing at all** — because its outbound message declares `DRAFTED, NOT SENT` in its **H1 title** and carries no `**Status:**` field, and only the field was being read. On that day 3 of the estate's 12 outbound messages carried the field while **10 of 12 carried the H1**, so the tool was reading the rarer of the two sites the convention actually uses. The second half is the one that matters: rank 3 is the row this whole source exists to raise, a real member of the public was waiting on it, and *nothing* was watching. Two fixes, each a declaration rather than an inference. **The outbound state is read from the H1 as well as the field, and either one saying it has not gone raises the row** — a contradiction nags rather than going quiet, and `\b` is what stops "redrafted" inside a SENT title nagging for ever. **An inbound message answered in another thread carries `**Answered by:** <path>`, and the pointer must RESOLVE** — which makes it stronger than the prose declaration above, since a typo or a rename silences nobody, and it needs no docstamp test because the field lives inside the message it is about. The suppressed thread is named in `meta.correspondenceSettled` like any other, and the letter appears exactly once, in the folder it lives in. The convention for both is written up in `Correspondence/README.md` in the `buses-data` repository — deliberately not linked, because a relative link out of this repository resolves on this laptop and nowhere else. Five new harness cases were watched go red against the old reads before either fix was believed.

**The scheduled loop had a channel to Peter and this list had never heard of it (OA-283, 2026-09-08).** A tick never asks a question: one that needs a decision writes `loop/your-move/<ref>.md` in `buses-data` and stops, and that folder is its whole outbound channel to a person. `loop/README.md` justified it in one sentence — a growing pile is *"visible in the run notification rather than rotting in a folder nobody opens"* — and both halves were false. There is no notification he reads; the run summary that names the folder is itself gitignored, per-tick and enumerated by nothing. His own words, reading `sched-2115`: *"I would only pick those up if I looked at the run itself."* **The live failure was not a missing row, it was a contradicted one.** `st-ives-v10.2-river.md` said in terms *do not send v10.2 for review* — that draft draws the river in seven fragments, one ending at y = −263.76 mm on a 210 mm page — while this list's own `draft-1` row said *"Open the map, check the sheets, then Send v10.2 for review"*. The loop found the fault, wrote the warning where the design told it to, and the board went on recommending the thing the warning forbade.

So `loop_your_move.mjs` reads that folder: **one row per hold at rank 3**, in `SOMEONE IS BLOCKED` beside an unsent reply and for the same reason — only a person moves it, and every tick that fires meanwhile does nothing about it. Its title is the file's H1, its reason is the file's own *What is needed* paragraph, and its age is the date the file states, falling back to mtime. A hold may also name the rows it contradicts in a **`**Blocks:** <key>`** field, and each named row is then **annotated, never dropped**: the warning prints above the row's commands and gates them behind *Only once that is settled*, so the row keeps its place, its age and its link but cannot be read as an instruction. A `Blocks:` naming a key that is not on the board today is reported as a warning rather than ignored, for the reason `adjudicated` is printed — a stale hold is how a convention quietly stops working. Both halves also ride in `--json`, on the item as `onHold` and in `meta.warnings`.

**And the DRAFTS in the same folder have a row too (2026-09-10).** A tick saves a draft when it finds something it cannot act on — a fix it was barred from making, a finding that needs a decision — and nothing in the loop will ever action one, which was right; but it was also enumerated by nothing, and five drafts sat there for up to two days each ending *"promote it, or file it as an open action, if you agree"*, addressed to a reader this list had never shown the folder to. The same module reads them: **one row for all of them at rank 7**, the bottom of `YOUR MOVE`, with the count, the oldest age and each draft's H1, date and tick. The three dispositions are on the row: promote (a `**Promoted:**` line under the H1, then move to `loop/adhoc/ready/`), file (an OA, then move to `loop/adhoc/done/` saying where it went), decline (a `## DECLINED` section, then `done/`). Never delete one; the reason is the record.

**The two used to be two folders, and OA-401 made them one (R8, 2026-09-18).** A hold went to `loop/blocked/` and a draft to the top of `loop/adhoc/`; they differ in urgency and in nothing else, and the review counted nine possible homes for a question aimed at Peter with one question sitting in three of them at two ranks. So there is one folder now, named for the band the board already prints these rows in, and **the FILE says which it is rather than the folder**: a *What is needed* section or a `**Blocks:**` field makes it a hold, and neither makes it a draft. That is not cosmetic. Under the old design a hold written into the drop zone in a hurry could not be seen as a hold by anything, at any rank, ever — and when the folders were merged, **two of the eleven drafts turned out to carry an ask**, one of them undecided since 2026-09-16, sitting at rank 7 inside an aggregate line instead of at rank 3 with a row of their own. Now a misfiled hold is read as a draft, which is a lower place and never a silent one, and a draft that grows an ask is promoted at the next run. `loop/adhoc/ready/`, `doing/` and `done/` are untouched and are a different channel — Peter's in-tray INTO the loop, the dispatcher's crash rule, and nobody — and `prove-red-loop-your-move.mjs` holds that line with a file in each, alongside a case that the partition is total and disjoint so nothing can be counted twice or lost between the two readers.

**Until 2026-09-17 the loop could never push, and until OA-326 (2026-09-12) nothing enumerated what that left behind.** `Bash(git push…)` was denied to an unattended tick, and that containment was the reason a tick could write to the map trees and to `claude-skills` at all — a wrong edit sat in an unpushed commit until a person read it. buses-data OA-394 (R1 of the process review) then gave the tick the push at the end of its unit, behind the push preflight, so a row here now means a push that did not happen rather than one that could not. What was missing, and is still what this source provides, was the channel: on the day this was filed, `check/schema-version-oa325` sat in the portal with **463 insertions and its own falsification harness**, no pull request and no hold in that folder, while this board printed `the portal  community-bus-maps — main, clean`. **`countUnpushed`, in the conditions block above, could not have seen it**: it counts `<basis>..HEAD` in the three main checkouts, so it answers only for the branch each checkout happens to have out, and a branch held in a git **worktree** — or checked out nowhere at all, the normal end state once a worktree is removed — is invisible to it. The worktree convention is spreading, so the blind spot was widening.

`unpushed_branches.mjs` asks the question of every local branch instead: **one row per stranded branch at rank 3**, in `SOMEONE IS BLOCKED`, carrying the branch, its age, its insertion count, its last commit subject and a self-contained `git -C "<repo>" push -u origin <branch>`. **Computed rather than declared**, which is the point: extending `loop/README.md` to tell every tick to declare its own residue would have been the cheaper change and it is the wrong one, because a declaration can be forgotten and on the day this was found it had been. A branch that a `loop/your-move/` hold **also** names still raises its row — the computed half must be loud exactly where somebody remembered, or it could not be told from the half that is silent where nobody did.

**The trap is that it must be computed from PATCH IDENTITY, never from ahead-count.** The portal squash-merges with `delete_branch_on_merge` on, so every branch it has ever merged stays ahead of `main` for ever: measured there on 2026-09-12, **34 local branches, 27 ahead of `origin/main`, 8 unmerged by `git cherry`, 6 raised**. A row built on ahead-count would have raised 27 items of which 24 were finished work, and it would have been muted inside a week. **And `git cherry` is not enough on its own, which was measured rather than reasoned**: a squash of a *single* commit keeps that commit's diff, so its patch-id still matches and `cherry` correctly says merged — but a squash of *two* produces one diff matching neither, so `cherry` calls it unmerged for ever. Both of the portal's two-commit strays are exactly that, `schema-note-token-column` (landed as `a12d247`, #127) and `tabs-read-as-buttons` (`709a9b5`, #125). The discriminator that rescues them was already on the disk: their upstream is **recorded and gone**, which in a repository that deletes a branch on merge is the remote saying the work landed.

**What it does not ask, said at the point of the narrowing — and the half that WAS built on 2026-09-21.** OA-326 specified `gh pr list --head <branch>` as the second half of the test, and this source still does not make that call: `worklist.mjs` touches the network only in `--url` mode, and a board opening a socket per branch would fail on a train. So a branch that **has** been pushed raises nothing here, whether or not a pull request is open for it, and the pushed-but-unmerged count is printed as a warning so the half not being asked is visible rather than silently absent. What has changed is that the question now has an instrument of its own, in the directory sweep's shape: `pr_sweep.mjs` below.

**Its first real run named six branches, three of which nothing in the estate knew about** — `oa059-pinned-dp`, `oa322/place-page-size-literal` and `oa323/tier-4-1-for-the-place-skill` in `claude-skills`, 574 insertions between them, none of them mentioned in any hold. `prove-red-unpushed-branches.mjs` builds a bare origin, a clone, a worktree and a squash-merge history under the temp dir, and **five of its assertions are controls asserting silence**, because the way this source realistically fails is by being right too often. A six-mutation sweep watched all six go red, and two of its results are worth carrying: replacing patch identity with ahead-count reddens **exactly one** assertion, the single-squash control, so that control is the whole defence against the change the module argues hardest against; and *each says how much work is sitting there* was first written as `/insertion/` over the row's own sentence, where it could not fail — the word is there whatever the number is — so it asserts the count now.

**The pull-request half, and why the guarantee above turned out to be too weak (OA-326 item 1, 2026-09-21).** The narrowing rested on *a pushed branch is on GitHub for anyone who looks*, and the measurement is that nobody looks: portal [#170](https://github.com/PeterC66/community-bus-maps/pull/170) was open **21 days** and named on no list until the day it merged, dependabot [#64](https://github.com/PeterC66/community-bus-maps/pull/64) has been open **31 days**, and #284 and #285 eight days each. `pr_sweep.mjs` closes it without putting a socket in the board: `node pr_sweep.mjs` asks GitHub and writes `loop/pr-sweep.json`, and the board only ever reads that record — the writer sits behind an entry-point guard, the reader is pure and takes its clock as an argument, and `worklist.mjs` imports the reader alone. The record is gitignored by `loop/*`, which is required rather than convenient: it is dated, and **a generated file under byte comparison must not read the clock**.

**Two questions, each asked with the instrument that can answer it, and neither derived from the other.** `gh pr list --state open` once per repository finds a stall and cannot miss one; `gh pr list --state all --head <branch>`, asked **per branch and over every state**, answers *has this branch ever had a pull request*. The second form is not a preference. On 2026-09-19 a tick scanned the branches this board prints and inferred absence from a list it had already filtered to OPEN, and reported that `worklist-demo-applications` had never had a pull request at all — #170 had been open on it for nineteen days, and the claim was repeated to Peter two days later before anybody checked it. **Inference from a filtered list answers ABSENT, and absent reads as a finding**; a branch with no pull request and a branch with a stalled one are different faults wanting different fixes, and no amount of branch-scanning would ever have surfaced the second.

**Four rows, and the silence is again the common case.** `pr-sweep-stalled-<repo>-<n>` and `pr-sweep-no-pr-<repo>-<branch>` at **rank 3**, `pr-sweep-due` and `pr-sweep-record` at rank 8; `needsOf` answers `[]` for the first two on the same load-bearing argument as `ci-red-` and `unpushed-branch-`, because `--safe-only` must never hide the row saying finished work has been sitting open for three weeks. A **DRAFT raises nothing** and is carried in a warning — `claude-skills` #3 and #4 forbid their own merge in their titles, and a row nagging about those two daily would be muted inside a week and would take the other rows with it. Staleness is measured from `createdAt`, **not** `updatedAt`, and that was measured too: #64 read 31 days open and 10 days quiet, so an activity test is reset by a bot comment and a pull request nobody means to merge evades it for ever while looking busy. The last-activity age is in the row's detail and is never the trigger.

**`prove-red-pr-sweep.mjs` is fourteen sections and a nine-mutation sweep put all nine red.** Section 11 is the one holding the shape in place: it asserts that the half `worklist.mjs` imports runs no `execFileSync` and no `gh` call, so nobody can "tidy" the sweep into the board. Section 8 is the one it was written for, asserting against a stubbed GitHub that the per-branch query carries `--state all`, that a MERGED pull request answers it, and that a **refusal is not an absence** — `gh` failing leaves the branch unmentioned rather than inventing a finding out of a network error. Two results are worth carrying. The entry-point guard cannot be written as `new URL(import.meta.url).pathname`: on Windows that is `/C:/…`, the comparison silently fails, and the sweep becomes unrunnable with no error to read. And a **missing row must fail the assertion that names it, not throw on the line after it** — widening `STALE_DAYS` first killed the harness with a stack trace instead of naming the control it had broken, so every indexed read goes through `one()`.

**A check that runs by hand runs once, unless something asks — so the national bus-map directory gets a MONTHLY row (OA-308, 2026-09-11).** `BusMapsUK/bus-map-directory/` records, for every English local transport authority, whether it publishes a bus map and where; it is a hundred-odd URLs belonging to seventy-six councils, and it rots without anybody here touching a file. `directory.mjs --links` is the check, and OA-308 ruled it **out of CI on purpose**: a council page dying on a Tuesday would turn `main` red with nobody having committed anything — the calendar-red shape OA-289 took out of the backlog index — and `buses-data` is the one repository in the estate whose Actions minutes are billed. That reasoning is sound and it leaves the check with no reader, which is what this row is. **It opens no socket.** The sweep WRITES `link-check.json`; `directory_links.mjs` only ever reads it, because a source that fetched 109 council URLs would turn a half-second board into a minute-long one and fail on a train.

**DEAD outranks DUE, and BLOCKED raises nothing at all.** Running the sweep is housekeeping; a dead link is a row of the directory pointing a member of the public at a 404 while the `/maps` panel tells them their council publishes a map, so it sits at rank 7 and names every URL with the directory rows citing it. A **BLOCKED** result is a bot wall refusing an automated reader — on the checker's first run 12 of the 14 "dead" links were HTTP 403 on pages the survey agents had opened in a browser that same morning, which is *the refusal read as an absence* — so blocked URLs are recorded, counted in the row's prose where a reader can see them, and are never a reason to act. The trigger for the whole source is `directory.json`, not the record: a tree with no directory is silent, a tree with a directory and no record has **never been swept**, and collapsing those two would make "never run" indistinguishable from "not applicable".

### The weekly document triage — `doc_triage.mjs`

**Two of `buses-data`'s document checks can never run in CI**, because their subjects do not survive `actions/checkout`: `list-archive-candidates.mjs` asks whether a dated document still has live work behind it, and `check-memory-paths.mjs` asks whether Claude's memory stores still point at files that have moved. `Development Docs/README.md` said since 2026-09-21 that the loop's weekly triage runs the first of them. **Nothing did** — grepped on 2026-09-22, the only things naming that script were four documents inside `buses-data`. The sentence described an intention and no move executed it, which is *a rule that is consulted is not a rule that is executed* with a whole recurring job as its subject.

**`doc_triage.mjs` is that move.** It runs both with `--json` — nothing here greps a report — writes `loop/doc-triage.json` as the stamp the loop prompt's `find -mmin 10080` test reads, and writes ONE draft, `loop/your-move/doc-triage.md`, when there is something to say. The draft carries no *What is needed from you* and no `**Blocks:**`, which is what makes it a draft rather than a hold: an archive candidate is a question and a stale memory path is a chore, and neither blocks anybody. It needs no worklist row of its own, because `loop_your_move.mjs` already counts every draft in that folder at rank 7.

**It is READ-ONLY on the memory store, deliberately.** `check-memory-paths.mjs --apply` is mechanical and harness-backed, and running it here would save a step. It is not run, because that store is in **no repository**: an unattended write to it leaves no diff anybody can review and nothing to revert to. The draft carries the exact command and a person runs it — the same line the estate already draws, that a tick may write where the history is readable.

**A half it cannot run is exit 2, never a clean report**, and that is what `prove-red-doc-triage.mjs` spends three of its eight cases on: a checker missing from disk, a checker that refuses, and a checker whose output stops being JSON. A triage that silently drops one half and prints *nothing to do* about a question it never asked would be the original fault wearing the fix's clothes. The controls hold the other half — that a run finding nothing **still stamps**, so the tick does not redo the work every tick, and that a draft already in `loop/your-move/` is **left exactly as it was**, because a draft is Peter's to move and a tick that tidied one away would delete the record of the finding with it.

**The limit, stated rather than hidden: a stopped loop runs no triage.** If `loop/STOP` is up for a fortnight these two questions go unasked again, exactly as before. That is visible — the board prints the loop's idle age — but it is not the same as a guarantee.

**Absent is the normal state, and that is what makes this hard to falsify.** `loop/` is gitignored in `buses-data` apart from its README, so the folder does not exist in a clone, a worktree, any other harness's fixture, or CI — the named shape *the subject that does not survive `actions/checkout`*, where a check is green for ever because its subject was never there. `prove-red-loop-your-move.mjs` therefore builds **real folders on disk** rather than injecting a fake reader, which is also why `readYourMoveDir` does its own I/O. A mutation sweep watched all eight mutations go red, and **two of them survived the first draft of the harness** and are worth knowing: an `existsSync` precondition in front of the `try/catch` could be deleted with everything still green, because `readdirSync` already throws for all three cases — *the check that could not go red*, so the redundant guard was removed rather than the assertion added; and `src.includes('for (const it of loopHolds.items) add(it);')` passed with the wire **commented out**, because a commented line still contains the string, so every source assertion now asks whether the line RUNS.

Prove it can go red, and go quiet, from `C:\u3a St Ives\.claude\skills\bus-work\assets`:

```bash
node prove-red-correspondence.mjs
node prove-red-commitments.mjs
node prove-red-refresh-review.mjs
node prove-red-loop-your-move.mjs
node prove-red-directory-links.mjs
node prove-red-unpushed-branches.mjs
node prove-red-pr-sweep.mjs
node prove-red-doc-triage.mjs
```

Every case there is a pair: make the state and see the row, clear the state and see it gone. Appearing is only half of it — a row still nagging about a letter that went out last week is a row that gets ignored, and then so is every row beside it. **The third is the same argument turned round and is the more dangerous half**: a refresh row suppressed by a review that never lifts has been silently deleted, and nobody would find out — so its cases prove a NEWER scan brings the row back with the old review still sitting in the file, and that an unreadable review file fails safe rather than quiet.

If a queue is missing from the list, fix the source that owns it — **a portal queue is fixed in `community-bus-maps/src/worklist/index.js`** (which fixes the admin console at the same time), a local-tree signal in `worklist.mjs`. Do not work around a gap by reading the admin console separately; that is the habit this skill exists to end.

