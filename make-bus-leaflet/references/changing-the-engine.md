# Changing the engine safely — the invariants, the gates, and the portal hand-off

Read this **before editing any file in `assets/`**. The stage references (`s1-services.md` …) tell you what each stage does; this one tells you what must remain true when you change the code that does it, and what else has to be updated in the same session.

`%SK%` = `C:\u3a St Ives\.claude\skills\make-bus-leaflet\assets` `%PSK%` = `C:\u3a St Ives\.claude\skills\make-place-bus-leaflet\assets`

> **2026-09-12 — DECIDE WHERE YOU ARE GOING TO WORK BEFORE YOU EDIT ANYTHING, because `claude-skills` is now PR-per-change.** Its `main` is protected — the `unit` and `status` checks required by name, branches must be up to date, nobody may bypass — so a direct push is refused by the server, and an engine change goes up as a PR. This matters here rather than only in that repository's own `CLAUDE.md` because **a session doing engine work usually has its working directory in `…\Buses` and so never loads that file**: you can edit `assets/`, commit, and only discover at the push that the commit has nowhere to go. Several sessions share the one checkout at `C:\u3a St Ives\.claude\skills` and it can only be on one branch at a time, so the isolation is a **worktree**, taken before the first edit. Two placeholders, both yours to choose, and `<branch-name>` is used twice — one self-contained command, from anywhere: `git -C "C:/u3a St Ives/.claude/skills" worktree add "C:/u3a St Ives/.claude/skills-wt/<branch-name>" -b "<branch-name>"`. Then `npm --prefix "C:/u3a St Ives/.claude/skills-wt/<branch-name>/make-bus-leaflet" ci` — **never junction `node_modules` in**, because `worktree remove --force` follows the junction out and empties what it points at. **And the worktree is not what the Skill tool runs:** the skill folders are junctioned into `C:\Users\Peter\.claude\skills` from the main checkout, so `status.js`, `rollout.js` and every gate below must be called by their absolute path *inside the worktree* or they will gate `main` and report green about a change you have not made. The full convention, including stamping with `--checkout`, is the *Git in this repo* section of `claude-skills`' own `CLAUDE.md`.

> **2026-08-25 — the board now asks whether any of this is actually LIVE.** `status.js` closes with a **Deployment** section that reads the `X-App-Version` response header off https://busmaps.uk and compares it with `origin/main` in the portal checkout. It exists because on 2026-08-25 the live site was one commit behind `main`, the commit it was missing was the one crediting NaPTAN in `legal.html`, and nothing in the estate compared the two — the portal's `/health` had carried `gitSha` until the S4 fix correctly gated it behind a token, and the only other surface was a `<meta>` injected by a script, so finding the gap took a headless browser (`technical-audit_2026-08-25` N2). **Three rules about when it may go red, and they matter more than the row itself:** a network failure is *never* red (that is what the uptime monitor is for, and a checker that cannot reach the network must say "I could not tell" rather than "your deployment is stale"); a missing header is *never* red (it means the live build predates the change, which is true and temporary); and BEHIND is amber for 12 hours and `BEHIND` after, because a merge is not a deploy but "merged and forgotten" is the state this exists to catch. **Since 2026-09-17 the word moves and the exit code does not** (buses-data OA-396, R3 of the process review): a dated `BEHIND` is a chore — printed here, carried by the bus-work worklist as its `deploy-pending` row — and never red, because three of the newest thirty reds on buses-data were exactly that. What still exits 1 is an *undateable* row: a live sha that neither the checkout nor the one fetch OA-392 added can find, which is a site running something `main` never held or an instrument that could not look. The row lives in `assets/deployment.js` (moved out of `status.js` under the line ratchet), `deployBad()` there is the whole rule, and `npm run test:prove-red-deploy-grace` proves it both ways — a dated backlog exits 0 and reads `BEHIND`, the same fixture against the pre-OA-396 rule exits 1, and the two undateable fixtures still exit 1. Skip it with `--no-live`, point it elsewhere with `--live <url>`, and watch the word move with `--deploy-grace-hours 0` against a deployment that is behind. Run from `C:\u3a St Ives\.claude\skills\make-bus-leaflet\assets`, no placeholders: `node status.js --no-quality --deploy-grace-hours 0`.
>
> **THERE IS NOW A FOURTH RULE, and it is the one that has already cost somebody an afternoon (buses-data OA-392, 2026-09-17).** `origin/main` is only as current as whoever last fetched it, so **a live build *newer* than this clone's copy of the remote is not a fact about the deployment at all** — and until this change it was reported as the worst kind of one. A laptop one commit stale read `BEHIND live 7c1297d != main f0d511f (undateable)` about a site that was running `origin/main` *exactly*; since every hold in `loop/your-move/` is a rank-3 worklist row, that verdict reached Peter's list carrying a pasteable deploy command for a site that needed nothing, and stood at the top of its band for four hours. The row now asks a question it never asked — **does this checkout even hold the commit the live site claims to be running** — and only when the answer is no does it fetch `origin` once, then say which of the two states it is in: `current` with `(this clone was the stale one)`, or `live ahead`, which passes and prescribes a `git fetch` rather than a deploy. The cheaper test that action first proposed, comparing `git log <deployed>..<ref>` against `git log <ref>..<deployed>`, **cannot see the case that raised it**: a stale clone does not hold the newer object at all, so both directions fail and the branch never fires — `git cat-file -t 7c1297d` answered `fatal: Not a valid object name` on the morning it was found, which is an ABSENCE rather than an inequality. Rule 1's null-means-red path is untouched for what it was written about, a live sha nobody can find *even after fetching*, and `--no-fetch` reproduces the old verdict exactly on the same fixture, which is how `prove-red-deploy-grace.js` proves the fetch is what moved the answer rather than the fixture.
>
> **2026-08-16 — the byte gate is no longer the only gate.** `status.js` now carries a **Quality** column beside each town's and place's reproduce columns, and a `REGRESSED` row makes it exit non-zero. It comes from `node "%SK%\quality_gate.js"`, which scores every `ci-reference` sheet with `quality_metrics.js` and compares it against `Development Docs\quality-ledger.json`. **A byte gate proves the generator is deterministic and says nothing about whether the sheet is any good** — six towns once shipped with their legends drawn on top of whole spokes while every number improved. Three figures are gated per sheet: `HARD` and `drop` as ceilings, and **`mapLabels` as a floor**, because every other measure counts what is *on* the page and so rewards a placer for dropping a label rather than solving it. After a change that genuinely improves a sheet, run `quality_gate.js --accept` **in the same commit as the rollout** so the ledger keeps describing what shipped. **Where the change makes a sheet WORSE, `--accept` refuses it without `--note "<sheet key>=<why>"`, and there is no bypass** (2026-09-04) — so budget the sentence before you type the command; the note is appended to that sheet's existing ledger entry, dated, by the tool rather than by hand. `status.js --no-quality` skips it. Full rationale — including why this is a ratchet and not "gate HARD at 0" — in [design-quality.md](design-quality.md).
>
> **2026-08-04.** The manual gate table in §2, the four-gate checklist in §2a, and the `cmp` loop over the §4 vendoring table are now automated: `node "%SK%\status.js"` gates every town, place, and portal fixture against the *current* template in one run (`--md` for a pasteable table, `--json` for scripting, non-zero exit on anything needing attention), and `node "%SK%\rollout.js" [--town "<Town>"|--all] [--apply]` does the §2a re-render sequence per town (dry-run by default; stops before publishing if a label is lost vs the previous build). Both are pure compute over data already on disk — no S1/S2 network fetch. The prose below still describes *what* the gate proves and *why* the re-render is needed; treat the tables as history, not as the thing to update by hand. Run `status.js` first — it already caught one real instance of the drift class in the next paragraph (`gen_external_places.js`, fixed the same day).
>
> **CI runs the same gates on every push** (`.github/workflows/gates.yml` in both `claude-skills` and `buses-data`, plus a daily schedule). It needed one extra piece: each town/place's `S4-generate` output is gitignored (rebuildable bulk, not tracked), so a fresh CI clone has nothing for `status.js` to diff a fresh regeneration against. `node "%SK%\sync_ci_reference.js"` mirrors just the `*.json`/`*.svg` files from each town/place's **latest** S4 run into a small tracked `ci-reference/` folder beside it (~19MB total, not covered by any gitignore rule); `gate_lib.js`'s `latestRunDir()` falls back to that folder only when the real `S4-generate` dir is missing on disk, so local runs are unaffected. `rollout.js --apply` calls the sync automatically, so it can't go stale — but if you ever commit an S4 run **outside** `rollout.js` (a manual `stage.js commit S4`), run `sync_ci_reference.js` yourself afterwards or CI will silently keep gating the *previous* published version. Two other CI traps already hit once each (2026-08-04): a real portal-vendoring fix can exist as a **local, unpushed commit** in `community-bus-maps` and CI will correctly fail until it's pushed — that's CI doing its job, not a bug; and don't add `--no-verify` to a test commit even when no hook is configured, since the rule against it is about intent, not about whether a hook happens to exist.

---

## 0. First ask: does this need a code change at all?

Most "the map should look different" requests are a **`routes.json` key**, not code. The generators are deliberately free of town literals. Check `s3-config.md` for an existing key before reaching for `gen_internal.js`. If the behaviour you want is genuinely new, **add a config key** — never a town literal (see the invariants below).

If it's a whole new *output* (a fourth kind of sheet), don't extend an existing generator: copy the pre-stage pattern in `schematic-engine.md` / `diagram-engine.md` — a config-gated script that rewrites geometry into a workspace and then runs the **unmodified** `gen_internal.js` there. Two outputs have been added that way with zero regression risk.

## 1. The invariants (breaking one of these is a defect, not a trade-off)

1. **No town literals in a generator.** Everything town-specific comes from `routes.json`. If you find yourself typing a place name, stop and add a config key.
2. **Absent config ⇒ byte-identical output.** Every new key must default to exactly the previous behaviour. This is what makes the gates meaningful and what lets the portal trust the engine.
3. **Overrides are Tier-1 data, never hand-edited SVG.** Adjustments live in `overrides.json` and are re-applied on every regenerate. A hand-edited SVG is lost at the next build.
4. **No network at render time.** The generators read only files in the run folder. The portal depends on this absolutely — it renders untrusted customer edits on a server.
5. **Deterministic output.** No timestamps, no `Math.random`, no locale- or filesystem-order dependence in the SVG. Byte-identical means byte-identical.
6. **Env contract is fixed:** `LEAFLET_DIR` (data folder; **preferred over cwd**), `SKILL_ASSETS` (where `icons.js` resolves from), `OVERRIDES_FILE` (customer edits; absent/empty ⇒ baseline), `EDITOR_KEYS`. The portal sets all of these. Don't add a new env var without updating `docs/DEVELOPING.md` in the portal repo.
7. **`gen_internal.js` is shared with the place skill** and with the schematic/diagram pre-stages. A change to it affects five outputs, not one. See `make-place-bus-leaflet/references/internal-reuse.md`.
8. **Reserve what you will actually draw, and claim it before anything competes for it.** Two rules, one cause, both paid for on 2026-08-30. A component that both RESERVES space and DRAWS into it must take both from one calculation: `font_metrics.js` holds real Arial advance widths and three sites were still guessing from the character count, so the interchange label reserved 26 mm while *Old Town (Aylesbury End / The Saracen's Head)* drew 71.5 — 45 mm of the page treated as blank, with labels placed into it. And anything positioned by hand — a `mapNotes` entry, an `internalDiagram` interchange lozenge — belongs in **the claim phase**, beside `reserveIcons()` and the anchor label, not drawn where it was told and reserved afterwards. Draw-then-reserve is invisible to every pass before it and un-helpable by every pass after it. A hand-placed thing that lands on claimed space should say so on stderr, naming what it hit, and should exclude page chrome from that warning — a note deliberately drawn inside the services panel column is a footnote, and a build warning that is right about the collision and wrong about whether anyone should care gets muted in its first week.

## 1a. The unit tests, and what they are for that the byte gate is not

Run them from `%SK%\..` — that is the `make-bus-leaflet` folder itself, not `assets`. Neither command takes an argument:

```bash
npm test
```

**497 tests in `test/`, about five seconds**, no network and no data tree. (This line has now been stale twice: it said *123 assertions across eleven suites* until 2026-08-28 and *324 tests across 22 suites* until 2026-08-30. Both were true when written. **A count written into prose is a measurement with no gate**, which is the same shape as the design-key register before `check-design-keys.js` existed — the difference being that the register's completeness is asserted by construction and this number is asserted by whoever last remembered.) **Run this before the byte gates**, which is also the order `gates.yml` runs them in: when the engine's own tests fail there is nothing to learn from being told that a wrong renderer still reproduces itself byte for byte.

**They answer a different question from the gate below.** The byte gate compares this engine's output against *this engine's own previous output*. That is a regression check and it is a good one, but it cannot tell you the previous output was right — and this project has already had a verification harness score 7/7 against committed data that WAS the bug's output. Every suite in `test/` is instead one fault we have already debugged, written down as a property: the placer's collisions and the `mustPlace` trade, the footer's measured wrap and its plate, the build-warning severities, the ratchet's arithmetic, the text quad, the engine hash, the gate helpers, the icon recolouring.

```bash
npm run test:prove-red
```

`tools/prove-red.js` copies `assets/` to a scratch directory, breaks it 191 ways one at a time and checks the suite goes red each time, then prints which test objected to which break. **A green suite that has never been seen to fail proves nothing**, and this one found a hole in itself on its first run. Add a mutation whenever you add a test; the runner reports an anchor that no longer matches the engine as stale rather than quietly passing.

**And read WHICH test objected, not just that something did** (2026-08-29, from the portal side of the same discipline). A rule that asks several things at once can be tested through one fixture that answers more than one of them, and then an assertion passes for a reason it does not name: breaking the clause it is about leaves it green, so the falsification round reports the check as sound when it is not. Three of four new assertions on `outputsNeedingRender()` were in exactly that state — a case written to test the payload clause was answered by the file clause, and two deliberate breaks produced no red at all. The remedy is a **fixture per clause**, chosen so only that clause can answer it, and the test is that breaking clause A reddens A's own assertion rather than somebody else's. That is what this runner's "which test objected to which break" line is for; it is not decoration. Named as *Satisfied by the other clause* in the failure-shapes list.

```bash
npm run gate:design-keys
npm run test:prove-red-design-keys
```

**Every `design.*` key the engine reads must have a row in the register**, and every row must name a key something still reads. The register is the table under `## `design`` in [`design-quality.md`](design-quality.md); the gate is `tools/check-design-keys.js` and its falsification is `tools/prove-red-design-keys.js`. Both run in `gates.yml`, the prove-red first. **Add the row in the same commit as the key** — that is the whole discipline, and the gate exists because it was not being kept: on 2026-08-28 the engine read **33** keys and the table held **19** rows, six of the missing fourteen discussed further down the same document and eight appearing nowhere in it. `design.laneOrientation` had been promoted to a DEFAULT the day before and the register had never named it. A table with a Default column asserts completeness by construction — there is no count to disagree with — which is why the checker prints its two counts even when it passes, and why the harness re-counts the population by its own independent walk rather than believing the verdict. The gate fires in both directions: a key you delete from the engine and leave in the document is the same document going stale from the other end.

```bash
npm run gate:attribution
npm run test:prove-red-attribution
```

**A sheet must CREDIT every source it draws** (OA-068, added 2026-08-28). Attribution is per-caller in this engine: each generator hardcodes its own `notes` array and `footer.js` prints whatever it is handed, so a shared footer component is not a shared attribution. On 2026-08-25 both live boarding sheets drew several hundred OpenStreetMap building footprints and printed OSM-only landmark names while crediting OpenStreetMap nowhere, because only `gen_internal.js`'s array named it. `tools/attribution-gate.js` asks it in three places — the five generator sources, the 52 sheets committed under `ci-reference/`, and coverage of sheet kinds — and it needs `--buses` because the second half reads what actually shipped. **The source half and the artefact half can disagree, and that is the useful case:** a correct generator with an uncredited shipped sheet is a map nobody re-rolled, and the reverse is a regression that has not reached the artwork yet. If you add a SIXTH generator, or teach an existing one to read a new OSM- or NaPTAN-derived input, this is the gate that will tell you the footer has not caught up.

```bash
npm run test:prove-red-status
```

**The status board's own exit code, falsified** (OA-151, added 2026-08-28; turned round by buses-data OA-396 on 2026-09-17). `prove-red-status.js` builds one-town scratch trees in the temp dir and pins what the exit code MEANS. From 2026-08-28 it stamped a town with a wrong engine hash and required the board to go red — the check that had been computed, printed and dropped for as long as the hash existed. Since 2026-09-17 a stale stamp is a **chore**, not a fault: the same case now requires exit 0 *and* the town named in `engineStale`, a mutation arm runs the same fixture against a copy of `status.js` with OA-151's term put back and requires exit 1, and the pair R3 asked for is there too — a stale S6 report is reported and green (with its own mutation arm, a copy that gates on it going red), and a town sheet that no longer reproduces goes red. It also proves an injected exception is exactly one town-and-hash pair wide, and that OA-057's completeness column and OA-170's schematic column are gated. It touches nothing under `Areas/` or `Places/`.

**The board's VENDORING verdict, falsified against a portal it builds itself** (OA-200, added 2026-08-31). `npm run test:prove-red-portal-drift` writes a synthetic portal repository into the OS temp dir — a manifest, two vendored copies of real skill files, and a `refs/remotes/origin/main` written by hand — and drives the whole board over it, so it needs neither `community-bus-maps` nor `buses-data` and touches neither. Seven cases: the ref in sync, the ref stale, a re-vendor sitting uncommitted in the working tree (`PENDING`), an unlisted `.js` on the ref, an unlisted `.js` on a branch only, a portal with no git at all, and — the row itself — **a stale CHECKOUT while `origin/main` is clean, which must stay green**. It then **falsifies itself**: the two cases whose subject is *which tree was read* are re-run against a copy of `status.js` with OA-200 taken back out, and each must go red naming the row the old reading produced. That last assertion is not decoration — the first cut of it scored both as "goes red" while they were actually reading `MISSING`, because a copy of `assets/` in the temp dir resolves every manifest `source` to a path that does not exist. A harness that accepts any red is not a harness.

**The red-team REUSE decision, falsified against the rule that bought three answers it did not need** (OA-166, added 2026-09-01). `npm run test:prove-red-redteam-fingerprint` puts `redteam_source.js`'s decision back to the pre-2026-09-01 pull-timestamp rule and requires its six guard tests to go red while two CONTROLs hold — an answer whose S1 never moved is still reused, and an answer past the 60-day window is still bought. It reverts by REPLACEMENT rather than by deletion, and syntax-checks the fixture before running it, because a fixture that does not parse reddens the controls too and a run where everything is red distinguishes nothing. Its sibling `test:prove-red-redteam-source` still falsifies the OA-141 ambiguity guard; the two cut different things out of the same file and each names what it cut.

**Both of those harnesses assert the CAUSE and not just the colour**, and so should anything you add beside them. Four of the attribution harness's five cases would have gone red together if the gate merely threw at startup, and a colour-only harness would have scored all four as catches — a confident green about a gate that had stopped working entirely. Read the red run's JSON, or match the specific sentence, and report a third verdict for *red, wrong cause*. The same trap on the green side is a case whose subject the run never found: a board that never enumerated a place satisfies "this place must not turn the board red" perfectly. Assert what the run must have SEEN.

```bash
npm run gate:line-endings
```

**Is any tracked file CRLF or MIXED in THIS working tree?** (OA-174, added 2026-09-01.) `.gitattributes` has said `* text=auto eol=lf` since OA-073, so the index is all LF and every fresh checkout writes LF — but `eol=lf` acts at CHECKOUT, and a file already sitting on disk keeps whatever terminator it was first written with. Four were still here on 2026-09-01 in an otherwise all-LF tree. It costs a session that edits one with a script: a multi-line anchor written `
` matches NOTHING, and a script that rejoins on `
` buries its real change in a whole-file diff. `--fix` rewrites them through `assets/line_endings.js`, on the bytes, and **changes no committed byte** — all four came out with the blob hash they went in with. **It is deliberately NOT in CI**, because `actions/checkout` writes that tree by the same rule and every file there is LF by construction: it would be green for ever, which is the shape *a check sited where its subject cannot exist*. **Its first cut was exactly that anyway** — the parser used `attr/\S*\s*	`, which cannot cross the space inside `attr/text=auto eol=lf`, so it matched no line and reported the tree clean. Making a file CRLF on purpose and watching it stay green is the only reason that was found.

**Adding a test:** load the module through `test/_engine.js` (`require('./_engine.js').load('labeller.js')`), never with a direct `require('../assets/…')` — that indirection is what lets the mutation runner point a suite at a broken copy through `ENGINE_DIR`.

**What they cannot reach, and why that matters here.** The five big generators are top-to-bottom scripts that read their inputs and exit at load, so nothing in them can be required. Extracting a helper out of one is not a tidy-up: the files inside `engine_version.js`'s hashed closure are re-stamped across nineteen maps when any one of their bytes changes, and the files vendored into the portal are compared file-by-file, so it means §4's re-vendor in the same change. **Both counts move, and neither is written here on purpose** — this paragraph said 21 and twenty-seven for a fortnight after they were 26 and 30. Read them off the run and off the manifest: the command in the pricing paragraph below, and `engine/vendored.json` in the portal.

**AND THE CLOSURE IS FOUND BY A SCANNER, WHICH HAS ITS OWN BLIND SPOTS — ASK IT, DO NOT ASSUME IT (2026-08-30, OA-167).** `dash_fit.js` was extracted out of three copies of the same primitive, required by two of the five entry points, and **was not hashed**. The `path.join` idiom in `DEP_PATTERNS` used `[^()]*?`, which cannot cross a `(`, so it was blind to `path.join(path.dirname(_LABELLER),'x.js')` — the form the external generators actually use. Nobody had noticed because the three other files named that way (`font_metrics.js`, `svg_primitives.js`, `strict_guards.js`) are ALSO reached from `gen_internal.js` by a luckier route, so the closure was right by accident and the accident ended at the first file named only that way. The pattern now allows one level of nesting — but the durable rule is the command below. **AND IT HAPPENED AGAIN ON 2026-09-02 (OA-224 Tier 3.4), to the same file.** Extracting the resolver into `engine_paths.js` rewrote that idiom as `_from('dash_fit.js')`, which the scanner also did not know, so `dash_fit.js` fell OUT of the closure in the same edit that put `engine_paths.js` IN — and **the file count did not move**, 21 before and 21 after. The test that had held the previous widening asserted it THROUGH `dash_fit.js` and so stopped asserting anything the moment `dash_fit.js` was reached another way; `engine_version.test.js` now names every idiom against a fixture written for the purpose, and the mutation suite carries a half-migration target for exactly this shape. **Run it after adding a file, and check the file you added is in the list.** A hash that has never been seen to move for a file is indistinguishable from one that does not cover it.

**PRICE THE CHANGE BY THE CLOSURE, NOT BY THE DIFF, AND DO IT BEFORE YOU WRITE IT.** Any byte change to one of the files in that closure moves the engine hash — a comment counts — and since 2026-08-28 a stale stamp FAILS the board rather than annotating it (OA-151). So a one-line `stderr` addition to `gen_internal.js` costs a re-stamp of nineteen maps plus a portal re-vendor with its own PR; the same line in `build_log.js`, `status.js`, `quality_metrics.js` or anything under `tools/` costs nothing at all. That is not a hypothetical: OA-118's remaining work is exactly one such line, it was prototyped on 2026-08-28, measured at `0a32b566d4` → `3110a326a4`, and reverted to ride with the next rebuild round instead. **THERE IS A THIRD CLASS, and the sentence above used to imply only two (2026-08-29, OA-127).** Hashed and vendored are DIFFERENT SETS, the second is not a superset of the first, and both counts are read off the run rather than from here. `gen_boarding.js` is vendored to `engine/expert/gen_boarding.js` and is **not** one of the five hashed entry points — so a one-line `stderr` addition to it re-stamps **no** map, needs no `track:engine` action, and `npm run track:engine` duly reported *11 already current, 0 BEHIND* — but `status.js` reports portal drift the instant the skill changes, and clearing it costs `npm run vendor:engine` and a portal PR. So the three prices are: **hashed and vendored** (re-stamp nineteen maps, re-vendor, PR), **vendored only** (re-vendor and a PR, nothing re-stamped), and **neither** (free). Ask BOTH questions before writing the line — the hash with the command below, and the vendoring by looking for the file in the portal's `engine/vendored.json`. The same change also shows why a diagnostic belongs on `stderr` and not on the sheet: all four boarding sheets stayed byte-identical, so `verify:place` reproduced them and every gate stayed green. Ask the question with a command rather than by reading the require graph, from `make-bus-leaflet`, with no placeholders:

**AND SINCE 2026-09-22 THE RE-STAMP IS A QUEUE RATHER THAN A GATE ON YOUR MERGE (buses-data OA-430, item 6 of R9).** The price above is unchanged in what it costs and changed in WHEN it falls due. A hashed change still owes every map in the estate a rebuild, and it no longer owes it *before the change can land*: each map's `ci-reference/routes.json` now carries `engineCommit` beside `engine` — written by `stampEngine()` at build time, and only when the hashed closure is clean, so the pair cannot lie by construction — and `status.js` asks a map that is BEHIND whether its committed sheets still reproduce under **the engine that drew them** rather than under today's. A DIFF is therefore a regression in committed artwork again, instead of the thing an ink-moving change makes true of the whole estate in a single step. What is owed becomes one `engine-rebuild-<map>` row per map on the bus-work worklist, which a loop tick can claim, build and commit one at a time. `engine_commit.js` holds both halves of that rule and is outside both hashes; the fourteen cases in `tools/prove-red-held-back.js` are where it is falsified, and four of them exist because four separate mutations of the gate had to be watched go red before any of it was believed. **The one thing that did NOT get cheaper is the portal re-vendor**, which is a different join with a different repository and still wants its own pull request in the same round.

**AND THE CLOSURE GREW AGAIN ON 2026-09-03, BY A FILE THAT IS NOT AN ENTRY POINT (OA-232 Tier 3.3).** `road_graph.js` is required by BOTH internal pre-stages and is in `engineFiles()` by CLOSURE rather than by being listed — `requireClosure` walks what the entry points require, so a shared module joins the hash the moment something in it loads the module, with no edit to `ENGINE_FILES`. That is the design working, and it is worth knowing because it means **a new file under `assets/` can move every map's stamp without appearing in any list you can grep for**. `test/road_graph.test.js` asserts the membership rather than trusting it, for the reason the paragraph above gives about `gen_boarding.js`: a hash that never moves for a file is indistinguishable from one that does not cover it. The same change moved the PLACE hash too, which is correct and is stated in `engine_version.js` — a place stamp includes the whole town closure, because a place's internal sheet is drawn by `gen_internal.js` itself. **And it had to be VENDORED**: the portal carries both pre-stages, a new load-time `require` of theirs that is not in `engine/vendored.json` takes the whole render down rather than staling one output, and `check-vendored.mjs`'s `UNRESOLVED` class is the check that says so. It went to `engine/` beside `projection.js`, `page.js`, `svg_primitives.js` and `wcag.js`, not into `engine/expert/`, because `renderMap.js` sets `SKILL_ASSETS` to `engine/`.

**THE CLOSURE GREW BY THREE ON 2026-09-02, AND THE THIRD CLASS LOST ITS LAST GENERATOR (OA-230, Tier 4.2 and 4.3 of the codebase review).** The town hash's entry points are SEVEN, not five: `diagram_internal.js` and `schematize_internal.js` joined `ENGINE_FILES`, because they draw the schematic and diagram sheets the byte gate certifies and a change to either had never moved a stamp — a town read `current` over a diagram drawn by code that no longer existed. `gen_boarding.js` went into the PLACE hash as a third half, under a `boarding/` prefix and with its own require closure minus anything the town closure already holds, because only a place has a boarding sheet; the "vendored only" price in the paragraph above now has no generator in it. And `internal_roads_config.js` joined through `gen_internal.js`, which reads its `internalRoads` block through that module — as the two pre-stages now do, and as they now call the engine's own `projection.js` rather than a copy commented "EXACT copy" that had drifted in four ways. The pre-stages were given exactly the inputs the copy computed (no overrides, no fixed orientation, no lenses, the flat 205 mm frame), so the 13 schematic and diagram sheets did not move; adopting the module's real frame was measured on the three sheets it would move and CLOSED by Peter on 2026-09-02 — the workspace refit makes the frame moot, and the buses-data plan `codebase-review_2026-09-01.md` holds the numbers. `LEGACY_FRAME`'s own comment still says "taken to Peter" and is reworded inside the next hash move, not before: a comment is a byte. So the counts in the paragraph above are the counts of 2026-08-29; read the current ones off the run, from `make-bus-leaflet`, with no placeholders:

```bash
node -e "const e=require('./assets/engine_version'); console.log(e.engineFiles().length, 'town files;', e.boardingEngineFiles(), 'boarding;', e.placeEngineFiles(), 'place')"
```

```bash
node -e "require('./assets/engine_version.js').engineFiles().forEach(f => console.log(f))"
```

**THE CLOSURE GREW BY ONE ON 2026-09-03 — `wcag.js` — AND THE PLACE SKILL JOINED THE RESOLVER (OA-232 Tier 3.1).** Three things went in as one hash move, all pure: `wcag.js` collected the seven copies of the 0.2126/0.7152/0.0722 sum that lived across five files, the two pre-stages took `page.js`'s `svgOpen()` and `engine_paths.js`'s new `spawnTarget()`, and `make-place-bus-leaflet` stopped carrying resolvers of its own. All 98 sheet verdicts were byte-identical to the pre-change baseline through every step. **The two things worth carrying out of it are both about what the finding said versus what was there.**

**The five bootstraps could NOT be collapsed onto SKILL_ASSETS, and the finding that said so was wrong for a reason a grep cannot see.** The review's move was "collapse the five bootstraps to one `require` of a sibling `engine_paths.js` with `SKILL_ASSETS` as its only fallback" — which would delete the laptop last resort from the bootstrap and leave it only in `engine_paths.js`. Measured before writing it: `rollout.js` copies ONLY the entry generator into the S4 workspace (`:214`, `:285`) and runs `gen_internal.js` with no `SKILL_ASSETS` in its env (`:230`, `:296`), so the last resort is the ONLY arm that answers in the ordinary production build, and dropping it would have broken every map build with a byte gate that could not have seen it. `rollout_places.js` does the same for `gen_external_places.js` (`:345`, `:472`). What was actually done instead is the opposite move: a FOURTH arm — a hop across to `make-bus-leaflet/assets/` — went INTO `engine_paths.js` and into the bootstrap, so the bootstrap is now SIX lines and there are SEVEN identical copies of it across BOTH skills, asserted character for character by `test/engine_paths.test.js`. The place skill's two private IIFEs had that arm and the town's did not, which is why they could not simply be deleted; it is the arm CI depends on, because on a runner the laptop path does not exist.

**THAT MEASUREMENT WAS TRUE AND IT WAS DESCRIBING A BUG (buses-data OA-342, 2026-09-14).** *"`rollout.js` runs `gen_internal.js` with no `SKILL_ASSETS` in its env, so the last resort is the ONLY arm that answers in the ordinary production build"* was measured correctly and then read as a property of the design. What it actually says is that an ordinary production build resolved `icons.js`, `poi_select.js` and every other shared module from **whatever engine is INSTALLED on this laptop** — the same engine, and therefore invisible, until the build is driven from a worktree, which the engine-PR ordering now requires for every engine change. Then the sheets whose recipe row named no engine are drawn by `main` while the rest of the same build uses the branch, and `routes.json` stamps all of them with the branch's hash: `c879f5a1` put eight such hybrid sheets on `buses-data`'s `main` on 2026-09-13. Both rollouts now draw through `build_s4.js`, whose `sheetEnv()` sets `SKILL_ASSETS` for **every** row rather than for five of the nine, so no build path in either skill reaches the last resort any more. **The conclusion of the paragraph above still stands and its reason has gone**: keep the laptop arm in the bootstraps because a person running a copied generator by hand in a run folder is the case it was written for, not because a rollout depends on it.

**Adding a dependency to `icons.js` turned 22 sheets FAIL, and the fault was in the gate.** `gate_lib.js` copied `icons.js` into the scratch workspace beside the generator, taking it out of `SK` — the LIVE assets — while `runGenerator` set `SKILL_ASSETS` to the `engineDir` the caller named. A sibling beats `SKILL_ASSETS`, so gating a held-back town against the engine that drew it handed that old generator TODAY's `icons.js`: the exact hybrid `runGenerator`'s own header, four lines below, records being caught once. It reproduced anyway because the two copies happen to agree, which is what a latent hybrid looks like from the outside — and it stayed invisible until a new require arrived in a folder with no sibling to satisfy it. The copy is gone (`copyJsons`), both requires of `icons.js` go through `_dep` to the engine the caller asked for, and no map directory in the estate has an `icons.js` of its own. **A copy list is a list you must remember to extend, and the fix for one is never a longer one.**

**THERE ARE TWO TEMPLATES SINCE 2026-08-30, AND A PLACE MAP IS STAMPED WITH THE OTHER ONE (OA-168).** `computeEngineVersion()` covers the town closure; `computePlaceEngineVersion()` covers that closure PLUS `make-place-bus-leaflet/assets/gen_internal_place.js` and `gen_external_places.js`, which nothing in the town engine requires. Until then all 12 place maps carried the town hash, and a round that changed `gen_external_places.js` by 266 lines and moved ink on nine shipped sheets left every one of them reading `current`. `rollout_places.js` stamps the place template, `rollout.js` the town one, and `stage.js stamps` picks by whether the run directory sits under a `Places` folder — the same rule `gate_lib.js`'s `findPlaces()` enumerates by. `status.js` prints both, one per section header. **A place stamp includes the whole town closure on purpose:** `gen_internal_place.js` is a pre-stage that runs the unmodified `gen_internal.js` in a workspace, so a town-engine change really does move ink on a place sheet.

`test/README.md` lists the known faults that sit inside that boundary.

## 1b. The re-render sweep — can a DELIVERED map still be published?

Run both from `make-bus-leaflet`. The `--buses` path is a real path on this machine, not a placeholder. **This section states its own folder deliberately**, and the reason is worth a sentence: it used to lean on a declaration four paragraphs up in §1a, and on 2026-09-03 a new paragraph inserted between them contained the words "from `SK`" — ordinary prose, which `check-doc-links.mjs` reads as a folder declaration, because the declaration in force is the most recent one anywhere above the fence. Two commands that had been correct for weeks went C2 red without being touched. A section that names its own folder cannot be broken by a paragraph.

```bash
node assets/render_sweep.js --buses "C:/u3a St Ives/Using AI/Buses" --expect 21
```

```bash
node assets/render_sweep.js --buses "C:/u3a St Ives/Using AI/Buses" --expect 21 --drop-framing
```

Run both from `C:\u3a St Ives\.claude\skills\make-bus-leaflet` — the engine's own folder, not the data repo. The path above is real on this machine and is not a placeholder; `--expect <n>` is the number of maps the tree should contain (21 today) and fails when the enumeration finds a different number, because a sweep that quietly covers 13 of 21 reports the same green as one that covers all 21. **A number typed here goes stale the day a map is built, and that is safe exactly here and nowhere else**: the sweep refuses in under a second and prints both numbers, so the whole cost is re-reading this line. It said `20` from 2026-08-27 until The Shelfords made the estate 21 on 2026-09-17 — and the copy of it inside [`tools/prove-red-sweep.js`](../tools/prove-red-sweep.js), which nobody re-reads because nobody pastes it, turned `buses-data`'s `main` red on a commit that had built a sheet and could be cleared only from this repository. That one now derives the count from `git ls-files -- "*ci-reference/routes.json"` in the data repository — a second instrument, reaching neither `findTowns()`, `findPlaces()` nor any manifest, so it cannot go stale and is not the enumeration it is checking. Add `--store "<portal data/maps dir>"` instead of `--buses` to sweep a portal store rather than the data tree — on the VPS that is `/opt/community-bus-maps/data/maps`, inside the container.

**This asks a question none of the gates above ask.** The byte gate runs the generators with `STRICT_GUARDS` **unset** — deliberately, so it cannot redden on a fixture that legitimately carries a warning. The portal sets `STRICT_GUARDS=1` on *every* render that goes public. So a map can reproduce its committed bytes perfectly and still be incapable of producing a NEW version, and every board we had would call it healthy. **Seven of the eighteen live maps were in exactly that state** and had been for about a year (OA-137); they were found by hand on the live host, not by any check.

**`--drop-framing` is the mode that matters, and it is not a stress test.** A place's expert framing — historically a river-hide, sometimes a frozen viewport — lives in a side file, and that file changes its name and its loading mechanism on the way to the portal: `overrides.json` read from `LEAFLET_DIR` here, `base-overrides.json` merged into a temp `OVERRIDES_FILE` there. `--drop-framing` renders as a delivery that has lost it, which is what the seven maps actually were. A sweep that renders a pack in place, letting the generator pick up whatever `overrides.json` happens to sit beside it, models only the local case and certifies the bug.

**Two things it reports carefully, both because a previous reading of the same output was wrong.** The refusal count comes from `strict_guards.js`'s own banner, not from counting guard-shaped stderr lines — `refuse()`, `warn()` and a bare `stderr.write()` are indistinguishable by text, and reading them as one turned five build notes into "five further refusals" on a map that had refused once. And a sheet whose generator has no `STRICT_GUARDS` contract reports `n/a` rather than `OK`: only `gen_internal.js` and `gen_boarding.js` participate, so **no external, schematic or diagram sheet can ever refuse** (OA-045).

```bash
npm run test:prove-red-sweep
```

Falsifies it in four runs: control green with framing and without, the old default restored and **invisible** while the side file survives (asserted as a deliberate no-op — that is why nobody saw this for a year), and the old default restored with `--drop-framing`, which must reproduce the named seven and exactly seven refusals.

## 2. The byte-identical gate (the town side)

**Harness:** run it from the skill folder (`C:\u3a St Ives\.claude\skills\make-bus-leaflet`, which is what `%SK%` stands for throughout this page): `node "%SK%\assets\gate.js" <genfile> <S4-datadir> internal|external <committed_svg>`. Four arguments, no defaults — `<genfile>` is the candidate generator, `<S4-datadir>` the run folder holding that map's `*.json`, the third names the sheet (`internal`, `external`, `internal-schematic`, `boarding`), and `<committed_svg>` is the reference to compare against. It copies the data `*.json` + `icons.js` + the candidate generator into a temp dir, runs it there, and compares the SVG with the committed one. **Exit 0 PASS, 1 DIFF** (the temp dir is kept and its path printed), **2** the generator produced no sheet, **3** an argument names something that is not there.

**It was `gate.sh` until 2026-09-03** (OA-224 Tier 5). That version hard-coded this laptop's assets path, and compared with a raw `diff -q` while every other gate in the engine compares through `gate_lib`, which ignores line endings on purpose — so on a checkout with `core.autocrlf=true` the hand gate and the board could return OPPOSITE verdicts on the same pair of files, and the one the documents told a newcomer to run was the wrong one. `gate.js` is a thin caller of `gate_lib.gate`, the same function `status.js` runs over the whole estate, and it is deliberately not a reimplementation: the value of a hand gate is that it agrees with the board, and the only way to guarantee that is to leave nothing here to disagree with.

Run it with **no `overrides.json` and no `EDITOR_KEYS`/`OVERRIDES_FILE`** — the gate proves the *baseline* is unchanged.

**The gate itself is now falsifiable, and two of its columns were decorative until 2026-08-27.** `npm run test:prove-red-gates` mutates a scratch copy of each of the five generators and requires the gate to object, one target per sheet type, with an unmutated control first — see [`../test/README.md`](../test/README.md). Before it existed, every sheet in the estate reported PASS and none had ever been watched go red. Writing it exposed two faults in `status.js`: the schematic and diagram rows guarded on `&& exists(<sheet>.svg)`, so a declared-but-missing derived sheet printed the benign `-` instead of `MISSING`; and `r.schematic`/`r.diagram` were in no part of the exit-code set at all, so a DIFF on either printed red on the board and exited 0. Both are fixed. **If you are extracting a module out of a generator — the OA-129 Phase 3 method — run `test:prove-red-gates` first**, so you know the gate you are about to lean on can still say no. **The PLACE half of the same fault was found on 2026-08-29 (OA-170), fourteen months of maps later.** `gatePlace()` gated internal, external and boarding and nothing else, because when it was written no place had a schematic — and High Wycombe Aldi has one, committed to `ci-reference`, which had drifted to the TOWN's title and lost its forced `Tannery Road Ind Est` POI on a published map with nothing to notice. It now gates `internal-schematic.svg` and `internal-diagram.svg` on `routes.json.internalSchematic`/`internalDiagram`, exactly as a town does, and both are in the exit-code set. **The interesting part is that it was RED on its first run and the sheet was not the wrong side:** `schematize_internal.js` writes a nested `schematic/` workspace and does not copy `overrides.json` into it, so `rollout_places.js` passes `OVERRIDES_FILE` explicitly when it BUILDS the sheet (§ the 2026-08-09 note above) and the gate did not — so the gate was regenerating by a different procedure from the one that made the reference. `gate_lib.js`'s `runGenerator` now takes `overridesFromWorkspace`, and `prove-red-gates.js` carries a control asserting the gate does NOT reproduce the sheet without it, because an option nothing has watched the gate fail without is indistinguishable from a no-op. **Before writing a gate over an artefact, read the tool that builds it and match its invocation** — every env var, cwd and copied input is part of the procedure the gate is claiming to reproduce.

**Gate the TEMPLATE, not the town's frozen copy.** Each town's `S3-config\<ts>\` and `S4-generate\<ver>\` folders hold a *copy* of the generator taken when that run was made. Gating a town against its own copy is circular — it passes by construction, because that copy is literally what drew the SVG. The gate that means something is **`%SK%\gen_internal.js` + the town's S4 data + no overrides == that town's committed SVG**, and that is what the table below asserts.

(Both runs are worth doing when you are diagnosing: *own copy* PASS + *template* DIFF says "the shipped map is intact, the town is just behind the template". *Own copy* DIFF means the data or the committed SVG has been tampered with — a different and more serious problem.)

**Current gate set — ask the board, do not read it here (OA-099).** A table of "the latest S4 of every town" is a fact that changes on every build, and this document is not rebuilt when a town is. Run this from `C:\u3a St Ives\.claude\skills\make-bus-leaflet` — both paths below are real on this machine and neither is a placeholder:

```bash
node assets/status.js --buses "C:/u3a St Ives/Using AI/Buses" --portal "C:/Claude/community-bus-maps" --md
```

`--md` prints exactly the table that used to be typed out here, for every town AND every place, with each one's latest S4, its external style, its reproduce verdict and its quality column — and it exits non-zero when anything needs attention, which a table cannot do.

**What was here until 2026-08-28, and why it is worth knowing it was removed rather than corrected.** It listed seven towns "all PASS as of 2026-08-03". Measured on 2026-08-28: **every one of the seven rows was wrong**, most by more than forty minor versions (St Ives `v6.14` → `v6.56`, High Wycombe `v2.2` → `v2.50`), and the eighth town, **Ramsey, was not in the table at all**. The note below has said "treat the tables as history, not as the thing to update by hand" since 2026-08-04, and that did not stop the table reading as current to anyone who scrolled to it — a table that is *labelled* stale still looks like an answer. Correcting the numbers would only have restarted the clock.

**There is ONE external template, and that is why the gate no longer chooses.** As of 2026-08-03 no town used `gen_external_busway.js` — St Ives switched to radial (v6.9) with an `externalHubLabel` combining its Bus Station and Park & Ride into one hub, because Peter didn't want the two-hub busway layout. It was then **kept in `assets/` unedited and untested-by-gate**, which is the sentence this paragraph used to end on, and the cost of it arrived on 2026-09-02: the Tier 3.4/3.5 extraction gave it two calls to an undeclared `_from`, and it threw `ReferenceError` at load for a whole day — through a re-vendor and a deploy — with `status.js` PASS, 98/98 sheet verdicts green, 249/249 mutations caught and CI green, because **every one of those gates can only ask its question of a generator some map actually runs**. `gate_lib.detectExternalStyle` probed it, got a failure, and read that as "this town must be radial, then". Peter's call was to drop it rather than repair it, and the machinery went with it: `detectExternalStyle` is now the constant `EXTERNAL_GENERATOR`, because a chooser that cannot tell a WRONG ANSWER from a BROKEN QUESTION will always prefer the reading that keeps it quiet. **The standing rule this leaves: an engine file that no map runs is certified by nothing here.** `test/generator_load.test.js` is the cheapest possible guard — does it load? — and it is the reason OA-224 Tier 4.1's `main()` wrappers were worth a hash move.

**The other four gates a `gen_internal.js` change must clear**, beyond the 14 above — it draws six outputs, not two (verified 2026-07-28 for P2):

| Gate | How |
|---|---|
| 4 place fixtures | `gate.js` against `…\Buses\Areas\*\Places\*\S4-generate\<latest>` — currently `Beaconsfield Simpson Centre v1.0`, `Beaconsfield Waitrose v1.1`, `St Neots Tesco Extra v1.2`, `St Neots Town Centre v1.3` |
| St Ives schematic | run `schematize_internal.js` in a copy of the S4 dir, diff `internal-schematic.svg` |
| St Ives diagram | run `diagram_internal.js` likewise, diff `internal-diagram.svg` |

**The portal's place fixture goes stale the same way a town does — and it is easier to miss.** `gate the TEMPLATE, not the town's frozen copy` (§ above) applies to `PLACE_FIXTURE_DIR` too. The portal reproduces a place leaflet using the generator **vendored into `engine/place/`**, and compares it against a **frozen fixture** in `…\Buses\Places\_portal-fixture\`. If the vendored engine and the fixture were frozen at the same moment, `npm run verify:place` passes **by construction** and keeps passing however far the skill moves on. That is exactly what happened: on 2026-08-02 the fixture was swapped from `Beaconsfield Simpson Centre` (v1.0, 2026-07-21) to `High Wycombe Aldi` (v1.1, 2026-07-30) and the internal SVG missed by 189 bytes — `engine/place/gen_internal.js` turned out to be **445 lines behind**, predating the whole complexity-triage ladder. The gate had been green for weeks.

So: **whenever you re-vendor, also refresh the fixture to the newest available build**, and treat a fixture swap as a test of the vendor chain rather than housekeeping. A fixture older than the last engine change proves nothing.

The place fixtures **legitimately differ on exactly two lines** — the title (`Buses within X` vs `Buses serving X`) and the `· Map v…` stamp — because `build_internal_place.js` post-edits both after running the generator. So the place gate is "**4 differing lines, 0 outside `y="16"` and `y="208"`**", not zero. Filter before judging, or you will chase a diff that has always been there:

```
diff new.svg shipped.svg | grep '^[<>]' | grep -v 'y="16"\|y="208"'   # must be empty
```

(The older docs say "the 4-way gate: St Ives + March". That was the gate set when there were two towns. It has grown with every town — gate them all; the whole point is that a template change is invisible to towns that didn't ask for it.)

**No town carries functional generator deltas any more** (verified 2026-07-28). Every town-specific thing lives in `routes.json`, per invariant 1, so "re-deriving a town's copy" is now just *copy the current template in*. The delta list in `overrides.md` §4 describes an era before those literals migrated into config; treat it as history, not as a checklist.

### 2a. A template improvement leaves already-built towns STALE — and that is the normal state

The gate failing after a deliberate improvement does **not** mean the town is broken. It means the town has not been re-rendered since. Before item 3 (2026-08-04) a build pulled its generator from its **S3 run**, not from `%SK%`, so a town frozen at an old S3 kept drawing with the old engine indefinitely — the shipped JPG and the code drifted apart silently and nothing complained. **Item 3 removed the freeze**: S3 no longer carries a generator copy at all, S4 always copies the two generators fresh from the live `%SK%` template and stamps the hash it used into `routes.json`'s `"engine"` field (`engine_version.js`, provenance only — see [s3-config.md](s3-config.md)). So a town's *next ordinary build* is automatically current; staleness now only describes a town that hasn't been **rebuilt at all** since an engine change, not one running old code by construction.

So a template change still has **two** halves, and the second is still easy to forget:

1. Prove the change (`node "%SK%\status.js"` gates every town; the affected ones will legitimately DIFF).
2. **Re-render every town the change affects** so code and shipped maps agree again — `node "%SK%\rollout.js" --all --apply` (or `--town "<Town>"` for one), or by hand below.

**How to re-render a town for an engine change only** (no data change) — the whole point of item 3 is that this needs only a **new S4**, not a new S3 (routes.json/overrides.json are unchanged, and S3 no longer holds anything to re-copy):

```
S4=$(stage.js new S4 --bump minor); cd "$S4"
stage.js pull S2 .; stage.js pull S3 .        # data unchanged; also syncs the version stamp
node %SK%/engine_version.js --stamp routes.json
node %SK%/build_s4.js                         # copies the CURRENT TEMPLATE in, draws every sheet
stage.js commit S4 --outputs <the list build_s4 printed> --note "adopt current engine template: <what changes>"
# then S5 render, then refresh_latest.js
```

(`rollout.js` does exactly this sequence automatically — prefer it over the manual steps above.)

**Step 4 used to be four lines and is now one** (buses-data OA-310, 2026-09-12). It was `cp %SK%/gen_internal.js .`, `cp %SK%/gen_external_radial.js gen_external.js`, `node gen_internal.js; node gen_external.js`, plus whichever pre-stage the config asked for — and it produced no `build-warnings.txt`, because only the two rollouts ever called `build_log.js`. `build_s4.js` is the one statement of how each sheet is drawn, for towns and places alike, and both rollouts now call it rather than keeping their own copy of the capture. It prints the `--outputs` string the commit wants, so that is read off the run rather than typed.

**A new S3 run does NOT force a `--bump major`, and after item 3 an engine-only rollout has no new S3 run to force one anyway.** §"Stage 4" in `s4-s5-build-and-render.md` used to read "major if you produced a new S1/S2/S3 run" — the rule that has actually been followed since v6.2 is **major = new *data* (S1/S2); minor = config- or engine-only re-gen**. Config/engine-only re-renders are **minor**.

**Pre-item-3 towns still work unchanged.** A town whose S3 predates 2026-08-04 still has a generator copy sitting in it; that copy is simply never read any more (S4 always copies fresh from `%SK%` regardless of what's in S3), so nothing breaks — it's just now dead weight in that folder. Its `routes.json` will read `engine: (none)` in `status.js` until its *next* S4 build stamps it, which is expected and not itself a problem to fix.

### 2b. Rolling out a CONFIG change across towns (added 2026-08-15)

§2a is for an engine-only change, which needs no new S3 because `routes.json` is unchanged. A **config** change does need one, and doing it by hand across eight towns is where the mistakes live. Two tools, in this order:

```
node "%SK%\preview_design.js" --all --patch '{"design":{"iconInk":"charcoal"}}'   # judge it first
node "%SK%\adopt_config.js"   --all --set   '{"design":{"iconInk":"charcoal"}}' --apply
node "%SK%\rollout.js" --all --apply --force --bump minor --note "..."
```

- **`preview_design.js`** rebuilds every sheet from the latest committed S4 data with the patch applied, measures the shipped sheet and the new one with `quality_metrics.js`, and reports the defect delta plus the gained/lost label strings — writing nothing under `Areas/`. Judge the change here, on all eight towns, before committing anything. It also has `--render`, and `--keep` to leave the build workspace on disk so you can re-run a generator by hand with `DBG_LABELS=1`.
- **`crop_compare.js`** is the other half of judging it: `node "%SK%\crop_compare.js" <shipped>.svg design-preview/<Town>/internal.svg out --at 134,129 --size 40 --label "before|after"` cuts the same page region out of both at 300 dpi and stacks them captioned, ready to send. Use `--poi 3` to have it pick the three densest symbol clusters instead. **A defect delta is not a verdict on this project** — twice the artwork has overruled the numbers, and once (plan §2.5) it confirmed them and settled the question.
- **`adopt_config.js`** writes the patched `routes.json` into a new S3 run per town and commits it. Dry-run by default. `--set` deep-merges and `--unset` takes a dotted path; neither can reach inside an **array**, which is what `features[]`, `mapNotes[]` and `internalCorridors` are. For those: `--rail <style>`, `--feature-pos <key>=<x>,<y>`, and — since 2026-08-15 — the general **`--set-path '<dotted.path>=<json>'`**, where a numeric segment indexes an array (`--set-path 'internalDiagram.mapNotes.0.y=189'`). It refuses to *create* a missing path, so a typo is an error rather than a new key nothing reads. **Reach for `--set-path` rather than adding a fourth one-off flag**, and note `preview_design.js` takes the same expression, so what you preview is what you commit.

> **A GATED engine change leaves every town reading STALE, and the fix is a stamp, not a rollout.** Adding a key that defaults to today's behaviour still changes the engine files, so `engine_version.js` computes a new template hash and `status.js` marks all eight towns behind it — which the worklist then reads as eight jobs. They are not jobs: the gate has just proved each town's sheets regenerate **byte-identically** under the new engine, and that is exactly the claim the stamp makes. Correct it in place rather than re-rendering and bumping eight versions for no pixel change:
> ```
> node "%SK%\engine_version.js" --stamp "Areas/<Town>/S4-generate/<latest>/routes.json"
> ```
> Only after the gate says PASS. If any town DIFFs, the change was not inert and it wants a real rollout.

> **AND IT NEEDS `--force` AFTER AN ENGINE CHANGE THAT DID NOT MOVE THE ARTWORK — the tool now SAYS SO, since 2026-08-30 (OA-179).** The same fast path used to report `UP-TO-DATE` on a town whose four sheets reproduce byte-for-byte but whose `routes.json` still names the previous engine build. On 2026-08-30 an engine round moved the template hash while changing the drawn output of exactly one sheet in the estate: `rollout.js --all` reported seven towns UP-TO-DATE and one to rebuild, `status.js` reported eight ENGINE STALE in the same minute, and both were right about what they measured — but since OA-151 a stale stamp FAILS the board, so the only tool that could clear it had just said there was nothing to clear. There is now a third verdict, **`STAMP-STALE`**, in both `rollout.js` and `rollout_places.js`: the sheets are current, the stamp is not, and the message names both hashes and the exact `--force` command. **It reports and does not write.** A `--stamp-only` mode that rewrote the field on a map whose sheets gate PASS would quietly convert `engine` from *which engine build drew this* into *which engine reproduces this* — the byte gate already answers the second, and this field is the only record of the first, which is what `track:engine` and the whole OA-130 tracking decision rest on. It also does not move the exit code: `status.js` is the board and already gates this, and a tool you run to find out what to do should not fail while telling you. Falsified by `npm run test:prove-red-rollout-stamp` — six cases over both tools, and four of the six are controls.

> **THE BOARD NOW ASKS A SECOND QUESTION ABOUT A BOARDING SHEET: IS ITS STORED DATA WHAT THE SCRIPT WOULD PRODUCE TODAY? (OA-188, 2026-08-31).** A boarding sheet is drawn from two DERIVED files — `boarding_index.json` (`boarding_index.py`) and `stands.json` (`naptan_stands.py`) — which live only in the S4 run and are copied forward by `seedPrevS4`, so no rollout has ever re-derived them. **The byte gate cannot see that, and is not wrong not to**: it asks whether the current GENERATOR redraws the sheet from its stored index, and the answer is yes however old the index is. Measured on 2026-08-30: three of the four boarding sheets carried an index written by `boarding_index.py` v1.2 while the script on disk was v1.3, and **27 destinations across them had trip counts the current script computes differently** — with `status.js` reporting all four PASS and `rollout_places.js` reporting all four UP-TO-DATE, both correctly. Same shape as OA-130, where a delivered map ran the generator it was imported with for ever, but for the DATA half of the pipeline rather than the code half. `dataScriptDrift()` compares the `generatedBy` stamp each script writes into its own output against that script's `SCRIPT_VERSION`, which is a claim already written down at both ends. The Boarding cell reads **`INDEX-STALE`** and the summary block names the file, both versions and the command; the cell replaces a `PASS` only, so a genuine `DIFF` — a louder problem — still shows, and the drift is printed either way. **It gates.** The whole character of this state is that every existing check is correctly green, so a column the exit code ignored would be the decoration OA-151 spent a row taking out of the Engine column. An absent file, an unstamped file or a missing script is **not** a finding — absent means the place has no boarding plan, and a gate that reddened every place without one would be muted inside a week. It landed GREEN because the arrears were cleared first, and `npm run test:prove-red-status` now carries a control saying so as well as the mutation. **The portal's `refresh-place-fixture.mjs` has the identical blind spot** and is not covered by this — it re-runs the generator against the fixture's own stored index, and once reported a boarding fixture `unchanged` while it was 924 bytes behind the sheet being shipped.

> **AND A THIRD: WHICH BODS FEED DID THAT INDEX COUNT? (OA-210, 2026-09-01.)** The paragraph above versions the derivation SCRIPT, which is exactly half the question. `region` in a boarding index names a FILE — `buckinghamshire.sqlite` — and that file is rebuilt in place by every refresh, so the name is stable across the very change that matters. **Measured, not imagined**: on the morning of 2026-08-31 three of the four boarding indexes were built at 05:07 and the Buckinghamshire feed was rebuilt in place at 10:01, and High Wycombe High Street was shipping eleven destinations whose trip counts today's feed does not reproduce — Beaconsfield 830 against 811, London 644 against 590, Loudwater 1141 against 1095, and eight more. Every check in the estate was green and every one of them was right, `dataScriptDrift()` included: it read v1.3 on both sides, because the SCRIPT had not moved and only the data had. `boarding_index.py` v1.4 records the `feed_version` it counted, so `dataFeedDrift()` only asks whether the two written-down claims agree; the Boarding cell reads **`FEED-STALE`**, replacing a `PASS` only and ranking below `INDEX-STALE`, and the summary names both versions and the re-count command. **It is REPORTED and does NOT gate, and that is a decision.** Clearing it costs a `--refresh-index` plus a rebuild, so wiring the monthly refresh (OA-091 in the buses-data backlog — a relative link would climb out of this repository and 404 for everyone but this laptop) would fire it on every boarding place every month, and a cell that is red every month by design is the column everyone learns to ignore. Settle the cadence there, then decide whether this gates. An index with no `feed` key, an unreadable `feed_info` or a `region` naming no feed file is **not** a finding — the same cannot-tell rule the version stamp adopted. **This is also the answer OA-210 asked for about the portal**: the buses tree asks the question for the portal fixtures too, in the same pass, because it is the one tree holding the fixture, the derivation scripts and `_gtfs/feed_info_<region>.json`. Falsified in `prove-red-status.js` with four cases — a matching feed, a rebuilt feed on a place, a rebuilt feed on a portal fixture, and no `_gtfs/` at all — and the two red ones were watched going VACUOUS with the check neutered, which is the only thing that proves the two green ones mean anything.

> **THE VENDORING VERDICT IS ABOUT A NAMED REF, NOT ABOUT YOUR CHECKOUT (OA-200, 2026-08-31).** `portalDrift()` used to compare each skill source against the portal's **working tree**, so its answer was a claim about whichever branch `C:\Claude\community-bus-maps` happened to be sitting on. On 2026-08-31 it reported `DRIFTED  gen_internal.js -> engine\place\gen_internal.js` while `main` was perfectly in sync — the checkout was on a feature branch cut before the re-vendor merged. The named file was the place generator, the skill change was OA-175's `exitCaption()`, and the consequence a reader would have seen is the live place engine printing `to X` on both tails of a one-way loop, which is the CORR-001 correspondent's first point, in writing, about a published sheet. A re-vendor PR was one command from being opened; the only thing that stopped it was `vendor-engine.mjs --dry-run` answering `27 already current, 0 would move`. Two tools reading two different things, and nothing but their disagreement to say which question each was answering. It now reads `origin/main` where the checkout has one and `HEAD` otherwise — the rule the Deployment row already followed — so the verdict means *is the deployable engine stale*, which is the question it is asked. **The board prints the ref, its SHA and the checkout's branch above the table on every run**, in sync or not, because the whole cost of this was that a verdict looked identical whichever tree produced it. A re-vendor that is on your disk and not on the ref reads **`PENDING`** and still gates: until it lands, the deployable engine is stale. A portal with no git falls back to the disk and says so in the same line. `origin/main` is only as fresh as your last fetch, and that is deliberately not fixed here — a board must not have network side effects — but it is not silent either, because a stale `origin/main` makes the Deployment row disagree with the live site. **That last clause was the whole safety net and it was not one (OA-392, 2026-09-17): the disagreement is real, but the Deployment row reported it as `BEHIND`, which is a verdict about the DEPLOYMENT, so the signal arrived wearing the face of the wrong fault and sent a person to `npm run deploy`.** The rule is now narrowed rather than abandoned, and the narrowing is the part to read: **the board still never fetches to make its own verdicts fresher** — this row, the vendoring row and every other one are computed from the ref as it stands — and it fetches in exactly one circumstance, when the live site names a commit this checkout does not hold *at all* and the row would otherwise have to guess which of two opposite remedies to print. One fetch, bounded, remote-tracking refs only, no branch and no working tree. The cost is named rather than hidden: the fetch happens after the vendoring row has already been computed, so in the rare run that fetches, the drift verdict is a sentence older than the deployment verdict — and catches up on the next run. **`prove-red-status.js` gained a preflight in the same change:** it runs the board over an EMPTY scratch tree first and exits **2** if that is red, because everything ambient reddens every case whose `expect` is 0, and `CONTROL RED` and `caught` are the same exit code seen from two expectations. Three of its nine controls had been red for exactly this reason, in a column nobody read.

> **AND A FOURTH VERDICT, `UNRENDERED`, FOR THE STATE THE TOOL'S OWN STOP LEAVES BEHIND (OA-198, 2026-08-31).** Both rollout tools commit S4 and then render S5 as a separate step, and both have a stop in between: a BLOCKING build warning returns `REVIEW-NEEDED` after the S4 commit and before `stage new S5`. Everything about the state that leaves reads healthy — the manifest advertises the new S4, the current generator redraws its stored sheets byte-for-byte because that generator drew them, every byte gate PASSES — and there is no JPG anywhere for the version being named. **Then the fast path buries it**: on the next ordinary run all sheets gate PASS, so the tool reports `UP-TO-DATE` and skips the map, and the S4 stays unrendered for ever. `--force` was the only thing that got past it, because the fast path is the only guard `!FORCE` disables — which made the recovery a flag nobody had a reason to reach for, on a map nothing had reported. The check asks the manifest whether any S5 run carries the head S4's **version**, which is the sentence "that S4 has been rendered" with no ordering assumption and no clock in it, and it sits in FRONT of both fast-path returns and of `STAMP-STALE`, all three of which are reachable and wrong in that state. **Unlike `STAMP-STALE` it moves the exit code**, because the whole character of this state is that nothing fails. It is in **both** tools: the row was written about `rollout_places.js`, and `rollout.js` has the same stop after the same commit and therefore the same hole. Falsified by `npm run test:prove-red-unrendered` — six cases over both tools, four of them controls, including a head S4 with no recorded `version` (unanswerable, must stay quiet) and an OLDER S4 whose render is missing from a history `prune_runs.py` thins on purpose.
>
> **AND A FIFTH, `STALE-INPUTS`, FOR THE ROLLOUT THAT IS NOT AN ENGINE ROLLOUT AT ALL (OA-225, 2026-09-03).** Both tools take their CONFIG from the latest S3 and their GEOMETRY from the previous S4's frozen copies — deliberate, documented at the head of each file, and exactly right for the job they were written for. What was never written down is that the data on disk is the previous **BUILD's**, not the latest S2's. Run against a town whose S2 has moved, the tool produces the new configuration over the old geometry: on High Wycombe on 2026-09-01 that was route 20 in the Services panel, in the palette and in the operator table, with **no line on the map, on all four sheets**, and `--bump major` did not change it because nothing in the invocation asks for the new S2. **It was caught, and not by this tool** — `services_panel.js` writes *"badged in the Services panel but draws no line"* to stderr and `rollout.js` faithfully captured and printed it. Without that neighbour the dry run's own headline was a GAIN-only label diff, which neither tool blocks on, and `--apply` would have committed and rendered it. **The UP-TO-DATE branch is what hid it**: a town whose sheets still gate PASS is skipped entirely, so on a data change the tool's first answer was *nothing to do*, and the only way past that was `--force` — whose documented meaning here is "I have reviewed the lost labels", not "use stale geometry". So this verdict sits in FRONT of every fast path, ahead of `UNRENDERED` and `STAMP-STALE` too, and **it moves the exit code**. The question is asked of the manifest, which already records both halves, and it prefers the exact signal: where an S4 carries `basedOn` it is an id comparison, and where it does not it asks whether the latest S2/S3 **completed after the S4 started**. The comparison is strictly greater-than — High Wycombe's v4.0 started at 23:08 and its S2 committed at 23:08, so `>=` would have refused the estate's most recently rebuilt town. **Both tools now pass `--based-on` to `stage.js commit S4`**, which neither ever did: that is why 9 of the 11 manifests on 2026-09-03 had no provenance on their latest S4 and the guard has a fallback at all. **That sentence said `stage.js new S4` from 2026-09-03 to 2026-09-17 and so did the code, and `new` has never read the flag** — only `commit` does, because only `commit` writes a run record. So the fix OA-225 describes here wrote no field for a fortnight and every S4 either tool built in that time went into the timestamp fallback, with nothing saying so: `staleInputs()` reports `how: 'timestamp'` only when it actually fires, so a clean answer never reveals which of the two questions was asked. Found by measuring one manifest rather than by reading either tool (OA-352 — Godmanchester Co-op Ermine Street v1.19, built by hand, carries both ids; v1.20, built by `rollout_places.js` three days later with the flag on its command line, carries none). `stage.js new` now REFUSES `--based-on` outright, so the two halves cannot silently come apart again, and `test/stage_based_on.test.js` asks the only question that could have caught this — does the field land on the record — rather than whether the argument was passed. A tool that erases the provenance of its own output is the reason the next tool has to infer it. Measured over all 11 town and place manifests before it was wired in — every one clean, so it refuses nothing that works — and wound back to the state of 2026-09-01 it fires. `staleInputs()` lives in `gate_lib.js`, shared by both tools for the same reason `unrenderedS4()` is: the row above was written about one of them and the other had the identical hole. Falsified by three mutations in `npm run test:prove-red` against `test/stale_inputs.test.js`, whose ten cases are **five controls and five refusals** — including the estate itself, read from the real manifests rather than a fixture.

> **`rollout.js` needs `--force` after a config change, and it is not obvious why.** Its first act is to gate the town's **previous S4** against the current template, and report `UP-TO-DATE` if that passes. It does pass — because that S4 folder still contains the **old** `routes.json`. The new S3 is invisible to the check, so without `--force` a config rollout silently does nothing at all and reports success for every town.

**A config change is a `--bump minor`**, same as an engine one; major is for new S1/S2 data (see §2a).

> **THE BATCHED FORM IS PROVEN, `--apply` INCLUDED, AS OF 2026-09-04.** It was measured as a dry run on 2026-08-30 — all eight towns built in scratch, exit 0, no lost labels, no blocking warnings — and this paragraph then said for five days that *the `--apply` half is still unproven*, because proving it means re-versioning the live estate. The OA-229 round was that occasion: **seven towns in one `rollout.js` invocation and twelve places in one `rollout_places.js` invocation, both `--apply --force --bump minor`, 19 maps versioned, rendered, committed, `_latest`-refreshed and `sync_ci_reference`d in two processes.** Every one reported DONE, and the board afterwards showed all 19 on the new hashes with every byte gate PASS and the quality ledger unmoved. So the batched selection, the batched build and the batched apply are all sound. **Read the board afterwards rather than the tool's summary** — that part of the advice stands and is the reason this can now be asserted.
>
> Two things worth knowing before the next one. **A `--town` list is how you leave one map out**, and it is better than rolling everything and undoing: the OA-229 round named seven towns because High Wycombe's latest S3 held an unbuilt landmark answer that a rebuild would have shipped. And **the label diff cannot see a symbol**. A POI that prints no name — a pharmacy, a GP surgery — leaves and enters a sheet with no line in the gained/lost report at all, so a change that moves symbols rather than text is invisible to the tool's own safety check and has to be measured on the SVG. **AND IT CANNOT SEE A PATH, WHICH IS THE GENERAL CASE AND THE DANGEROUS ONE.** On 2026-09-07 (buses-data OA-059) a one-character fix to `stitchSegs` was written up as reaching "only Beaconsfield's railway", read straight off this diff, which named Beaconsfield alone; **eleven sheets across six maps had moved ink** — three diagram sheets, three schematics and five geographic internals including a place's — every one of them paths and no labels. The byte gates were PASS throughout and were right to be: a byte gate regenerates each sheet with the SAME engine and compares, so it says the engine is REPRODUCIBLE, never that it draws what it drew yesterday. Neither instrument answers *what moved*, and nothing runs the one that does. **After `--apply`, before writing down what the change affected, diff the tracked sheets across the commit** — from the buses-data root, `git diff <before> <after> -- "*/ci-reference/*.svg"`, both SHAs real and neither a placeholder — and count the changed lines per sheet with the version-stamp line excluded.

> **ROLL OUT INTO A QUIET TREE. Two agents editing this skill at once is fine; two agents rolling out is not.** Each town's build copies the generators fresh from `%SK%` at S4 time, so if anyone edits a generator *while* `rollout.js --all` is running, the towns pick up different snapshots and freeze **different engine hashes** into their manifests. It happened on 2026-08-15: eight towns, five hashes, `status.js` reporting STALE against hashes that no single commit contains. Nothing broke — the other session's feature was gated off, so every sheet's output was identical and all 27 gates passed — which is exactly why it is easy to miss. If you find a board like that, one `rollout.js --all --apply --force --bump minor` re-stamps it. If another session is live in this tree, wait, or agree who rolls out.

**Before you re-render, diff the label sets, not just the byte count.** A placement change can silently drop or surface a label, which is a content change hiding inside a "cosmetic" diff:

```
grep -o '>[^<>]*</text>' old.svg | sed 's/^>//;s|</text>||' | sort > /tmp/a
grep -o '>[^<>]*</text>' new.svg | sed 's/^>//;s|</text>||' | sort > /tmp/b
comm -23 /tmp/a /tmp/b   # LOST     <- read every line before shipping
comm -13 /tmp/a /tmp/b   # GAINED
```

For the schematic and diagram outputs, gate St Ives v6.6 (`internal-schematic.svg`, `internal-diagram.svg`) the same way — run the pre-stage, then diff.

## 3. The place side

`make-place-bus-leaflet` reuses `gen_internal.js`, `render.js`, `stage.js`, `gtfs_query.py` and `icons.js` **unchanged**. If your change touches any of them, also rebuild a place fixture and diff. Worked places on disk: `…\Buses\Areas\St Neots\Places\St Neots Tesco Extra`, `St Neots Town Centre`, `Beaconsfield Waitrose`, `Beaconsfield Simpson Centre`. The portal's fixture is `…\Buses\Places\_portal-fixture`.

### 3a. The boarding sheet has its own gate set, and it is not in the portal

`gen_boarding.js`, `naptan_stands.py`, `boarding_index.py`, `boarding_verify.py` and `pull_locator.js` are engine-level files in `make-bus-leaflet/assets` used by the place skill as Phase 3. **`gen_boarding.js` IS vendored into the portal as of 2026-08-23** — `engine\expert\gen_boarding.js`, a row in §4's table and in `status.js` — so §4 applies to it in full. The other four are **not**, and do not need to be: the portal never runs the Python. It consumes what they wrote (`stands.json`, `boarding_index.json`, `locator_geo.json`) as committed payload inputs, exactly as it consumes `routes.json`, and `import-map.mjs` copies them with the rest of the `*.json` with no change. That settles the open question about whether the portal would pull its own locator: it does not, and it should not.

Everything else here applies, and invariant 2 above especially: a new `boardingPlan` key must default to exactly the previous behaviour.

**Two things about `gen_boarding.js` were only true because it had never run anywhere but here, and both were fixed on 2026-08-23 when the portal first ran it.** It required `footer.js`, `icons.js` and `font_metrics.js` from `__dirname` alone — fine beside them in `assets\`, and a throw at require time from `engine\expert\`, before a single input is read; it now self-resolves sibling → `SKILL_ASSETS` → skill path, the way `gen_internal.js` has for months. And `--dir` defaulted to `.` with no `LEAFLET_DIR` fallback, so it worked in the portal only because `renderMap.js` happens to spawn with `cwd` set to the data dir — true today, and not invariant 6's contract. **The general lesson: a dependency that resolves is not a dependency that is portable, and the only way to find out is to run the file somewhere else.**

**As of 2026-08-23 the gate and the rebuild are both automated, and the hand procedure below is the fallback.** `rollout_places.js` builds a boarding sheet whenever `routes.json` carries `boardingPlan`, and gates it in its fast path — so `node rollout_places.js --all` now covers every boarding sheet on the board, and a place whose ONLY stale sheet is its boarding plan no longer reports UP-TO-DATE. Before that the tool had no boarding branch at all: a rollout committed an S4 with no `boarding.svg` in it while `_latest` went on mirroring the previous run's copy, so the sheet did not look missing — it looked fine. The same run taught it to read which sheets a place ships off its previous S4 rather than assume an internal, because High Wycombe High Street and High Wycombe Town Centre have never had one. The first thing the new gate found was High Wycombe Town Centre one build behind: the current generator draws a traffic-signal glyph its v1.0 sheet had not got.

**The gate is a byte-identical re-run of every built boarding sheet.** Copy the run folder's `*.json` into a scratch directory, run the candidate `gen_boarding.js` there, and `cmp` the result against the committed `boarding.svg`.

**Which places those are is a question for the tree, not for this document (OA-099).** A boarding sheet exists exactly where `routes.json` carries `boardingPlan`, and `sheet_registry.js` records that boarding is **place-only** — `gen_boarding.js` reads `place.json` on its way in, and `rollout.js`, the area rollout, does not mention boarding at all. Run this from `C:\u3a St Ives\Using AI\Buses`, the repository root, with no placeholders:

```bash
grep -rl boardingPlan --include=routes.json Areas Places
```

There were four on 2026-08-23 and there are still four; the point is that the list is derived rather than typed, so it cannot quietly fall behind the way the town table above did.

**Take the baseline before you edit, and take it by RUNNING the old generator, not by reading the committed file.** Two of these four are already several lines behind the current engine and have been since 2026-08-23, so a straight `cmp` against the committed `boarding.svg` reports a DIFF that has nothing to do with your change: on the High Street build it read "13 changed lines" on two sheets before a line of the new code had run. Copy the `.orig` generator over the same inputs, keep that output, and diff **baseline against candidate**. That is what separated "13 pre-existing" from "1 corrective" and made the change safe to keep. Then also re-run each sheet's own `boarding_verify.py` (and `--self-test` on one, to prove the gate can still go red) and `quality_metrics.js --all`, whose 44-sheet output is a second, wider ratchet: on 2026-08-23 a rotation fix to the metric was accepted only because that whole output came back byte-identical.

Two of the 2026-08-23 changes were **deliberately not byte-identical**: markers that overlap each other now push apart, and a street label now tries more than one position. Both are corrective on the two older sheets (13 changed lines each) and both leave them **stale until re-rendered**, which is the normal state §2a describes — but those two sheets are live on the portal, so re-rendering them is a re-delivery and a decision, not housekeeping.

The High Street build added a third: **the traffic-signal layer now calls `hits()` before drawing**, as every other layer on the locator already did. Corrective, one element each on St Neots town centre (a signal on a stand marker) and High Wycombe town centre (a signal on the `Bridge Street` label), nothing on St Ives. So the standing arrears are St Neots 14 lines, St Ives 13, High Wycombe town centre 1 — and only the first two are live. The other four changes in that build (`standKeyNames`, `emptyStandLabel`, `locatorLandmarkNames`, and a stand-key width guard) default to the previous behaviour and are byte-identical everywhere.

## 4. The portal hand-off — a generator change is NOT done until this is done

The portal (`C:\Claude\community-bus-maps`) holds **byte-for-byte copies** of some engine files. It does not import them from the skill; it vendors them. A skill-side change leaves the portal running the old code, and its gates will keep passing against the *old* shipped fixture until you re-vendor.

**THE LIST OF VENDORED FILES IS NOT IN THIS DOCUMENT ANY MORE.** It is `engine/vendored.json` in the portal, which names every `.js` under `engine/` as either `vendored` (with the skill path it came from and a hash of the bytes) or `portal-owned` (a wrapper with no counterpart here). Read it there; both drift checks read it there too, so nothing can fall off one list and stay on the other.

The table that used to sit here listed eleven files, and `status.js` listed the same eleven, while the portal tree held sixteen. The three nobody was watching were `engine/area/gen_external_busway.js`, `engine/area/gen_external_radial.js` and `engine/place/gen_internal_place.js` — and on 2026-08-25 the radial one turned out to have been stale since 2026-08-21, missing the March X32 badge-overlap fix and the `sheetQr` default, silently, for four days (`technical-audit_2026-08-25` N14). A hand-maintained table of what to check is a list you have to remember to extend; a manifest that fails when the tree holds a file it does not name is not.

**After copying a file across, restamp the manifest.** Run this in the portal repository root (`C:\Claude\community-bus-maps`), and commit the manifest change in the same commit as the file it describes:

```bash
node scripts/check-vendored.mjs --update
```

### AND RE-CUT BOTH COMMITTED FIXTURES, IN THE SAME BREATH (OA-182)

**There are two, and since 2026-09-01 both are a script.** Re-vendoring makes the portal run the new code; the fixtures are what its byte gates compare against, and a fixture frozen alongside the change passes its own gate **by construction** — the old engine reproduces the old sheet perfectly and `verify` reports PASS about code that has not shipped. The area one was forgotten twice in three days: once caught by somebody reading an unrelated backlog row, once by a portal PR's `verify` job going red.

Run these two, in either order, and run BOTH. Neither takes a placeholder. The first is run from the portal root (`C:\Claude\community-bus-maps`); the second can be run from anywhere, because every path it needs is derived — and it asks the town's MANIFEST which S5 run is latest rather than a directory listing, which the recipe it replaces did not: a string sort puts `v1.9_2026-08-18` after `v1.23_2026-08-30` one character in, and that is the bug `status.js`'s own freshness check shipped with. Add `--check` to either to see what would change without writing; the area one exits 1 when the fixture is behind, the same verdict the board gives.

```bash
node scripts/refresh-place-fixture.mjs "C:/u3a St Ives/Using AI/Buses/Places/_portal-fixture/High Wycombe Aldi" --apply
```

**`refresh_area_fixture.js` replaced a shell recipe in a README (OA-182).** A recipe is a thing you have to remember to run, which is a different category of guarantee from a thing you can run — and it was forgotten three times in three days, once caught only by portal PR #162's own `verify` job going red. The script was measured against the artefact rather than against "it ran": pointed at a scratch copy of the tree it reproduces the committed fixture **byte-for-byte**, it names a changed file, it removes a file the run no longer writes, and it excludes the same three things the README always did — the JPGs (rasterisation is platform-dependent by design, which is what `render-parity.yml` is for), `svc_*.html` and `build-warnings.txt`.

```bash
node "C:/u3a St Ives/.claude/skills/make-bus-leaflet/assets/refresh_area_fixture.js" --apply
```

Then verify both with `FIXTURE_DIR` deliberately unset, so you are gating the committed fixtures and not whatever your `.env` points at:

```bash
BUSES_DIR="C:/u3a St Ives/Using AI/Buses" FIXTURE_DIR= npm run verify:area
```

**`status.js` also tells you when the area one has gone stale** and exits non-zero on it, so the board you already run before re-vendoring is the reminder. It and `refresh_area_fixture.js` ask the same two questions the same way — `latestRunDir()` for which run, `sameIgnoringLineEndings()` for whether it matches — so the tool that WRITES the fixture and the check that JUDGES it cannot disagree about what current means. It is a laptop check by construction — `S5-render/` is gitignored, so a fresh CI checkout has no render to compare with — and CI answers the complementary question once the PR is open.

**THE PORTAL VENDORS ITS FIXTURES TOO, SINCE 2026-09-18** (`buses-data` OA-398, R5 of the process review of 2026-09-17), and that is a third step rather than a change to the two above. Both scripts still write into `buses-data`, beside the render each pack comes from; the portal's byte gates now read a COPY of those packs, under `gate-fixtures/` in the portal itself, because `verify.yml` no longer clones the private repository at all. So re-cutting a fixture changes nothing the portal's gates see until the copy is refreshed. From the portal root (`C:\Claude\community-bus-maps`), with no placeholders:

```bash
npm run fixtures:vendor -- --apply
```

**The old instruction here was *push `buses-data` BEFORE opening the portal pull request*, and it is retired.** The fixtures lived in that repository and `verify.yml` checked it out with no `ref:`, so a pull request's engine was gated against whatever was on its `main` at that moment — a truthful red when this side was ahead, and a false GREEN whenever that side was, which is the direction that matters. Nothing in the portal reads that repository at run time now, so the ordering does not matter; what matters instead is that `gate-fixtures/` carries the bytes you mean. Whether the copy has fallen behind is asked by `npm run fixtures:vendor` with no `--apply`, from the laptop and daily from `buses-data`'s own gates workflow.

### AND RE-DRAW THIS REPOSITORY'S OWN FIXTURE ESTATE, IN THE SAME COMMIT AS THE ENGINE EDIT (OA-398)

**This is the step that stops an engine pull request opening red.** `claude-skills` now owns a miniature map estate at `make-bus-leaflet/test/fixtures/estate/` — five maps, twelve sheets — and its `status` job gates that instead of cloning `buses-data`. Two things follow. An ink-moving change makes those twelve sheets stop reproducing, exactly as it does to the real estate. And **every pack's engine stamp goes stale at once**, which matters more than it sounds: `prove-red-held-back` needs a DONOR pack whose stamp IS the current engine, so an edit to any hashed engine file used to empty the donor set across a whole 21-map estate and the harness exited 1 before a single case ran. That is `buses-data` OA-341's ordering trap, where the only thing that could clear the red was a commit in another repository — and appending one comment line to `icons.js` was enough to cause it.

Now the remedy is inside the pull request. From `make-bus-leaflet`, with no placeholders:

```bash
npm run fixture:estate -- --apply
```

Run it with no `--apply` first if you want to see what moved; it exits 1 when the fixture is behind, which is exactly what the `status` job reads. Commit `test/fixtures/estate/` in the SAME commit as the engine edit — a fixture refreshed in a LATER commit is one whose own gate passed by construction in between, which is the trap this section's first paragraph is about, one repository along.

### ✅ CLOSED 2026-08-26 — A NEW MODULE is a hand-off the drift table could not warn you about, and now it can

`gen_internal.js` gained a `require` of a new sibling, `lane_normals.js`, when `design.laneOrientation` was added. The requirement it creates is absolute and it is not a byte gate: **the portal must receive both files or neither.** The generator requires the module at load, before it reads a thing, so a portal that got `gen_internal.js` alone would throw on the first internal render of any map — area or place — whatever any config key said.

**Nothing in `status.js` will tell you.** Its drift table reads `engine/vendored.json` and compares the files the PORTAL already has. A file the portal does not have yet is not in that manifest, so it is not a row, so it cannot be red. The one row you do see — `gen_internal.js → engine/place/gen_internal.js` differs — looks like an ordinary stale vendor and says nothing about the new dependency riding with it. **Since 2026-09-02 (OA-224 Tier 3.6) the fix is a command, and `check-vendored.mjs` prints it for you.** Every `UNRESOLVED` row is now followed by the exact line to run from the portal root: `node scripts/vendor-engine.mjs --add <engine/path.js> --source <path under the skill root>`. It writes the manifest row, copies the file, and lets the restamp compute the SHA-256 from the bytes that landed — the hash is never typed, which matters because a hash is the one thing in that repository nobody can check by eye. It refuses a `--source` the skill tree does not have (exit 3) and a path that already has a row (exit 2), and where the skill trees are absent it offers the likely sources and says they are unverified rather than guessing at one. What it still cannot do is decide WHICH skill the file came from when both have it — that is `--source`, and the audit that follows is what tests the claim.

It also joined `ENGINE_FILES` in `engine_version.js`, so the template hash moved. A file that decides where a lane is drawn and is outside the hash would let the engine change while the stamp stayed put.

**Closed 2026-08-26.** Both files are vendored, `lane_normals.js` has its own manifest row, and the whole round was byte-inert: `npm run verify:place` reproduces all four place sheets across both fixtures byte-for-byte, because `design.laneOrientation` is absent from every fixture and the OFF path is the code that shipped before it. Measured the same day, on the area fixture too: rendering it through `628a881` (the commit before the lane change) and through the new engine gives the identical md5, so the key really is the only thing that moves.

**Two things the round taught that this note had not predicted.**

**1. The hand-off IS provable, and no document said with what.** `npm test` cannot do it — `test-p7.mjs` and `test-lifecycle.mjs` write `'// stub\n'` in place of a generator, so nothing there ever loads the real one. Neither can `verify:area` or `verify:defaults`, which run the *fixture's own* `gen_internal.js` because it travels with the map. The one gate that exercises the vendored copy is **`npm run verify:place`** (`scripts/verify-reproduce-place.mjs:93` copies `engine/place/*` into its scratch and renders through them). Copying `gen_internal.js` alone and running it gives exactly the failure this section predicted, in the place it predicted:

```
Error: Cannot find module 'C:\Claude\community-bus-maps\engine\lane_normals.js'
    at Object.<anonymous> (…\gen_internal.js:176:12)  code: 'MODULE_NOT_FOUND'
```

Note the path it names. The module goes at **`engine/lane_normals.js`**, the engine ROOT beside `icons.js` — not `engine/place/` beside the generator that requires it. `renderMap.js` passes `SKILL_ASSETS = engine/`, and the copy `import-map.mjs` plants in a map's `data/` folder has no sibling to find first.

**2. "The drift table cannot warn you" was true of a table, not of a check.** `UNLISTED` walks the tree and asks what is here that the manifest does not name; `MISSING` walks the manifest and asks what it names that is not here. A file in NEITHER — because nobody has copied it across yet — is not a row in either direction. So the portal now enumerates a **third population: what the vendored code asks for.** `requireScan()` in `scripts/lib/vendored.mjs` extracts every `process.env.SKILL_ASSETS ? path.join(process.env.SKILL_ASSETS,'x.js')` literal out of every engine file and fails with `UNRESOLVED` when the target is not there. In the exact state this section described, it prints one row naming the file and its requirer, where before there was only `place/gen_internal.js DRIFTED` looking like an ordinary stale vendor.

The `?` in that pattern is load-bearing in one direction and was wrong in the other — see the correction immediately below. `diagram_internal.js` and `schematize_internal.js` use `process.env.SKILL_ASSETS && path.join(…, 'gen_internal.js')` as one candidate in an `existsSync` list that reports its own miss, and matching that form too would have opened the check red on day one, which is how a check gets muted rather than fixed. That exclusion still stands.

### ⚠️ CORRECTED 2026-08-27 — the scan was blind to the way two generators already ask, and a tidy-up made it blind to a third

**A helper that makes the code tidier can make a checker blinder, and the checker will keep saying yes.** On 2026-08-27, OA-129 Phase 3 extracted four modules out of `gen_internal.js` and — as part of the first extraction — replaced its four copies of the resolver IIFE with the single `_dep(name)` helper `gen_boarding.js` already had. The generator was re-vendored carrying `require(_dep('strict_guards.js'))` and three more like it, **none of which were in `engine/`**, and `check-vendored.mjs` reported *"21 engine files accounted for, and every module they require is here."*

**Why.** `requireScan()` recognises a dependency by finding the module NAME as a literal beside `SKILL_ASSETS`. Factor the resolver into `_dep(name)` and the only thing beside `SKILL_ASSETS` is the parameter — the literal has moved to the call site — so the pattern matches **nothing at all** and the file reads as requiring nothing. This was not new damage: **`gen_boarding.js` had been invisible in exactly this way for as long as it has used `_dep`**, and `gen_external_places.js` writes a third form, `if (process.env.SKILL_ASSETS) return path.join(process.env.SKILL_ASSETS,'footer.js')`, with no `?` at all, which the pattern never matched either. Three of the five generator files were outside the scan and nobody had noticed, because their dependencies all happened to be present for other reasons.

**It would have failed everywhere but here.** Each of the four is required at LOAD, so the portal throws `MODULE_NOT_FOUND` on the first internal render of any map. It did not fail on the laptop only because `_dep`'s last-resort branch is a hardcoded path into the skill checkout — the same shape as the recorded lesson that a dependency which resolves is not a dependency that is portable.

**The fix, in the portal (PR #111, `1bd0e4b`).** `SKILL_ASSETS_REQUIRE` is now a LIST of three patterns — the `?` form, the `if (…) return` form, and `_dep('x.js')` — and the `&&` candidate-list exclusion is written down beside them rather than left to be rediscovered. Widening the first pattern far enough to swallow `_dep` was tried first and immediately caught `diagram_internal.js` and `schematize_internal.js`, which is the thing the paragraph above warns against. Five assertions in `scripts/test-vendored.mjs` now cover all three idioms plus the exclusion, and the whole set was watched go red: with the fix in, the check named exactly the four missing modules and nothing else; with the `_dep` pattern removed again, the suite fails on the case that matters.

**The rule for anyone extracting a module from a generator.** Adding a `require` is a portal hand-off, which this section already said. What it did not say is that **changing HOW the require is written is also a portal hand-off** — the scan's population is "what the vendored code asks for", so it has to keep up with how the code asks. After any change to a resolver, run `node scripts/check-vendored.mjs` in the portal and confirm it can still see the file's dependencies at all, rather than reading a green tick as coverage.

**And re-vendor at the end of each working session, not at the end of the phase.** OA-129 originally put the re-vendor in its Phase 4 close-out. Five extractions in, `status.js` was exiting 1 with two `DRIFTED` rows and would have stayed that way for days — a board that is expected to be red is a board nobody reads, and this repository's own rule is that a generator change is not finished until the portal has been re-vendored.

**The scan then passed its first real test, on extractions 6–8 (2026-08-27).** Copying `gen_internal.js` across before writing the three manifest rows failed the audit and named all three by name — `UNRESOLVED  required through SKILL_ASSETS by place/gen_internal.js — vendor it and add its manifest row` — where a day earlier, in exactly that situation, it had said every module they require is here. Doing it in that order deliberately, once, is worth the thirty seconds: it is the only way to see the guard fire on a real new module rather than on a fixture.

### ⚠️ IT HAPPENED AGAIN 2026-09-02 — the same class, in BOTH scanners, from one edit

**A resolver change is a scanner change, and there are TWO scanners.** OA-224 Tier 3.4 moved the resolver into `engine_paths.js` and, with it, rewrote `path.join(path.dirname(_LABELLER),'x.js')` as `_from('x.js')`. Neither scanner knew that idiom, and both said yes:

- **The engine's own `engine_version.js`.** `dash_fit.js` had been reachable only through the `path.dirname` form, so it fell OUT of the hash closure in the same edit that put `engine_paths.js` IN — and **the closure stayed at exactly 21 files**. The verdict was green and the count was unchanged; only listing the NAMES showed the loss. A map's stamp would have stopped covering the dashed-spoke code with nothing to say so.
- **The portal's `requireScan()`.** `external_primitives.js` is required by all three external generators through that idiom ALONE, and the audit reported *every module they require is here* over an `engine/` that did not contain it. The vendored radial would have thrown `MODULE_NOT_FOUND` on the first external render on the VPS.

**Both had said so in writing.** This file's §1a already carried *a scanner is not proved by the answer it gives; it is proved by a name only it can find*, and `scripts/lib/vendored.mjs`'s header named the `path.dirname` arithmetic as something it could not see, adding that neither of its two blind spots *"would be caught if a future edit introduced them for the first time"*. **Read a stated limit as a bug report with a date on it.**

**What changed, so this class is smaller.** Both scanners read the idiom now; `engine_version.test.js` asserts every idiom against a FIXTURE written for the purpose instead of through whatever the estate happens to contain (the 2026-08-30 test pinned the widening through `dash_fit.js`, and stopped asserting anything the moment `dash_fit.js` was reached another way); `tools/prove-red.js` carries a **half-migration** target — only the old spelling followed — which is the shape rather than the instance; and `scripts/test-vendored.mjs` has two new cases, confirmed red with pattern 4 removed. **After any change to how a require is WRITTEN, do three things and read all three:** `node -e "console.log(require('./assets/engine_version.js').engineFiles().join(' '))"` from `make-bus-leaflet` and diff the LIST against the one before, not its length; `node scripts/check-vendored.mjs --skills "C:/u3a St Ives/.claude/skills"` from the portal root; and `npm run test:selfsufficient` there, which names the modules actually loaded.

### Extracting a module: the loop that has now done twelve of them

Written down because the method, not the target shape, is what makes this safe. Extract, never rewrite: move a block into a module along the comment banners already in the file, have the script require it through `_dep`, and prove all 20 maps byte-identical after **every single extraction**.

1. **Count the real interface before designing the module.** Grep each name in the block for uses AFTER the block. `projection.js` looked like twenty-odd exports and was eleven; `linear_features.js` looked like a dozen and is four, because its eight polyline helpers are called by nothing outside. Check the deps in the other direction too — a name defined BELOW the block cannot be passed into a factory built above it, which is why `drawFeatureLabel` stayed in `gen_internal.js` when the rest of the linear features moved out: siting a feature's name needs the coreBox, the panel edge, the footer plate and an auto-label solver that has not run yet. **A dependency that points forwards is a sign the boundary is in the wrong place, not a problem to engineer round.** And an interface can come out at zero: `services_panel.js` moved 598 lines and exports nothing, because not one of the thirty-odd names it declares is read below the block. **Use `grep -w`, not `awk` with `\<name\>`** — awk does not support those word boundaries, reads them as literal `<` and `>`, matches nothing, and reports every name as unused. That is a perfect-looking answer to the wrong question, and it agrees with what you were hoping for; `grep -w` found the one real hit among thirty-four, which turned out to be a block-scoped shadow twelve lines further on. The check that confirms your expectation is the one to re-run with a different tool.
2. **Run the gate after every extraction, not at the end.** `npm run gate:extraction -- --baseline` **before the first one**, then `npm run gate:extraction` after each: it reduces `status.js --json` to its 74 sheet verdicts (68 map sheets plus the two portal fixtures) and prints only what moved. **27 seconds**, which is what makes it affordable per extraction rather than per session. It reports portal drift as a note and never gates on it, because between an engine change and the re-vendor that closes it the portal is *expected* to differ. The first cut of this script let `execFileSync` throw, and `status.js` exits 1 whenever the board is red — so it died with a stack trace instead of naming the 30 sheets a deliberate mutation had turned red. The try/catch in it now is the whole point of the file: **a harness that only works while everything passes is not a harness**, so falsify it once yourself before leaning on it.
3. **Measure which branches the maps take by INSTRUMENTING, never by reading the config.** `npm run gate:branch-coverage -- tools/<your-spec>.js` — write a small spec naming the module, the anchors to instrument and the labels to count; [`tools/branch-coverage.linear_features.js`](../tools/branch-coverage.linear_features.js) is a worked one that still runs, with its expected answer in its header. About a minute for 18 maps, and it copies the engine to a scratch folder so nothing under `assets/` is touched. Reading `routes.json` instead gave the wrong answer by seven maps on the first try, because a town with no `features[]` still gets a river from the legacy fallback and its `hide` override therefore does fire. Put the unit tests where the count is **zero or one**; where every map takes the branch, write only what the byte gate cannot express — ordering, precedence, a round trip.
4. **Check the two things no byte gate reads**, before and after: `npm run gate:dark-paths -- --before <the generator at the previous commit>`, then `-- --after assets/<generator>.js`, then `-- --diff`. It compares the SVG hash, the `data-kind` count, the exit status and the normalised stderr for every map under `EDITOR_KEYS=1` and `STRICT_GUARDS=1`. Both are genuinely dark to `status.js`: `gate_lib.runGenerator` deletes `EDITOR_KEYS`, so `gk()` emits nothing on the board while **18 of 20** maps emit `data-kind` attributes once it is set; and stderr is in no artefact, while **every one of the 18 maps that has an internal sheet writes to it** (the other two of the 20 have no internal sheet to render). The normalisation is not housekeeping — temp paths and `at …:LINE:COL` frames move whenever a file's length changes, which an extraction always does.
5. **Add the prove-red mutations with the tests, never afterwards.** A survivor is almost always a hole; occasionally it is an **equivalent mutant** — a break with nothing to break, because the mutated code computes the same answer. `badgeStack`'s one-element fast path is one: delete it and the general loop draws the same bytes at every radius, since `y0` collapses to `y`. Do not write a test asserting a difference that does not exist. Replace the mutation with an observable one, record in `prove-red.js` why the equivalent one is absent, and go back and correct the source comment that claimed a fork the code does not have.
6. **Re-vendor before you stop**, per the paragraph above. A row for a file the manifest has never named is `node scripts/vendor-engine.mjs --add <engine/path.js> --source <skill/path.js>` from the portal root — `--update` only restamps rows that already exist, which is why adding one used to be a hand-edit of a file holding a hash.

**Three findings from extractions 6–8 that a reader of this engine should know.** The **whole v1 label placer is dark**: every one of the 18 maps with an internal sheet runs v2, so the eight-candidate greedy search, the manual-offset path, the icon-box relaxation and the give-up return are certified by no gate. It is a live feature `labels.engine` selects, not dead code — do not delete it. The **OS-style railway tie symbol is drawn by nothing**: all six railway maps take `rail:"chequer"`, which sets `ties:false` and `minSegLen:0`, so the tie keys and the stub filter are both dark. And the **entire hand-adjust path for a linear feature** — overrides `segments`, `points` and `move` — has never been used on a shipped sheet.

**Extraction 9 added a fourth, and it is a whole layout.** `design.panelCols` — the multi-column Services list, with its own badge-radius floor, its own type-scale check and its own `badgeFit` width guard — **is drawn by no committed map**. High Wycombe is the only town with a `panelCols` block and it sets `panelCorridors` as well, which wins the if/else above it; only `panelCols.keyAt` has ever been read, to pin the Key. Alongside it the whole **`design.panelScale` opt-out** is dark on all 18 maps, which means the hand-tuned size set that the "absent config ⇒ byte-identical" invariant exists to protect is certified by nothing but `test/services_panel.test.js`. Same rule as the v1 placer: these are live features a key selects, not dead code.

**What it still cannot see, and both are already manifest rows saying so:** `font_metrics.js`, reached by `path.dirname(_LABELLER)` arithmetic rather than a literal, and `qr.js`, required lazily inside `footer.js` only when a sheet asks for a code.

**Extractions 10, 11 and 12 (2026-08-27) finished the named blocks.** `complexity_ladder.js` — the four rungs, in three functions because they are read at two different moments: the config read aliases colours IN PLACE (so `C` and `TXT` go in and are mutated), the coreBox geometry cannot run until the projection exists, and `thinKeep` needs the laneKey the config read returned. `north_arrow.js` — the compass, which was inline top-level code inside `if (NORTH_ON) {…}` in three pieces 1,100 lines apart, and has to stay in three: the angle decides the footprint, the position cannot be settled until the ink is stamped, the drawing happens last. `feature_labels.js` — the block extraction 7 deliberately left behind, built LATE, after `AUTOPOS` and `isAuto`, rather than beside `linearFeatures()`. `gen_internal.js` is **3,933 → 2,550, a 35% cut**, and the remaining named banners are the main draw itself.

**The template hash was one of the things this refactor broke, silently, and it is fixed.** `engine_version.js` listed five files by hand; ten extractions had moved most of the drawing code into siblings on nobody's list. MEASURED on the day: appending a line to `services_panel.js` did not move the hash at all, nor did `complexity_ladder.js`, nor `labeller.js` or `footer.js`, which had never been on the list. `engineFiles()` now walks the transitive closure from the same five entry points, following the four idioms this engine uses to name a sibling — `_dep()`/`_from()`, `path.join(<dir>,'x.js')`, a bare relative require, and the `SKILL_ASSETS` forms `requireScan()` reads. Five files became eighteen and have kept growing — sorted, so the hash cannot move when an extraction reorders requires. **The number is deliberately not written here**, and the sentence that used to end "ask , as §1a now says" had lost the command it was pointing at, which is what quoting a total from a document costs. Ask the run, with the command in the pricing paragraph above. **An extraction no longer has to remember the hash — but a new WAY of writing a require does, exactly as it does for `requireScan()`.**

**And widening it broke a test's premise without breaking its assertion**, which is the sharper half. `engine_version.test.js` proves the file NAME is hashed by building two trees whose content-plus-delimiter streams collide — a construction that needs the two files to be ADJACENT in hash order. Sorting put a third between them, so the streams differed for a reason that had nothing to do with names, and the mutation that test exists to catch quietly survived. The suite had stopped proving the property while still passing. It takes the first two in sorted order now.

**DARK MEANS "NO MAP IN THIS RUN TOOK IT", AND THE RUN IS A CHOICE — two traps, both hit in the same session.** `gate:branch-coverage` renders `internal.svg` only, so `north_arrow.js`'s `angle` branch reported ZERO and is taken by **twelve committed sheets**: `schematize_internal.js` and `diagram_internal.js` inject an explicit angle before re-running the generator, because their coordinates are pre-rotated and run at rotationDeg 0. Deleting it would point north straight up on every schematic and diagram — maps that are not north-up — with the internal gate still green. And a mark placed on the NEXT guard's `if` sits after the `return` it was meant to observe: three of `feature_labels.js`'s refusal rows could not have fired whatever the maps did. Re-anchoring them gave the same 17 dark of 34, which is the point — a number that cannot change is not evidence, even when it turns out to be right.

**And the copying is no longer done by hand.** `npm run vendor:engine` copies every `vendored` row from its skill source, restamps, and then runs the audit — the July 2026 dedup proposal's Option B, five weeks after its Option A shipped as `check-vendored.mjs`. Its DEFAULT run cannot invent a row for a module nobody has listed, which is precisely why it and `requireScan()` are a pair: one moves the bytes, the other notices when the set of bytes should have grown. **`--add` is the third piece and it landed 2026-09-02** (OA-224 Tier 3.6): `requireScan()` reports `UNRESOLVED` and `check-vendored.mjs` prints the exact `--add` command for that file, so the answer to the refusal is a command rather than a procedure.

### ✅ DECIDED 2026-08-27 — a published map TRACKS the engine, and one command is what makes that true

**Peter's decision on OA-130: track, not freeze.** A map's data pack carries its own copy of `gen_internal.js`, and the portal's `generateSvg()` runs THAT copy — `path.join(dataDir, generator)`, with no fallback to `engine/` — so until this was decided, an engine fix did not reach a published map at all until somebody re-imported it. Measured the same day, the stored packs were **1,383 lines behind** and required eleven modules they do not mention, which happen to resolve because `SKILL_ASSETS` points at the portal's `engine/`. Freezing would have guaranteed that a published map re-renders identically for its whole life; tracking means one fix reaches every map, and that is the choice.

**So the hand-off gains a step, after the re-vendor and before you stop.** From the portal repository root, no placeholders:

```bash
npm run track:engine
```

It reports which stored packs are behind the vendored engine and **exits non-zero when any is**, so it is a check as much as a report — a re-vendor that forgets this step fails it. `node scripts/track-engine.mjs --apply` brings them forward. On the VPS it runs inside the container, where `DATA_DIR=/data`; on the laptop it reads the dev store under `data/`.

**It never re-renders, and that is the whole safety of it.** The stored SVGs and JPGs are untouched, so nothing a member of the public can see changes on the day you run it. What changes is the NEXT render of each map — a preview, an accepted proposed update, a re-publish — which then uses the current engine instead of the one frozen at its import. Proven rather than assumed: after applying it locally, two maps were rendered straight through `generateSvg()` and both wrote a sheet, resolving all eleven modules out of `engine/`.

**An area pack's `gen_external.js` used to be deliberately skipped, and since 2026-08-30 it is not (OA-143, closed).** It is copied from either `gen_external_radial.js` or `gen_external_busway.js` and the FILENAME cannot say which, so the tracker still will not guess — but the answer is now RECORDED, in `engine-source.json` beside the generator, by `import-map.mjs`, which knows it because it chose the file it staged. A pack that declares is tracked like any other; a pack that does not is still skipped, and the report says which of the two it is. **This mattered more than it sounds:** the skip was eight of the eighteen live maps on every run, every one of them a town's EXTERNAL sheet, so an engine change that touched only `gen_external_radial.js` reached the ten place maps and no town at all. The live store now reports `46 already current, 0 BEHIND, 0 skipped` where it had said 38 / 0 / 8 since the tracker was written. `node scripts/backfill-engine-source.mjs --apply` is the one-off that filled in the packs predating the change; it is idempotent, and running it after a fresh import is a no-op.

**And the script reads `DATA_DIR` the way `src/db/index.js` computes it rather than importing that module for the constant** — importing it opens and MIGRATES the database as a side effect, which is a great deal of machinery, and a great many ways to fail, for a script that only reads files off disk. The first cut did import it and died on a migration in the dev database.

### The SVG vocabulary is now a portal-side allowlist — a NEW ELEMENT is a hand-off too

Added 2026-08-25 (`technical-audit_2026-08-25` N18). The portal inlines a published sheet into a web page as live DOM, and since that date it filters it through an **allowlist** in `src/public/svgSanitise.js`: eight elements — `svg`, `g`, `path`, `rect`, `circle`, `line`, `text`, `clipPath` — plus `tspan`, `title` and `desc`, and 38 attribute names. That list is a census of what these generators emit, measured across all 1,277 sheets in the map tree.

**So if you make a generator draw something new — a `<tspan>`, a gradient, a `<use>`, a `style` attribute — the print sheet will be right and the WEB view will silently lose it.** The portal counts and logs every drop, and `scripts/test-inline-svg.mjs` goes red on the fixture corpus, but neither is in this repository. Treat it as a fifth step of the hand-off: add the element to `ALLOWED_ELEMENTS` (or the attribute to `ALLOWED_ATTRS`) in the same round as the re-vendor, and if it is URL-valued say why in the Caddyfile's CSP block, which reasons about exactly this sink.

That trap is not hypothetical: the allowlist shipped without `tspan` because the census covered generator output only, and the portal's own pilot band — stamped on after the generator runs — uses one. Every inlined sheet lost the words "PILOT — SAMPLE MAP" for an hour.

The two checks ask different questions and neither can answer the other's. The portal's `scripts/check-vendored.mjs` runs inside its `npm test`, needs nothing but the portal itself, and asks *has the portal's copy been edited since it was vendored* — so it works in CI, where this skill tree does not exist. `status.js` here runs on the laptop where both trees are present and asks *has the source moved on without us*, which is the drift this section is about; it reports `UNLISTED` for a portal file the manifest does not name, so the population is checked from both ends.

**Area (town) generators are NOT in this table** — `gen_internal.js`/`gen_external_*.js` for an area map are copied straight into that map's own `data/maps/<id>/data/` by the portal's `scripts/import-map.mjs`, from whatever `--src` was used at import time, not from `engine/`. So an existing area map (St Ives, March, …) stays on whatever generator it was imported with, same as a town in this repo stays on whatever its own S3 run committed — re-importing (or manually refreshing that map's `data/maps/<id>/data/gen_*.js`) is the only way an already-built area map picks up a skill-side change. **If you're live-verifying a change by clicking through the portal against an existing demo area map, check that map's own generator copy first** — a stale one will silently no-op the new behaviour even though the sanitize/validation layer accepts it fine, which reads exactly like a bug in the new feature until you notice the file predates your change (caught 2026-08-03 verifying `hiddenOperators` against March's demo map).

**Procedure after any change to a file in that table:**

1. Pass all the town gates in §2 first. Don't propagate a change that hasn't been proved locally.
2. Copy the changed file(s) verbatim to the portal destination.
3. In the portal, set `FIXTURE_DIR` and `PLACE_FIXTURE_DIR` in `.env` (git-ignored) to point at the staged fixtures in the Buses repo. **`npm run verify` exits 0 with "skipping" if they are unset — a green run then proves nothing.**
4. Run, and require all to pass:
   ```
   npm run verify:area
   npm run verify:place
   npm run test:p7
   npm test
   ```
5. If a gate now legitimately fails because the *output changed on purpose*, the shipped fixture is stale: re-render the fixture town/place from the new engine, re-import it, and record why in the portal `CHANGELOG.md`. Never edit a gate's expectation to make it pass.
6. Commit **both** repos in the same session — the skills repo (`C:\u3a St Ives\.claude\skills`) and the portal — and note the pairing in each commit message.

**The `LEAFLET_DIR` trap.** `gen_internal.js` prefers `LEAFLET_DIR` over cwd. A pre-stage spawns it with cwd = the workspace and an inherited environment — so an inherited `LEAFLET_DIR` sends the render back to the parent folder and silently reproduces the ordinary geographic map instead of the schematic/diagram. The portal wrappers delete it for the child; the skill's own invocation must run **without `LEAFLET_DIR` set**. If a schematic or diagram comes out looking like the plain internal map, this is why.

## 5. Finishing the session

Per the standing review step in `SKILL.md`:
- Fold anything that tripped you up into `gotchas.md` (or the relevant stage reference).
- If the change altered the duplication picture, update the duplication map in `…\Buses\Documentation\README - How to enhance the system.md` **and** this file's §4 table.
- Commit every repo you touched. Parts 1 and 2 have no remote — an uncommitted change is one disk failure from gone.

## 6. Known rough edges

The duplication in §4 is manual and has no automated drift check: nothing fails if the portal's vendored copy diverges from the skill's. A written proposal to fix it is at `…\Buses\Development Docs\engine-deduplication-proposal_2026-07-25.md`. Until it is actioned, §4 **is** the mechanism — follow it every time.

A one-line drift check over the §4 table is worth running whenever you open this doc:

```
cmp -s "%SK%/<file>" "<portal dest>/<file>"   # per row; silence == in sync
```

**Drift found and RESOLVED, 2026-07-28.** `engine/expert/diagram_internal.js` had gone *portal-ahead*: the portal's copy wrote a `wll` field (workspace lat/lon per solved junction) into `solved-nodes.json` that the skill's copy did not, added during P7 for the admin pin editor (`src/expert/index.js`), which needs it to line its handles up with the sheet. Back-ported to the skill; the two files are byte-identical again (46,996 bytes) and **all six rows of the §4 table were verified in sync**.

Two things that made the back-port safe to do by straight copy, and are worth repeating next time:

- **The change was confined to one block.** `diff` showed exactly two hunks, and both helpers it calls (`INV`, `rll`) already existed in the skill's copy, so `cp portal skill` back-ported the intended change and nothing else. Check that before copying a whole file in either direction.
- **`solved-nodes.json` is an editor sidecar, not a drawing input.** The block writes it after the SVG work is done, so the field is genuinely SVG-neutral. Proven rather than assumed: St Ives v6.7's `internal-diagram.svg`, `internal-schematic.svg` **and** `internal.svg` all re-hashed byte-identical, only `diagram/solved-nodes.json` changed (25/25 entries gained `wll`), and the 14-run gate set stayed 14/14. Sanity-check the values land in the **workspace** coordinate space, not real lat/lon — St Ives' are ~±0.01, inside the workspace `atco2ll.json` range, which is the point of the field.

**A portal-ahead drift is the easy one to miss**, because the skill's own gates all pass — the skill is self-consistent, it is simply behind. Only the `cmp` over the §4 table catches it, so run that check whenever you open this doc, not just after you change something.

> **Caveat on that `cmp` — the two repos disagree about line endings** (noted 2026-07-28, not fixed). The skills repo has `core.autocrlf=true` and **no `.gitattributes`**; the portal has `core.autocrlf=false` plus `.gitattributes` `* text=auto eol=lf`. Both *store* LF, and both working copies are LF today, so `cmp` is honest right now. But a **fresh clone or checkout of the skills repo writes CRLF** into the working tree (~970 CRs in this file alone) while the portal's stays LF — after which `cmp` reports **every row of §4 as DRIFTED** when nothing has actually diverged. Don't react to that by copying files around; confirm with `diff <(tr -d '\r' < a) <(tr -d '\r' < b)` first. The clean fix is to give the skills repo the same `.gitattributes` as the portal, but that renormalises every tracked file, so it is Peter's call, not a side effect of an engine change. (Source line endings do **not** affect generator output — the SVG writers emit `\n` from string literals — so the byte-identical gates are unaffected either way.)

