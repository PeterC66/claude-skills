# Gotchas / fixes

Reactive troubleshooting reference for the `make-bus-leaflet` workflow — read when something misbehaves. `%SK%` = the skill's `assets` folder.

## Design quality / the label placer (2026-08-15)

- **`quality_metrics.js` reads the SVG a copy of `routes.json` beside it, or every ink measure silently reads far too low.** Without it, `isRouteInk()` falls back to a `width >= 2.0` rule, which misses every 1.7 mm route line — so a scratch build measured in a bare folder reported "point labels over route ink" collapsing from 13 to 0 and the ink share by ninth as `0% 0% 0% … 1% 7% 8%`, which is what an empty map looks like. The numbers were spectacular and entirely fictional. **If a measurement improves implausibly, check what the tool thinks the route palette is before believing it.**
- **The metric stamps ink from the path `d` attribute, and a clipped polyline is clipped, not trimmed.** Route tails still carry the vertices that fall outside the frame, so the raw geometry credits the sheet with ink that is never painted. It did not matter while the frame was the full 30–205 band; it mattered the moment `design.footerSafe` shortened it, because "route ink under the footer" then barely moved on sheets whose map genuinely no longer reached the band. `parseSvg` now tracks `clip-path` on the group stack and the ink loop clips each segment to the frame first. This changed the frozen Phase 0 baseline from 658 to 657, and the legend fix below took it to 628 — both called out in the scorecard, per the rule that a measurement change invalidates the comparison.
- **An external sheet's operators legend is a floating box, not a reserved column, so its own text was being counted as map labels "into the panel"** — five on St Ives, every one of them the legend's own content inside its own box. A fabricated defect is worse than no measure: `quality_metrics.js` now excludes text inside the detected legend rect, exactly as internal sheets exclude everything right of the panel edge. It surfaced because Phase 4 blocked the legend for the placer and the number did not move.
- **A label placed beyond the leader-line length cap used to be drawn anyway, without its leader.** The position was accepted by the cost function, then `_record()` silently failed the length test and set `leader = null` — leaving six High Wycombe names stranded in the left margin pointing at nothing. **A "can I do this?" test belongs in the candidate filter, not in the code that draws the result.** `_leader()` now rejects the candidate outright.
- **A terminus label's `own` exemption must match the box the cluster actually reserves, not the row it belongs to.** The frame-cut cluster reserves `bxMin-3.5 … bxMax+3.5` around ALL its badge rows *after* the per-row loop, so a label exempted only from its own row found every candidate blocked by its own badges and was dropped (St Ives' `to Boxworth`). The requests are queued during the loop and added afterwards, with the cluster's real extent as `own`.
- **Adding more candidate positions can lose labels.** A third leader ring at 3.9× the nominal gap was tried on the theory that more reach means more placements; High Wycombe lost five more names, because the extra reach let low-priority labels claim distant space that higher-value ones then could not use. Two rings only. **Measure a placer change across every town, not on the one sheet that motivated it.**
- **Taking the colour out of an icon set is not a flat recolour.** Two traps, both found by rendering rather than by reading the diff: a **pale fill is a backing plate**, so sending it to charcoal turns the allotments glyph's bed into a solid black block; and a symbol that was **already a neutral grey was drawn light on purpose** — flattening the industrial estate's `#777` to charcoal made a cluster of factories the heaviest ink on the High Wycombe sheet, which is the exact opposite of what removing colour is for. `icons.js`'s `inkify()` sends pale fills to white and keeps an already-neutral colour's tone, and only genuinely coloured symbols go to charcoal.
- **Anything drawn AFTER the labels needs to reserve its box, or it will land on one.** The north arrow is emitted at the very end of `gen_internal.js`; on High Wycombe it printed straight through route 130's terminus badge and the railway, and nothing complained because no placer had ever heard of it. Under v2 it now **places itself in the nearest blank corner** and reserves that box before the labels are solved. **Reserving it is not gate-neutral**: the first cut reserved unconditionally and immediately DIFFed two town diagrams and a place fixture, so it is v2-only.
- **The labeller's ink grid is not a map of "what is drawn" — do not reuse it for that.** It counts route ribbons and dark features, because that is what a *label* must not sit on. The river (`#9ec9e8`) is too pale to qualify, so the first auto-placed north arrow went straight into the middle of the Great Ouse on St Neots, with the search reporting the spot as 0% covered and meaning it. Anything that needs "is this square centimetre empty?" wants its own, broader stamp.
- **`if (naCfg)` vs `if (naCfg !== false)` — a default-ON feature must have a default-ON *injection*.** The schematic and diagram pre-stages compute the north bearing and inject it as an explicit `angle`, because their workspaces run at rotation 0 and cannot re-derive it. Both tested the key's **truthiness**, which was fine while every town pinned an explicit `{x,y}` — and silently wrong the moment towns started letting the engine own the arrow, because an absent key then skipped the injection and the schematic's compass pointed straight up on a map that is not north-up. Caught by comparing the drawn shaft's bearing across all three St Ives sheets before rolling out, not by any gate.
- **A config rollout without `--force` reports success and does nothing.** `rollout.js` opens by gating the town's previous S4 against the current template and returns `UP-TO-DATE` when it passes — which it does, because that S4 folder still holds the **old** `routes.json`. A freshly committed S3 is invisible to that check. The sequence is `adopt_config.js --apply` then `rollout.js --apply --force`; see `changing-the-engine.md` §2b.
- **Adding a dev tool to `assets/` does not make every town STALE, but editing a generator does.** `engine_version.js` hashes the engine files, not the whole folder, so `preview_design.js` / `adopt_config.js` / `quality_metrics.js` can be added freely. Read the hash off `status.js` rather than assuming it moved (or didn't) — a note quoting a hash from one round earlier is worse than no note.
- **`spreadIcons` needs a deterministic push direction for exactly-coincident symbols.** Two POIs at the same point have no separating vector; deriving one from the pair's loop indices keeps the output byte-reproducible, where anything hash- or random-based would break invariant 5.
- **Putting a hard-coded size behind a ternary can break every gate on a trailing zero.** Making the panel's type sizes configurable turned `font-size="3.0"` into `font-size="${PS?PS.sub:3.0}"`, and JavaScript renders the number `3.0` as `"3"` — a one-character diff that failed all 27 byte-identical gates at once with the key **absent**, which is the one case that was supposed to be untouched. The fallback has to be the string `'3.0'`. Any value whose literal form has a trailing zero, or more decimals than its numeric value needs, has this trap.
- **Equal arithmetic is not equal optical spacing when the rows are not all text.** The first panel-rhythm pass gave `Services` and `Key` the same baseline-to-cap-top gap and `Services` still looked tighter, because a route row's tallest ink is the **badge disc** (3.4 mm above the title's baseline against the cap's 2.52 mm), not its text. Space to whatever actually stands highest in the row.
- **A feature label is drawn outside the map's clip group, so a bad `labelPos` lands in the Services panel.** Wisbech shipped for months with "River Nene" struck through `46 Wisbech – March` and "A47" adrift under the Key, at `x=210` and `x=250` against a frame that ends at 196. Neither the byte gate (it is deterministic, just wrong) nor the panel-intrusion metric (it counts point labels) could see it. `drawFeatureLabel` now refuses a label right of the frame and says so on stderr, the same way the `coreBox` guard has always refused one inside the town-centre box.
- **A config key that lives inside an ARRAY needs an escape hatch in both tools — never hand-edit a committed S3 to get at it.** `--patch` / `--set` deep-merge objects and cannot reach `features[]`, `internalCorridors` or anything else in a list. `--rail` was the first escape hatch; **`--feature-pos <key>=<x>,<y>`** is the second (2026-08-15). The tempting shortcut — let `adopt_config.js` create the S3, then edit the `routes.json` it has just written — works exactly once and leaves the change unreproducible. Add the flag to `preview_design.js` **and** `adopt_config.js` together, or preview-before-adopt silently stops covering the thing you are changing.
- **A metric reporting no change is not a metric reporting no improvement.** `design.panelScale` moved every text size and every gap in the Services panel on all eight towns and `quality_metrics.js --all` stayed at 271, because every measure it has is about the **map**. Before chasing a flat number, check the tool measures the thing you changed. (`minTextMm` is the one panel-adjacent measure, and on `panelCols` towns it reports the auto-fitted **badge** text, not the body copy — so the 2.3 → 2.45 mm lift that cleared the print floor did not show there either.)
- **On a town that has been through the complexity triage, read its config before calling anything a defect** — see [complexity-triage.md](complexity-triage.md). The design-quality diagnosis called High Wycombe's shared corridor navy "an exhausted colour-blind-safe palette" and its labelled town-centre box "an empty rounded rectangle that reads as a placeholder"; both are rungs of the ladder, working as approved. `complexity.json` `applied` shows what a town has already had done to it.

- **`rollout_places.js --apply` crashed every place with `ENOENT: roads_geo.json`, even though `--` dry-run (the same script) worked fine (2026-08-10).** Root cause: `roads_geo.json`/`routes_paths.json` are S4-*generated* files (written by `build_internal_place_roads.js`'s one-time OSM pull), never registered as S2 outputs — so `stage.js pull S2` genuinely has nothing to bring them in with. The scratch/dry-run path knew this and separately copied forward every extra `*.json` from the previous S4 run; the real `--apply` path never got the equivalent step, so it silently relied on `stage.js pull S2` alone and crashed the moment it needed a file that pull never had. **A dry run passing is not proof the real apply path does the same work** — when a tool has two code paths for "preview" vs "commit," diff what each one actually copies, don't assume parity from a green dry run.
- **A footer/shared-helper file (`footer.js`) can drift on the portal side even when `status.js` says every row is "in sync" (2026-08-10).** `gen_internal.js` resolves `footer.js` via `SKILL_ASSETS` at runtime exactly like `icons.js` — but `footer.js` had never been added to the portal vendoring-drift table (`changing-the-engine.md` §4 / `status.js`'s `portalDrift()`), so a footer-only change (dropping the printed engine version) silently left the portal's copy stale. It didn't show up as a "DRIFTED" row — it just wasn't checked at all. It surfaced instead as a **2-byte SVG mismatch** in `npm run verify:area` (`Map v6.23 · …` from the old footer vs `Valid from …` from the new one) — small enough to look like noise, but `verify:area` doesn't pass on noise. **Any file `gen_internal.js` resolves via `SKILL_ASSETS` needs a row in the portal vendoring table, not just the files that "feel like" the engine** (`icons.js`, `render.js`, the generators themselves) — added, so this can't recur silently.
- **A delivered map's pre-flight verify can fail on the live host even though it passes locally, and that's the live host telling you the truth, not a flake (2026-08-10).** `npm run deliver` re-verifies against the host's *own* running container before touching anything — if the host is running older code than what produced your local fixture (because `git push` never auto-deploys there — see [[project_bus_portal_go_live]]), the pre-flight step correctly fails with the exact same "old footer text" mismatch `verify:area` would show locally on stale code. The fix is to deploy (`git pull && docker compose build portal && docker compose up -d portal` on the host — `docs/DEPLOY.md` §9), not to touch the map data. The script did exactly what it's for: it left the live service completely untouched and told you why.

- **Converting a busway town's `external[]` to radial: drop the leading town-name stop.** The busway `leftBranch()` loop draws `stops[1..n-1]` (skips index 0), so busway-shaped data conventionally starts each arm's `stops` with the town's own name as a throwaway index-0 placeholder. `gen_external_radial.js`'s spoke loop draws **all** of `stops[0..n-1]` as ticks — it has no such skip — so copying busway `stops` arrays straight into a radial config draws a spurious "St Ives" (etc.) tick right next to the hub on every spoke, badly cluttering a multi-spoke town. Strip the leading town-name entry first (confirmed against March's radial config, which never carries one). **Real-corridor spokes will still look crowded even after that fix** if several services share a real corridor before diverging (St Ives's A/B/5A busway trio all run Fenstanton→Swavesey→Longstanton→Oakington→Histon before splitting to three different termini) — that's the same congestion the busway generator's shared "spine" was built to absorb, so expect to spread those spokes' bearings **20–30°+** apart (not the true ~10° geographic gap) and set `side` on each to steer their intermediate labels onto different sides of the line, same remedy as any crowded radial town.
- **A radial `bearing` near 270–300° can swing the hub-ward end of a long town name straight into the top-left operators legend / arm-note text**, because at those bearings `dx,dy` both go negative (up-and-left) and the terminus townNode has no collision awareness of the legend block. Nudge the bearing back toward due-west (~275–285°) rather than pushing it further round; the legend's own position (`legendAt`) is the other lever if a town genuinely needs a spoke through that quadrant.
- **2026-08-06: `gen_external_radial.js` gained four always-on fixes** (badge clearance, legend backing box, note word-wrap, version placement), applied by rolling out to all 8 built towns after an external-map audit found every non-High-Wycombe town had at least one of these problems (High Wycombe alone had hand-tuned `badgeOffset`/`legendAt.box`, which is what made it look "right" while every other town relied on defaults that don't hold up once a town gets busy). Specifics:
  - **Badge clearance is now automatic.** `_boff` (mm back from the terminus for the first badge) is `Math.max(badgeOffset ?? 8, measureNodeWidth(label,timeLabel)/2 + 4.5)` — it always clears the terminus lozenge's own computed half-width, so a wide destination name (e.g. "Hemel Hempstead") no longer needs a per-town `badgeOffset` override to stop the green box painting over the badge (badges are drawn before the box, so anything shorter than the box's half-width got covered).
  - **The operators-legend backing panel is now always drawn**, auto-sized to its content (badges + operator names + the wrapped note), not opt-in via `legendAt.box` any more. An explicit `legendAt.box` still wins (hand-tuning escape hatch) and also **caps the note's wrap width to the box's own interior** — needed because without a cap the note-wrap step (below) would size lines past a hand-tuned box.
  - **The "runs as two arms" note now word-wraps** (`wrapText()`, no 2-line cap unlike the label `wrap()` helper) instead of one unbounded `<text>`. Wrap width defaults to `max(contentWidth, 100mm)` — a **wide-but-short** wrap is preferred over narrow-but-tall, because the auto panel's HEIGHT is what risks reaching down into a nearby terminus lozenge (St Ives: a 60mm floor wrapped its 3-multi-arm note to 6 lines, reaching low enough to cover the Hinchingbrooke box; 100mm wraps the same note to 4). Don't push the floor much past ~100mm either — St Ives' note at a 110mm floor wrapped to 3 lines but widened the panel enough to newly collide with the Ramsey-via-Old-Hurst lozenge on the *right*; there's a real width/height trade-off here, not a knob to crank freely.
  - **The version stamp moved from mid-right-edge (rotated -90°) to the bottom-right corner (horizontal, unrotated)** — the old spot is exactly where a busy town's easterly/southerly spokes terminate.
  - **Auto-sizing isn't a substitute for the "spread bearings, only then place the legend box" sequence in s3-config.md.** Two towns still needed a manual nudge after the auto-box shipped: **Beaconsfield** (`legendAt:{x:10,y:155}` — the default top-left position put the panel right under the High-Wycombe-bound spoke's terminus, which the box then painted over) and **Wisbech** (`legendWrap:{perRow:2}` + explicit `legendAt.box:{w:80,h:75}` — 6 operators including two long names made the auto width alone reach ~107mm, which was wide enough to fully swallow the "Sutton St James" lozenge in the top-left quadrant; legendWrap folded the 4-badge Stagecoach-East-Midlands row onto 2 rows to bring the content width down, and the explicit box then also capped the note's wrap width per the point above). **Always check the panel's actual rendered `<rect>` (x/y/width/height) against every nearby terminus's box, not just eyeball it** — hand math on `measureNodeWidth`/`measureText` is a rough (deliberately generous) estimate and was off by enough in both cases above to miss a real overlap.
  - **`rollout.js`'s real-apply path had a bug that silently dropped `internal-schematic.svg`/`internal-diagram.svg`**: it ran `schematize_internal.js`/`diagram_internal.js` straight out of the new S4 dir without ever copying them in first (unlike its own dry-run scratch path, which does copy them), so the spawn failed with ENOENT, `r.ok` was never checked, and the output just vanished from the commit with no error surfaced. Caught when High Wycombe, Beaconsfield, St Neots and St Ives all lost their diagram (St Ives also its schematic) across a `rollout.js --all --apply`. Fixed by copying the file before `runNode` and checking `r.ok`, same as the dry-run path already did. **If a rollout ever reports DONE for a town with `internalSchematic`/`internalDiagram` configured, re-check `status.js`'s Schematic/Diagram columns afterward** — a silent-fail here won't show up any other way.
- **2026-08-06 (later same day): the legend backing panel could drift away from its own content when `legendWrap` was in play — root cause, not just a stale box.** User feedback: Wisbech and High Wycombe's box outline didn't line up with the "Operators & services" text; Ramsey's box overwrote the Peterborough terminus. Two separate bugs, both in `gen_external_radial.js`'s legend block:
  - **The real bug (Wisbech/High Wycombe): `ly` gets reassigned after the `legendWrap` loop** (`ly = yy - 6.6*OPS.length`, to keep the arm-note's default y-offset sane), but the backing `<rect>`'s own y-position (`ly-10`) was computed from this *same, already-mutated* `ly` — so once a town used `legendWrap`, the panel's top no longer matched where the header (`Operators & services`, drawn earlier from the *original* `ly`) actually sat. The panel would land too low, floating away from its header and clipping its own last operator row / note. **Fixed by capturing `const legendTopY = ly` before any mutation and using that for the rect's y (and for the auto-height calc)** — a town that never uses `legendWrap` is unaffected (ly is never mutated there), so this is byte-identical for every non-wrapped town.
  - **The masking symptom (both towns): a frozen `legendAt.box:{w,h}` doesn't get revisited when routes are added.** Wisbech's `box:{w:80,h:75}` and High Wycombe's `box:{w:92,h:76}` were both hand-set against an earlier, shorter operator list; by 2026-08-06 the actual content needed more room in both cases, so the frozen height clipped content regardless of the `ly` bug. **`gen_external_radial.js` now lets `box.w`/`box.h` be set independently** — the missing dimension auto-sizes — so a width hand-tuned to dodge a spoke label (High Wycombe keeps `box:{w:92}` to stop route 40's "Stokenchurch" being swallowed) no longer freezes the height too.
  - **Fixing the panel's height then exposed a second, pre-existing overlap at High Wycombe**: with the panel correctly sized (taller than its old, too-short frozen height), its bottom edge reached the "Oxford" terminus lozenge (bearing 272°, nearly due-west, landing almost level with the hub). Old fixed-y-position bug had *fully* swallowed that lozenge in translucent white (bad, but at least not a half-cut edge); correctly-positioned auto-height clipped just its top ~6mm (worse-looking, a hard line through the box). Fixed by nudging the spoke's `bearing` 272°→267° to drop the terminus ~11mm further south, clear of the panel. **Any time a legend panel's size or position changes, re-check bearings near 260–290° (the same quadrant as the panel) for newly-exposed or newly-created overlaps — don't assume the panel is the only thing that moved.**
  - **Ramsey: a single wide operator row, not a box or `ly` bug.** One operator (`Dews Coaches`) with 8 routes and no `legendWrap` drew all 8 badges on one row, auto-sizing the panel to ~90mm wide — wide enough to reach under the Peterborough terminus (bearing 326°, landing top-right of the panel). Fixed with `legendWrap:{perRow:4}`, same remedy as Wisbech's original Stagecoach-East-Midlands row, just not yet applied since Ramsey is a Tier-2 unattended draft (`draft_town.py`) that had never had its external layout hand-checked — see `DRAFT-REVIEW.md` in Ramsey's town folder.
  - **Takeaway for any future legend collision**: check the rendered `<rect>` in the SVG (x/y/width/height) against (a) its own content's actual draw coordinates, not just against nearby termini — a panel that's internally misaligned will pass an eyeball check against its neighbours while still clipping its own text.
- **Wisbech's Manea/March/March/Chatteris spokes (bearings 183/196/207/214) landed within a 31° arc, all forced onto the RECT's bottom edge, with gaps far below the boxes' combined half-widths** — the classic crowded-radial symptom from `s3-config.md`'s "spread bearings by hand" sequence, just not yet applied to that cluster. Re-spread (175/197/215/228 — see Wisbech's 2026-08-06 S3 commit) using the rect-projection math in `gen_external_radial.js`'s `rayToRect()`: compute each candidate bearing's terminus `(tx,ty)`, then require adjacent gaps ≥ sum of half box-widths (`measureNodeWidth(label)/2`) + ~2mm margin. Don't eyeball spacing in degrees — the same angular gap compresses hugely near a rect corner vs. the middle of an edge.
- **bustimes locality page is padded** with expired and non-serving routes — verify every one (this is the whole point of S1, and feeds the disagreement audit). It can also **omit** services that do serve the town — so a route absent from the locality list isn't proof it doesn't run; cross-check the operator.
- **Operator PDFs via `WebFetch` → save → Read** (see S1 step 3): `WebFetch` can't parse a PDF but saves it and prints the path; Read that path (no `pages` param here). Read every page — don't trust the front-page day-type heading.
- **`gen_disagreements.py` and `gen_verification.py` need `python-docx`** (`pip install python-docx`; installed on this box). Pure python-docx, nothing that needs Word.
- **`gen_disagreements.py`'s PDF conversion (`convert_to_pdf()`, 2026-08-03) calls `soffice` directly, not through any office-skill wrapper** — that wrapper is known to fail on Windows. It's best-effort: missing `soffice` or a failed conversion just skips the PDF and the docx is still written, so a run never hard-fails over it. The portal's `import-map.mjs` auto-detects the PDF from `<TownDir>/_latest/disagreements.pdf`, walking up from `--src` — **run `refresh_latest.js` before importing**, or the importer will warn "no disagreements.pdf found" and the map ships without a customer-facing report even though one exists in an S1 folder.
- **`icons.js` require** — the current template **self-resolves** it (`__dirname/icons.js` when run from `%SK%`, else the skill path / `$SKILL_ASSETS`), so a verbatim copy works in any run dir with no edit. (Old town copies that hardcoded `__dirname+'/icons.js'` break once copied out of `assets/` — rewrite to the absolute skill path, or just re-copy the current self-resolving template.)
- **sharp + `density`** mis-scales `mm` SVGs; instead set the SVG's `width/height` to the pixel target (3508×2480) and render natively — `render.js` already assumes this.
- **Label overlap (internal):** the de-collision placer drops a label rather than overlap; if important places lose labels, nudge the icon, shorten the name (the name-tidy block), or raise the candidate-position count. Tight retail clusters may need a manual offset.
- **Road-name label silently missing under `internalRoads`, even though it's in the "chosen" set** (2026-07-03) → road labels use a multi-candidate search (whole-road weighted centroid first, then points spread along the road's used length) and only give up if **every** candidate collides or falls off-frame — set `DBG_LABELS=1` to print each road's outcome (`placed` / `SKIP(overlap)` / `SKIP(off-frame)`). A genuinely busy road (e.g. St Ives Constable Road, boxed in by a POI + badges) can still lose after all candidates; that's expected, not a bug — same "give up rather than overlap" philosophy as `placeLabel()`.
- **A rotated road-name label visually overlaps a badge/icon even though `DBG_LABELS` says it "placed" cleanly** (found + fixed 2026-07-04, item 5 work) → the road-label collision box (`gen_internal.js`, road-label block, `if(IR&&SKEL)`) used to be **axis-aligned regardless of the label's own rotation angle** (`[cx-w/2-1,cy-2,cx+w/2+1,cy+2]`, ignoring `ang` entirely). A steeply-rotated name (e.g. -35°/77°) is drawn correctly rotated but its *reservation* was still the un-rotated box, so the visual glyph could swing well outside what the collision check thought was reserved — caught two live cases in shipped v6.1: "Hemingford Road" ran straight through the 5A badge, "Constable Road" ran through the doctors'-surgery icon. **Fixed:** the reservation is now the axis-aligned bounding box of the label's own rotated rectangle (rotate the 4 corners by `ang`, then min/max) — reduces to the exact old box when `ang=0`, so unrotated labels are unaffected; verified via the byte-identical gates (all three gate towns still PASS) and by DBG_LABELS before/after (the two previously-mis-placed roads now correctly `SKIP(overlap)`; Constable Road was already a "no space" case per the note above, just previously overlapping silently rather than honestly dropping). Re-check `DBG_LABELS` output any time you touch this block — a `placed` result only means "the (correct) box didn't overlap," not "eyeballed clean," so always spot-check a rendered crop for any road at a steep angle. To force a named road to draw at full **skeleton weight** even when no bus route uses it today (e.g. a real corridor continuation whose nearest-junction stretch is currently unused, so it fades to the 0.45 mm context line and breaks visual continuity), add it to `internalRoads.keyRoads` — it also folds into the label-eligible set for free (no need to *also* list it in `roadLabelInclude`). Diagnose with a `roads_geo.json` closest-point scan between two road names to confirm they actually share a node before assuming a "gap" is real.
- **"to X" continuation arrow lands at an INTERIOR junction instead of the frame edge** (internalRoads) → the tail is *truncated*, not mis-rendered. `pull_roads.js` sizes its OSM bbox from the **drawn** stops (`routes_intown_atco.json`) + a flat margin, so a route whose next real stop sits beyond that box has no road geometry past its last in-box stop; `match_routes.js` stops there and `gen_internal.js`'s continuation fallback (correctly) draws the arrow at the last point it has. The arrow/frameCut code is fine — the fix is upstream: add `match_cfg.json` `reachExtend:{ "<route>":{ "start":N, "end":N } }`, which makes `pull_roads.js` union a few more of that route's real full-chain stops into the pull bbox so the matcher reaches out to where the fisheye+frame actually cut the tail (St Ives item-3, 2026-07-04). **The projection is unaffected** — under internalRoads the fit set is town-core stops only (locality prefix + `fitExtra`), so extending the pull only lengthens tails; the town core, frame, scale and stop positions don't move. `start`/`end` = which chain end continues (match `contStart`/`contEnd`); use `DBG_TRIM=1` to read each cut point and check it lands on the MX0/MX1/MY0/MY1 boundary (St Ives used `N:2` on all five affected ends — the fisheye pushes the first out-of-town stop way past the frame, so 1–2 stops is plenty; don't over-extend or you balloon `roads_geo.json`). **Traps:** (1) `reachExtend` candidates are filtered through the SAME `viaPrefixes`/`viaExclude` as the matcher, so an excluded variant-loop is never reintroduced — BUT if a route's onward main journey needs a prefix the current `viaPrefixes` excludes (St Ives 9's Hemingford→Godmanchester continuation is the `0500HHEMA` prefix, previously dropped along with the Hilton/Elsworth village loop), you must **add that main-journey prefix to `viaPrefixes`** or the extension is filtered out and nothing changes. Verify the extended route traces the intended corridor (dump its edge road-names) — the village loop must stay absent. (2) Extending the bbox changes `match_routes.js`'s `k0` projection constant slightly, so **every** route's `stopT` bytes change — this is sub-pixel and moves no tick visibly (a couple of loop-stop ticks that project onto a re-traced line can shift a few mm along it — sensible, still on the line). (3) A bigger, denser road graph can only make Dijkstra paths **shorter or equal**; if an unaffected route's *drawn* line shifts ~one `gap`, that's **corridor bundling** re-offsetting because the newly-reached route now co-runs a corridor (its matched `pts` in `routes_paths.json` are unchanged — check those, not the rendered path). `roads_geo.json` grew 465→821 KB (1.76×) for St Ives; report before/after size + pull time.
- **Measuring how much a bundled corridor family really co-runs: the two obvious measures are both WRONG** (found while building rung 1, 2026-07-28) → `internalCorridors` needs a health check ("do these services actually run together, or have I just painted two different routes the same colour?"), and the first two attempts each *confirmed a deliberately absurd bundle* before the third one caught it. (1) **The lane-bundling test** (`corridor.dist`, 2.4 mm ≈ 65 m at map scale) is the right tolerance for "should these share a grey casing" and far too generous for "would a reader see one line" — in a dense core it makes nearly every route a neighbour of every other. (2) **Page-space coincidence of the DRAWN lines is circular**, and this is the subtle one: bundling is *precisely* the operation that removes the lane offset between members, so measuring after it inflates every family towards 1.0. Two unrelated High Wycombe routes (`1`, `33`) read **0.70 shared at a 0.3 mm tolerance** — the coincidence was real, it was just an artefact of the thing being measured. **Use the same measure the gate proposes families with:** mutual ~111 m-cell overlap of the raw matched lat/lon paths (`complexity_score.js --overlap`, default 0.6). It is independent of the fisheye, the page scale and the bundling decision, and it scored `1/33` at 0.17/0.13 and `1/1A/1B` at 1.00. General lesson: **never health-check a transformation with a metric the transformation itself moves**, and sanity-check any new metric against a case you know is bad before trusting one you hope is good.
- **`coreBox` comes out far bigger than its radius suggests — because the fisheye magnifies the core** (2026-07-28, P3) → the box's page rectangle is derived by projecting a real geographic circle of `radius` metres and taking its bounding box, which is exactly right (it stays true under any fisheye or `lenses[]`, with no scale assumption) — and that is *why* it looks large: `internalRoads.focus` deliberately magnifies the centre, so 600 m of real ground occupies a lot of page. **`coreBox` and `focus` are two answers to the same problem**; a town wanting the box should usually cut `focus` right back or drop the extra `lenses[]` over the centre. Check the S5 JPG, don't reason about it.
- **A `features[]` label can print INSIDE the `coreBox`** (2026-07-28) → feature labels are hand-positioned via `labelPos` and have no collision logic of their own, so a river label sited on the town centre lands on the box ("River Wye" did, on the High Wycombe proof). The generator now **drops such a label and warns on stderr** rather than printing it. The fix is to move `labelPos` (or `overrides.json` `internal.features[key].label.pos`) — not to hide the feature, whose line is legitimately covered by the box.
- **`corridorPalette` does not reduce a town's colour count — and expecting it to is the whole misunderstanding** (2026-07-28) → High Wycombe v1.0 already drew only 12 distinct hues; the defect was that they were spread over 31 routes **at random**, so two unrelated lines read as one corridor. Rung 3 makes the sharing *structured*: you re-assign `palette` so each corridor gets one hue and no two corridors share one. The generator's payoff is the **clash check** — it names every hue still used by unrelated groups (nine of them on the first High Wycombe pass), which is the actual defect. Related: with `corridorPalette` set, `complexity_score.js` reports **R as distinct colour groups** and adds `linesDrawn` — R exists to police the ~12-hue palette, not to count ink, and K5/D5 still measure every line.
- **A same-coloured line that loses all its badges is unidentifiable, and the normal badge pass will happily let that happen** (2026-07-28) → badge placement drops a badge rather than overlap something, which is right while colour still says which route a line is, and fatal once `corridorPalette` (or a diverged `internalCorridors` branch) means it doesn't. So for those lines only, the generator now places one badge **regardless of collision**. If you add a future remedy that makes colours ambiguous, add it to that guarantee too — a crowded sheet beats an unreadable one.
- **A "family" whose members share the same streets *inside the town* is a good bundle even when their full routes differ** → High Wycombe's `1/1A/1B`, `32/32A` and `102/103/104/105` all score 1.00 on in-town geometry; the variation is entirely outside the frame, which the internal map never draws. Conversely a pair that looks like a family by number can fail: the plan's hand-picked `31 / 41` measured 0.40 / 0.46 and the generator warned on it. Trust the measurement over the numbering.
- **Bundling a family internally without merging it externally breaks the internal/external colour correspondence** → `internalCorridors` gives every member the lead's colour on the internal sheet only. If the same family also shares a spoke via `external[].routes`, **use the same lead in both** and the two sheets agree; otherwise that family is one colour on one sheet and several on the other. Decide per family, don't discover it at print.
- **PCA rotation** can flip north left/right; that's fine for legibility (add a small north arrow if asked). It keys off the in-town stop spread.
- **Overpass 406** → missing User-Agent header. `curl` sets one automatically; **Node `fetch` and bare requests do not** → pass `headers:{'User-Agent':'make-bus-leaflet/1.0','Accept':'application/json'}` (every mirror — overpass-api.de / kumi / lz4 — 406s without it). Observed here: the `bbox` MCP exposed only `list_osm_tags` + `generate_share_url` (**no `search_overpass`**), so raw Overpass-with-UA stays the working geometry path. **bustimes 404** on a guessed locality slug → use the search page to find the real slug.
- **Editor renders blank / `Cannot find module icons.js`** → the editor runs the S4/S3 generator copy; its `icons.js` require must be the absolute skill path (the template's `__dirname` form only works when run from the skill's own `assets/`). **Editor port 5179** in use → stop the old `node edit-server.js`. Start template `assets/overrides.example.json` for the schema.
- **Editor straighten distorts OTHER routes, or the Active-route / colour / per-route options are missing** → the town's S3/S4 generator copy **predates the per-route editor** (no `routeStops`/`OVERRIDES_FILE`). A town stays on whatever generator it was built against. **Fix:** re-sync that town's `gen_internal.js`/`gen_external.js` from the *current* template (re-apply only that town's small customizations) and run the byte-identical gate before trusting it. Quick gate: copy the town's S4 `*.json` + the candidate generator + `icons.js` into a temp dir, `node gen_*.js`, `diff` the output SVG against the committed one — must be **identical** with no `overrides.json`. (Reusable harness: **`%SK%\gate.sh <gen> <S4dir> internal|external <committed.svg>`** — it lives in the skill's `assets/` now, not the old `C:\Claude\bus-editor-work\` scratch folder. Full procedure, current gate set, and the portal re-vendor step: [changing-the-engine.md](changing-the-engine.md).)
- **The map prints the WRONG version number** (folders say v1.1, the sheet says v1.0) → the printed stamp comes from `routes.json` `"version"`, not the `v<N.N>_<ts>` folder name, and branching a build from an older `routes.json` used to carry the old value across. **Fixed at source 2026-07-25:** `stage.js pull` now rewrites the field to match the run dir and `stage.js commit S4|S5` refuses a mismatch (`--force-version` to override; `stage.js stampver <runDir>` to repair by hand). The guard fires at *commit*, by which point the SVGs are drawn — so a mismatch means **re-run the generators**, not just edit the field. Don't reach for the `LEAFLET_VERSION` env var instead: it only reaches `gen_internal.js`, so the external sheet keeps the old number and the two disagree (this is what happened to the place build `St Neots Tesco Extra v1.1`).
- **A bad saved `overrides.json` (e.g. a distorted straighten)** → open the editor and press **Clear all** (or just delete `overrides.json` and regenerate). The editor no longer auto-saves, so new mistakes only persist if you press Save; Ctrl+Z undoes within a session.
- **Overrides not taking effect** → confirm `overrides.json` sits in the generator's working dir, the top-level key is `internal`/`external`, and ATCOs/keys match (stops by ATCO, POIs by `cat:name`, branches by route). Absent/`{}` is *expected* to change nothing.
- **Linear features draw nothing / wrong place** → a feature's `key` in `routes.json` `features[]` must match a key in S2's `features_geo.json`; a missing/empty geometry array draws an empty (invisible) feature rather than erroring. Recolour a feature that clashes with a route via `features[].style.stroke` or `overrides.json internal.features[key].style`. Omitting `features[]` entirely is valid (legacy single-river fallback) — don't add an empty `features:[]`, leave the key out.
- **bustimes per-direction parsing (`pull_routes_full.js`):** direction headers are `<h2>A - B</h2>` **or** `<h2>A to B</h2>` (both separators occur, even within one town). EXCLUDE the operator `<h2 itemprop="name">` and the "Possibly similar services" `<h2>`, and **stop collecting `/stops/` links at the first such non-direction heading** — otherwise links to *other* services leak into the last direction (B once parsed 107 "stops" instead of 54/56 from this). A merged direction list interleaves all journey patterns of that direction (a two-arm route shows both arms concatenated) — that's expected; it's the full record.
- **A route's in-town stop count looks wildly too high** → you merged a minor sub-service's town loop. e.g. St Ives 301S (Saturday) does a 31-stop town circuit while base 301/301X/301V hit 2 stops; merging all four made "301" appear to circle the whole town. `derive_intown.js` draws the **canonical** (first-slug) pattern only, so it shows 301's real 2–3 in-town stops; the loop still lives in `routes_full_atco.json`. List the canonical/primary slug **first** in `route_slugs.json`.
- **Long "tentacle" spokes on the internal map** → a buffer (out-of-town) stop is kilometres away because that route has no intermediate stop near town (express 302→Pidley 6.8 km, VL14→Huntingdon 7.8 km, 69→Papworth 8.7 km). Set `intown_cfg.json` `maxEdgeKm` (with `anchor`) to drop buffer stops beyond the real town edge (~3.5 km), and/or tune `routes.json` `internalZoom` to compress what remains. Without `internalZoom`, far edge stops blow the map fit out and shrink the town — St Ives went compact→needs `{corePct:0.9, comp:0.18}` once edge buffers were added.
- **`backfill_coords.js` curl note:** `--data-urlencode` is normally fine here, but `backfill_coords.js` POSTs Overpass with a `Content-Type` header from Node `fetch` (UA required, else 406). The bbox must cover **every** terminus, not just the town — under-sizing it leaves out-of-town stops uncovered (they then fall to the slower bustimes per-stop fallback, or go missing and get silently dropped by the generator's `atco2ll` filter).
- **Generator drops a stop silently** → it only draws ATCO with a coord in `atco2ll`; a stop missing from `atco2ll` vanishes with no error. After S2, confirm `backfill_coords.js` reports 100 % coverage of `_all_atco.json`.
- Keep fonts ≥ the reference; if a panel is cramped, drop content, don't shrink below ~2.5 mm.
- **S6 `verify_report.js` exits non-zero / prints "BLOCKED"** → that is **by design** on a HARD finding (it's the build gate), not a script crash. Read `verification.json`, fix the upstream stage, re-verify. A clean run exits 0 / "PASS".
- **Red-team JSON won't parse, or spurious day/terminus SOFTs** → the agent often emits HTML entities (`&amp;`, `P&amp;R`, `Mon &amp; Fri`); `verify_report.js` unescapes the common ones on load, but if the agent wraps the JSON in prose or emits other entities, strip to the bare `{...}` object before saving it as `redteam.json`. Spurious `days` diffs are almost always an un-unescaped `&amp;`.
- **Borderline direction SOFTs (55–90°) are expected, not bugs** → a route that leaves town on a different bearing than the straight line to its terminus throws a soft `direction` flag (St Ives 301 goes east via Needingworth/Morrisons then curves north to Ramsey; 5A heads southwest before swinging to Bar Hill). Only **>90°** (genuinely drawn the wrong way) is HARD. Likewise terminus SOFTs from locality coding (Cambridge/Trumpington stops are coded `CITY`) are naming, not errors.
- **S6 on a pre-S6 town** → `stage.js` lazily backfills the missing `S6` slot (and any other absent stage) in canonical order on first touch, so old manifests (March, early St Ives) just work; no migration step needed.
- **Gating the config-driven `gen_internal.js` against an OLD (pre-2026-06-07) S4 dir DIFFs** (route order, POI tidy, road-label rotation) — not a regression: that dir's `routes.json` predates the config keys (`routeOrder`/`panelOrder`/`orientationRoute`/`internalDesc`/`poi`), so the template falls back to defaults. Gate against an **augmented** `routes.json` (the town's re-homed S4, e.g. St Ives v4.1 / March v1.2, or a prep dir with the keys added). An existing town only builds byte-identical on the generic template once its `routes.json` carries those keys — that's what "re-homing" adds.
- **A new town's external hub / title shows the WRONG town** (e.g. "March" on a Huntingdon map) → a generator still has a **town literal**. The generators are meant to be fully config-driven; the radial/busway hub label was the last holdout (fixed 2026-06-07 to use `routes.json` `town`). If you find another literal, replace it with a `routes.json` key and re-gate St Ives + March byte-identical — never leave a town name baked in. The **radial hub box auto-widens** for long names (`w=max(22, town.length*2.6+6)`); if a long town name overflows a fixed box the white text spills onto white background and the overhang vanishes.
- **Overpass POI query rejected / `overpass failed`** → the verbose per-key selector form (one `node[...]`/`way[...]` line per amenity) can be refused or time out. Use the **compact regex selector** form from `%SK%\overpass-pois.txt` (`["amenity"~"^(pharmacy|doctors|…)$"]`) in a single bbox, and retry across mirrors (`overpass-api.de` / `kumi` / `lz4`) with a UA header. Add `way["landuse"="industrial"]` if you want estates. POIs go in `osm.json`; `osm2.json` can be `{"elements":[]}` (the generator reads both).
- **`bootstrap_town.py` anchor lands on the wrong stop** → it prefers an in-town stop **named** like a bus station/interchange (with ≥½ the max route count); if a town's bus station isn't named that way in NaPTAN it falls back to the most-routes stop — fix `anchor`/`anchorLabel` in the draft by hand. The bootstrap `--radius-km` (default 1.6) may also pull in an adjacent locality's prefix (Huntingdon picked up `0500HGODM` Godmanchester) — keep only the town's own prefix as `atcoPrefix`.
- **Console `UnicodeEncodeError` (`≈`, `·`) on Windows** → the cp1252 console can't print the report's `≈`/`·`; the helper files are written UTF-8 *before* the print, so the output is fine — `bootstrap_town.py`/`scaffold_town.py` now `sys.stdout.reconfigure(encoding="utf-8")` to avoid it.
- **A shared road looks "fat" — bare grey casing shows beside the colour, so a route reads as running *alongside* the road rather than on it** (internalRoads) → the grey road casing width is `(cnt-1)*gap + stroke + skeletonPad` (`gen_internal.js` ~L496) where `cnt` is that segment's **geometric-bundle membership** — i.e. *every* route that shares the corridor, even where only some of those colour lines are actually **drawn in-frame** (the others have turned off onto another road or continue out past the frame). So a corridor used by N routes always draws an N-lane casing, but where fewer than N colours occupy it the extra lanes are bare grey. Example: St Ives Somersham Road (B1040, used by 300+301+302) draws an 8.6 mm 3-lane casing its whole length, while much of the in-frame stretch shows only 1–2 colours; St Audrey's Lane (route 300 only) is a 3.0 mm one-lane casing where the green sits centred. Not a matching bug — the routes really are on that road — and it also drives the interchange knot (casing hits 17–22 mm where 6–8 routes converge on the bus station). **DONE (St Ives item 4c, shipped v6.1 2026-07-04):** the casing loop now runs *after* the TRIM pass and sizes each segment by the routes whose **trimmed in-frame line actually spans it** — of the bundle `MEM[r][i]`, keep only routes whose `TRIM[s].draw` range covers the segment midpoint (within `corridor.dist`), take the span from the lowest to the highest **drawn** lane offset, and **centre the casing on the drawn lanes' midpoint** (offset perpendicular by `mid` along `refDir(bundle[0])`'s normal). Centring matters: a corridor showing only its outer lane would otherwise overrun a centreline-anchored casing (or leave bare grey on the empty side). When every bundle route is drawn, `mid=0` and `span=(nb-1)*gap`, i.e. the old formula exactly, so all-drawn corridors (incl. the interchange knot itself, still ~22 mm — that's honest, all colours fill it) stay byte-identical; only under-occupied corridors slim (St Ives Somersham/Needingworth 5.8→3.0 mm). `TRIM[r].draw={s0,e}` was added for this. `DBG_CASE=2` prints per-segment `name bundle drawn w mid`; `DBG_CASE=1` prints the max casing width. All inside `if(IR)` — gated towns unaffected. (4c did NOT declutter the interchange itself — the converging edge is genuinely all-lanes; that needs 4a's fisheye retune below.)
- **Two "to X" terminus labels sit right on top of each other / jitter apart untidily** (internalRoads, e.g. two routes leaving via the same corridor a few mm apart on the frame edge) → **DONE (St Ives item 5, shipped v6.2 2026-07-04):** the terminus-arrow block (`gen_internal.js`, `if(IR&&TRIM)`) now **single-link-clusters** every non-suppressed frame-cut point within `internalRoads.terminiClusterDist` mm (default 7, the old jitter threshold) and draws **one arrow + one badge/label box per cluster** instead of one per route: same-destination members (e.g. St Ives A+B both "to Cambridge") share one row and ONE "to X" text side-by-side; different destinations at the same exit stack as separate rows. A cluster of one route reduces to the exact old per-route arrow+badge+label (same maths, same draw order) — byte-identical for any route whose cut has no neighbour in range, and the 3 gate towns (no `internalRoads`) still PASS untouched. **Badge pitch matters:** badges are drawn at 3.0 mm radius (6 mm diameter) — the side-by-side/stacked pitch constants (`BS`/`RH`) must clear that or adjacent badges overlap and the later one hides the earlier one (caught this the first time through: `BS=3.3` let a 2nd badge vanish under the 1st; fixed to `BS=RH=6.6/7.0`). Always visually crop-check a multi-member cluster, not just diff the SVG — an overlap like this doesn't show up as a missing element, just a hidden one.
- **The bus-station interchange is too crowded / lines pile onto the anchor** (internalRoads) → the `internalRoads.focus` fisheye keeps everything within `coreKm` at true scale and compresses beyond by `comp`; the map then **fits-to-frame**, so to give the interchange more page space you must shrink the *compressed extent* → **lower `comp`** (compress the outer town harder), which raises the fit scale and magnifies the true-scale core. Counter-intuitively, **raising `coreKm` makes the interchange SMALLER** (bigger extent → lower fit scale). St Ives went `comp 0.42→0.34` (item 4a, v6.1) for ~+8 % core spread. Trade-off: magnifying the core enlarges the whole map, so **peripheral POIs near the frame can push off-frame** (St Ives lost only *Copley Scout Park* at 0.34; 0.30 also dropped *Wheatfields Primary*) — stop ticks are unaffected (the fit set is town-core stops; check `r="0.8"` circle count is unchanged). It also **shifts the terminus-arrow frame-cut points** (they stay on true edges, but harder compression can flip which edge a route exits — St Ives 5A stayed on the right edge under single-stage `comp`, but a **three-zone** fisheye flipped it onto the bottom and re-bunched the 9/5A/69 cluster; prefer single-stage unless you specifically want to protect the mid-town). **Optional three-zone** for magnifying the core without pulling the mid-town: add `focus.midKm` + `focus.outerComp` (true scale < coreKm, moderate `comp` out to `midKm`, strong `outerComp` beyond). Absent ⇒ single-band, byte-identical. In practice single-stage `comp` gives more core magnification per unit of tail compression (it also compresses the mid band), so reach for three-zone only when mid-town spacing must be preserved. Tune with `DBG_TRIM=1` (cut points land on MX0/MX1/MY0/MY1?) and by counting `r="2.4"` POI icons before/after.
- **Same-road routes draw as ONE fat band / only the last colour shows / they re-converge at every stop** (internalRoads) → two separate bugs, fix in order. (a) **Too little daylight:** if lanes ARE offset but jammed, the reveal between adjacent lines is `gap-stroke` and is *constant regardless of route count*; `gap` only just above `stroke` (e.g. 1.9 vs 1.7 = 0.2mm) reads as one band at 300dpi — raise `internalRoads.gap` to `>= stroke + ~1mm` (St Ives uses 2.8/1.7). (b) **Lanes never offset at all** (one colour hides the rest, lines pinch together at stops): the map-matcher put same-road routes on **different OSM edge ids** (parallel ways / a split node / the stop vertex), so edge-id grouping saw each route 'alone' → offset 0 → all on the centreline. Fixed by **geometric corridor bundling** (`gen_internal.js`, `internalRoads.corridor{dist,angle}`): lanes are grouped by page-space proximity+parallelism, not edge id, so split-edge routes still offset apart and hold separation through stops, and the grey casing is bundle-sized. If distinct-but-close parallel streets fuse, lower `corridor.dist`; the raw gap retune (a) alone will NOT fix (b) — it only spreads lanes the bundler already grouped.
- **A multi-badge "to X" terminus row prints its label straight over a sibling badge** (internalRoads termini clusters) → fixed 2026-07-04 (St Ives v6.3): the cluster block drew a row's badges but never reserved them before placing the row label, so when the first (rightward) label candidate was rejected — e.g. at the right frame edge — the left `'end'` candidate landed on the other badge and `placeLabel` thought the spot was clear (shipped v6.2 had "to Cambridge" covering B's badge; the SVG text-diff looked fine, only a rendered crop showed it). Multi-badge rows now reserve their badges and use row-level candidates (right / left / above / below the whole row); **solo rows keep the legacy path unchanged** — verified the fix changes exactly one line of St Ives v6.2's internal.svg (that label) and the three gate towns stay byte-identical.
- **Octolinear schematic (`internalSchematic`) traps** — lessons from building it (St Ives item 6, v6.3; engine details in [schematic-engine.md](schematic-engine.md)): (1) **Leg-length targets must be ARC length, not chord** — chord targets shrink every straightened corridor and contract the whole town centre. (2) **The workspace must bundle far tighter than the geographic map** (`corridor:{dist:0.6}` vs 2.4): schematized shared legs are exactly coincident, while distinct streets end up much closer than reality — the geographic distance fuses separate streets and the casing width explodes into a grey blob. (3) **Twin corridors between one junction pair collapse the solver** (two octants demanded between the same two points squeeze it to zero length, 20–45° residuals): drop near-coincident twins (dual carriageways) onto the kept corridor, force a mid-bend on genuinely-separated block loops. (4) **Contract sub-2mm junction clusters first** — OSM roundabout/dual-carriageway micro-cycles cannot close octolinearly and keep visible off-angle residuals however hard the solver pushes. (5) **Do NOT octolinearise the river — keep it WINDING** (warp its vertices through the road displacement field only, no 45° snapping, no crossing-pins). v6.3 snapped + pinned it, which distorted it into crossing routes it never meets in reality (St Ives 300/A/B) and made it read as a road; v6.5 switched to warp-only winding (Peter: "the river can keep its geographic winding shape, but routes must not overlap it unless they do in reality"). A smooth river among straight routes is a recognised schematic style (London Underground's Thames). (6) The warped river MOVES, so give it a schematic-specific `labelPos`/`labelReserve` (`internalSchematic.features.river`), placed **on the water** — and a stale reserve box can also block a terminus label from placing at all (St Ives's B "to Huntingdon & Hinchingbrooke" vanished until the river label moved).
- **A linear-feature label (e.g. "River Great Ouse") sits a long way from the feature** (either internal map) → the label is a FIXED page position (`routes.json` `features[].labelPos` for the geographic map; `internalSchematic.features.river.labelPos` for the schematic), it does not track the drawn geometry. If the feature's drawn course doesn't pass through that point the label floats in empty space. St Ives's geographic label sat at x=40 while the river only reaches x=77–194 (37 mm clear of it) — a legacy value nobody had checked against the actual projected river until v6.5. Fix: project the feature (same planar→rotate→fisheye→fit as the map) and read off its in-frame page-mm span, then set `labelPos` onto an open stretch of the water (St Ives geo → {x:92,y:193}); the schematic river moves independently so set its label separately. `river_coords.js`-style probe (replicate the projection, dump in-frame river page pts) is the quick way to find a good spot. (7) **A route draws an "odd spur"/spike not on the geographic map** → junction contraction turned a route's through-node into a degree-1 dead-end stub (both its neighbours merged into one cluster), so the route folds in-and-out of it. Fixed 2026-07-04 (v6.4): contraction now runs to a **fixpoint** and also merges junctions joined by a short graph edge (`mergeEdge`), so a second pass absorbs the stub; `weldLeg` co-locates any residual sub-mm leg after the solve. Trace one with `DBG_NODE="<lat,lon>"` (dumps a node's incident legs) / `DBG_PAIR="<keyA>|<keyB>"` (degree + connectivity of two nodes). St Ives's route-300/Burstellars spike was a St Audrey's Lane **keyRoad** junction landing ~21 m off route 300 — keyRoads adding junctions near route paths is the usual trigger.
- **A road-name label floats where there is no such road / on a road you don't recognise** (internalRoads) → the label placer aggregates EVERY segment sharing a name into ONE label at their combined length-weighted centroid. A name common to several localities the routes pass through — classically **"High Street"** (St Ives has none, but 7 surrounding villages do: Somersham, Fenstanton, Bluntisham, Needingworth…) — averages to a centroid that can land spuriously inside the town, on a faint context line that isn't even a bus road. Confirm with a quick scan (`RG.ways` named "High Street" clustered by position; count any within ~1.2 km of the town centre — St Ives had 0). Fix: add the name to `internalRoads.roadLabelExclude` (drops the label only, no road line changes). Shipped St Ives v6.4. Same root cause could mislabel any generic name (Church Street, Station Road) in a multi-village pull — check a doubtful label's actual segments are in-frame before trusting it.
- **North arrow points the wrong way on the schematic** (internalRoads) → the arrow direction is the screen bearing of north under the map's rotation. The geographic map derives it from `theta`; the SCHEMATIC can't (its coords are pre-rotated and it runs at `rotationDeg:0`), so `schematize_internal.js` precomputes the angle and injects `internalRoads.northArrow.angle` into the workspace routes.json. A town only sets `internalRoads.northArrow:{x,y}`; if you add a north arrow to a NEW schematic town and it points up (screen-north) instead of true north, that injection isn't happening — check the schematizer wrote the `angle`. Verify direction by projecting anchor vs anchor+Δlat through the real transform and comparing to `atan2(-cos(-theta), sin(-theta))` (they matched exactly for St Ives, −156°).

- **Tube-map DIAGRAM (`internalDiagram`) traps** — lessons from building it (St Ives v6.6, 2026-07-10; engine details in [diagram-engine.md](diagram-engine.md)): (1) **Render extensions must key off the WORKSPACE-only `internalDiagramRender`, never the town's `internalDiagram{}`** — the town routes.json is read by the geographic AND schematic builds too, so keying gen_internal's `ID` off it leaked the curated-stops filter into both (internal.svg silently lost ~2 KB of stop ticks; caught only because the v6.6 S4 output size was compared against v6.5's). Whenever a pre-stage needs to pass render config through routes.json, use a key only the pre-stage writes. (2) **Pins must be solved in FRAME space, after the refit** — the diagram's solve→refit rescales/re-centres everything, so a pin applied in the first solve lands somewhere else on the page. The engine runs a second frame-space LSQ (same octants, refitted lengths, pins as strong springs) with no refit afterwards; a dropped pin lands within ~0.2 mm. (3) **IDW warp is NOT enough for off-network content when displacements are topology-sized** — the schematic's warp-only approach (fine for its small displacements) folded the river into loops and stranded town-centre POIs in emptied space. POIs: snap to the nearest route segment and keep the offset (rotated with the segment), warp only beyond `poiSnapDist`. River: pin it EXACTLY at its route crossings (segment intersections mapped through the corridor solve), similarity-transform each span between crossings, and **damp perpendicular excursions toward the span chord** (`featureDamp` 0.35) — undamped, one long winding span swung right across the town. Note this deliberately differs from the schematic's rule (5) above: same river, different regime. (4) **Don't "normalise the version stamp" with a blanket `sed s/6\.6/6.5/` when diffing generated SVGs** — coordinates contain `6.6` too (`96.63`→`96.53`), manufacturing phantom diffs; replace only the literal stamp text (`Map v6.6`).

- **Upgrading an EXISTING (pre-2026-06-07) town to `internalRoads` — `match_routes.js` crashes `Cannot read properties of undefined (reading '0')`** → the town's `routes_full_atco.json` is the **old flat-list shape** (`{route:[ATCO…]}`) rather than the full-data `{directions,canonical,all}` the road-matcher expects. Towns migrated from the pre-staging flat build (March was one — its `routes_full` was only ever a copy of `routes_atco`, never true both-direction chains) never had real full chains. Fix: rebuild S2's full data first — `route_slugs.json` from the locality page → `pull_routes_full.js` → `backfill_coords.js` → `derive_intown.js` (write an `intown_cfg.json` if the town lacks one) — *then* `pull_roads.js` + `match_routes.js`. Don't try to bolt the road graph onto stale flat data. (March→v2.0, 2026-07-12.)
- **A busway / limited route is SKIPPED by `match_routes.js` (`<2 in-bbox stops, skipped`) and vanishes from the internalRoads map** → the matcher traces a route's **canonical direction only**, and that direction can barely enter town. Huntingdon **T1** (Busway Tiger) canonical was *Huntingdon→Cambridge*, which touches just the rail station in town then makes a ~9 km **non-stop** busway hop to Fenstanton — 1 in-bbox stop, so no path. `reachExtend` can't help when the next stop is a long non-stop hop away (widening the bbox to Fenstanton would blow up the map). Fix: **swap that route's `canonical` in `routes_full_atco.json` to the town-serving direction** — T1's *Cambridge→Huntingdon* direction terminates at the bus station with 3 in-town stops (rail station → Millfield → bus station), which is exactly the in-town path you want drawn. Check both directions' in-bbox counts (`dir['stops']` filtered to `roads_geo.bbox`) and pick the fuller one. Such a stub route's terminus arrow may land interior (T1's short 0.8 km path ends at the bus station, not a frame edge) — set `internalRoads.termini.<r>.end:false` to drop a misleading arrow, or leave it and tidy by hand. (Huntingdon→v2.0, 2026-07-12.)
- **Windows Python mojibakes en-dashes in `routes.json` → the render shows `Monâ€"Sat` / `March â€" Ramsey`** → `open(path)` on Windows defaults to the **cp1252** locale encoding, so reading a UTF-8 `routes.json` (with real `–`/`—` bytes) and re-dumping corrupts them. Always `open(path, encoding='utf-8')` for both read and write, and `json.dump(..., ensure_ascii=False)`. (Mirrors the existing PowerShell `ConvertTo-Json`/`Out-File` BOM+mojibake gotcha — same class of bug, Python flavour.)

- **Two different routes share a number, or a route's key can't be the badge text** (Wisbech, 2026-07-13) → `palette`/`textOn`/the S2 data files are all keyed by the same string, so you **cannot have two `46` keys**. Key the second one distinctly (`46L` for the Lynx King's-Lynn 46 alongside the Stagecoach March `46`) and add **`routes.json` `badgeLabels`** `{"46L":"46"}` so both badges still read "46" in their own colours (operator disambiguates them in the panel/legend). Same key for lettered/branded services whose blind ≠ their data key: Wisbech's First **excel** is keyed `excel` (to match its S2 chain) with `badgeLabels:{"excel":"A"}` — the single-letter blind fits the small badge circle where "excel" would overflow. `badgeLabels` is honoured by all three generators incl. panel + operator-legend badges; absent ⇒ badge = key (gated byte-identical on the 3 existing towns). See [s3-config.md](s3-config.md).
- **First "excel" (lettered A/B/C/D) and some Lynx services return a SHORT full-chain from `pull_routes_full.js`** (Wisbech, 2026-07-13) → bustimes groups First's excel journey-letters under **one** service page titled "A, B, C, D - excel …" whose only `<h2>` is the operator name (no `Place A - Place B` direction headers), so the puller can't split directions and grabs just the stops it finds (Wisbech excel came back 15 stops, ~town-vicinity only; some Lynx pages similarly returned only the in-town loop, and 60 + 46L came back with **identical** in-town lists). This is fine for **drawing**: the internal map only needs the in-town stops (present) and the external map uses **hand-curated place names** from `routes.json` `external[]`, not these ATCO chains. But the stored `routes_full_atco.json` is incomplete for those routes, so **S6's terminus/direction check and any future full-chain use are unreliable for them** — note it, and set the internal `internalRoads.termini` + external `external[]` by hand for those routes rather than trusting the derived ends. GTFS (primary) still gives the correct operator/days/termini for the facts. If a complete chain is needed, read the operator PDF or the bustimes stop-list per direction manually.

- **A bustimes service page renders the timetable FOR ONE DATE (today by default), so pulling on the wrong day SILENTLY TRUNCATES the stop chain** (St Neots, 2026-07-19) → the first St Neots S2 ran on a **Sunday**; route **66** came back with its Fenstanton–Huntingdon journeys and **zero St Neots stops** (`derive_intown` then reported `66 intown 0`), and other Mon–Sat/Thu-only routes lost stops too. There is no error — you just get a short chain. `pull_routes_full.js` now takes **`--dates d1,d2,…`**, fetches each service page once per date and merges per direction (same path as multiple sub-services); `canonical` is merged across the dates of the first slug, and a date on which the service doesn't run yields a no-`<h2>` page whose junk `'(single)'` list is dropped whenever any real direction exists. **Pass a Thursday + a Saturday + a Sunday** for a general town (covers Mon–Fri, Mon–Sat, market-day/Thursday-only community services and Sunday-only workings); omitting `--dates` keeps the legacy today-only behaviour. Always sanity-check the per-route counts the puller prints and `derive_intown`'s `intown N` line — a `0` or a suspiciously small number is this bug, not a quiet town.
- **`pull_routes_full.js` only matched `0500…` ATCO codes — a BORDER town's chains were truncated to their own county** (St Neots, 2026-07-19) → `0500` is *Cambridgeshire*. St Neots sits on the Cambs/Beds border: 905 runs to Bedford (`020035…`), and 112/193 come from Biggleswade (`021026…`), so those chains stopped dead at the county line (112 returned 4 stops of 50). The stop regex is now `\d{4}[0-9A-Z]+` (every NaPTAN ATCO starts with a 4-digit area code). This also silently affected **Wisbech** (excel reaches Norfolk `2900…`) — its stored `routes_full_atco.json` is incomplete for that reason and would improve on a re-pull. Watch for it in any town within ~15 km of a county boundary.
- **A cross-border town is under-reported by a PREFIX-ONLY GTFS pull, and its built-up area spans several NaPTAN localities** (St Neots, 2026-07-19) → two compounding traps. (a) St Neots' urban area covers the localities *st-neots-cambs, eynesbury, eaton-ford, eaton-socon, loves-farm* (all `0500HSTNS`, plus 3 stops `0500HEYNE`) and *little-paxton* (`0500HLITN`); route **69** terminates on the Eynesbury-coded stops, so `gtfs_query.py 0500HSTNS` missed it entirely — always **also** run `--near "lat,lon,4"` and diff, and walk the locality page's *sub-locality* links, not just the main one. Put the contiguous extra-locality stops in `intown_cfg.extraCore` so they count as town core. (b) Four services that genuinely serve the town (**112, 193** Ivel Sprinter; **W9, W10** Wanderbus) are absent from the Cambridgeshire BODS dataset because their operators register in **Bedfordshire** — the same class of gap as community/DRT services, but caused by geography rather than service type. The bustimes pass is what catches them; record them `"source":"bustimes-community"` so a refresh re-checks them there instead of expecting them in the dataset.
- **A route with only ONE in-town stop is skipped by `match_routes.js` and draws nothing** (St Neots 69, 2026-07-19) → 69 calls at Eynesbury Tesco and then runs ~10 km non-stop to Papworth, so its in-town subset is a single stop (`<2 in-bbox stops, skipped`) and `maxEdgeKm` correctly refuses to draw a 10 km tentacle to reach the next one. This is honest — there is no in-town *path* to draw — but the route still belongs on the leaflet. Keep it in `palette`/`panelOrder`/`internalDesc` (it renders in the Services panel and on the external map) and say so in its panel subtitle ("Tesco stop only"); don't inflate `maxEdgeKm` to force a line.
- **A solo "to X" terminus label printed over its own badge, and a terminus badge on the frame edge rendered half-clipped** (found on St Neots, fixed 2026-07-19, affects every `internalRoads` town) → the 2026-07-04 multi-badge fix reserved badges only for multi-route rows; **solo** rows still went through `placeLabel` anchored on the badge centre, so the text started inside the badge ("65to Buckden", "905to Bedford"). Solo and multi rows now share the reserve-then-row-candidates path (label offset 3.7 mm rather than 2.6). Separately, the cluster badge is centred on the frame-cut point, which sits *on* the frame — a 3.0 mm badge there is half off-page (St Neots printed "905" as "05"; Huntingdon's bottom cluster sat at y=205.1 with `MY1=205`), so `bx`/`by` are now clamped to keep the whole badge inside `MX0/MX1/MY0/MY1`. **This intentionally breaks the byte-identical gate** for the four existing towns: verified the ONLY diffs are the 1.1 mm label shifts and Huntingdon's bottom badge moving 205.1→201.6 (which fixes one of its open hand-tidy items). They will pick the fix up on their next re-render — a **minor** bump, no data change.
- **POI pictograms can be drawn over a terminus badge/label** (internalRoads; visible on St Neots v1.3 where a red GP icon hides the "9" of a 905 badge and clips "to Biggleswade") → the terminus arrows/badges are placed against the label-reservation system but POI icons (drawn without a reserved box) are not. **Largely mitigated by `reachExtend`** (St Neots v2.0): pushing each tail out to the frame edge moves the badge/label to the boundary, away from the interior POI it was colliding with. For any residual overlap, hand-tidy in the drag editor by nudging the POI, or hide it; the deeper fix (reserve POI icon boxes before the terminus block, and draw POIs after) is deferred as it perturbs every internalRoads town.
- **A single-ended terminus destination silently vanished when map-matching put the tail on the OTHER chain end** (St Neots 66/C2/193, fixed 2026-07-20, affects every internalRoads town) → `internalRoads.termini` lets a town label a route `end:"X"` or `start:"X"`, but WHICH of a route's two frame-cut tails is the "start" vs "end" depends on `match_routes`' canonical direction, which the config author can't predict. St Neots' 66 reached the top frame edge as a **startCut** while its config said `end:"Huntingdon"`, so the label was pinned to the missing endCut and no "to Huntingdon" drew at all (66/C2/193 all hit this). The terminus block now: if a route has a single tail and one single-ended configured label, it routes that label to **whichever cut exists** (explicit `false` still suppresses that side). So you can keep writing the destination as `end:` or `start:` without checking `DBG_TRIM` first. Verified inert on both-cut routes (e.g. 905 Cambridge/Bedford) and it correctly surfaced a previously-hidden "to Sutton St James" on Wisbech's 43A.
- **`reachExtend` grows the bbox and can silently RE-ENABLE a route you meant to leave undrawn** (St Neots 69, 2026-07-20) → 69 is a Tesco-stop-only service that `match_routes` auto-skips when it has `<2 in-bbox stops`. But extending 18/905 toward Cambridge grew the road bbox eastward — exactly where 69 runs — so 69 suddenly had ≥2 in-bbox stops and drew a full (misleading) line. Fix: **`match_cfg.json` `skipRoutes:["69"]`** — an explicit no-line list `match_routes` honours regardless of stop count (the route still appears in the Services panel and external map). Reach for it whenever a neighbour's `reachExtend` re-animates a service you deliberately kept lineless. Set the truncated tails' `reachExtend` end/start per `DBG_TRIM` (the cut moving from an interior mm to exactly MX0/MX1/MY0/MY1 confirms success), and remember an express with no intermediate stops (905 → Cambridge) extends the bbox all the way to the next real stop — `roads_geo.json` can jump several × in size.
- **python-docx DOCX show a "2013-12-23" date in Windows Explorer** (2026-07-20) → `Document()` starts from python-docx's bundled blank template, whose core-properties `created`/`modified` are baked to **2013-12-23 23:15**. The *file* mtime is correct (when it was written), but Explorer's "Date" (content) column shows the document's internal date, so a freshly-generated `disagreements.docx`/`verification.docx` reads as "clearly wrong". Fix: set `doc.core_properties.created = doc.core_properties.modified = datetime.now()` before `doc.save()` (done in both generators). Existing files can be patched in place with the same two lines. Separately, **`refresh_latest.js` now stamps each `_latest\` copy with the refresh time** (`fs.utimesSync(dest, now, now)`) — `fs.copyFileSync` otherwise carries the *source* mtime, so a just-rebuilt JPG could sit in `_latest` showing an older render time next to its siblings and read as "not updated".
- **`diagram_internal.js` must run with `LEAFLET_DIR` UNSET, or it clobbers the geographic `internal.svg`** (2026-07-20) → the engine spawns the shared `gen_internal.js` with `cwd` = its `diagram/` workspace, relying on `DIR = cwd` so the generator writes `diagram/internal.svg` (which it then copies to `internal-diagram.svg`). It inherits `process.env`, so a `LEAFLET_DIR` you exported to run the engine LEAKS into that child and overrides `cwd` → the child writes `internal.svg` into the S4 dir instead, overwriting the real geographic map, and the engine's copy of `diagram/internal.svg` then fails with `ENOENT`. Run it as plain `cd <S4 dir>; node "%SK%\diagram_internal.js"` (no `LEAFLET_DIR`). Symptom to recognise: the workspace gen prints `rotation° <nonzero>` (it read the S4 routes.json, not the workspace's `rotationDeg:0`) and 1.3 MB not ~0.6 MB.
- **S6 raises FALSE HARDs on a cross-border, two-terminus or elongated town** (St Neots, 2026-07-19 — three `verify_report.js` fixes, all made after confirming the MAP was right and the CHECK was wrong). (1) **Terminus vs a non-`0500H<LLLL>nnn` chain end:** `localityToken` returns null for stops outside the NaPTAN locality-coded style (Bedford `020035035`), so 905's perfectly correct Cambridge↔Bedford chain matched nothing and went HARD. Untokenisable ends are now counted as *unverifiable* → SOFT "confirm by hand". (2) **Direction on a route with TWO out-of-town ends:** the check compared the drawn edge stop against only the single farthest chain stop, so 905's edge stop (correctly heading for Bedford) was measured against Cambridge and flagged "drawn the wrong way". It now takes the closest match across **every** chain end. (3) **Edge stop ≠ farthest drawn stop:** in an elongated town the farthest drawn stop can be a core stop on the opposite side from the exit — 66 leaves north via Little Paxton for Huntingdon but its farthest drawn stop is Eaton Socon 2.35 km SOUTH-WEST (175° out, HARD). The edge stop is now taken from the derive_intown **buffer** stops (outside `intown_cfg.prefix`/`extraCore`), falling back to the old behaviour when no `intown_cfg.json` is present. General lesson: **when S6 blocks, check the map against reality before touching the data** — on a town whose shape or coding differs from the towns the checks were written against, the checker is the likelier suspect, and the fix belongs in `verify_report.js`, not in a reproduced baseline.
- **A town OUTSIDE the built Cambridgeshire GTFS region — the region setup is a DATA step, no code** (Beaconsfield, Buckinghamshire, 2026-07-21 — the first out-of-region town). NaPTAN ATCO area for the county ≠ `0500`/`0570`, so `gtfs_query.py`/`bootstrap_town.py` find nothing against `cambridgeshire.sqlite`. Fix per SKILL "Region-agnostic": (1) add a `_gtfs/regions.json` entry (Beaconsfield → `south_east` BODS region, `keepPrefixes:["0400"]`); (2) download that region's BODS GTFS zip (`.../gtfs-file/south_east/`, ~211 MB — no Content-Length, streams); (3) build a filtered sqlite `python gtfs_build.py --zip <zip> --out buckinghamshire.sqlite --keep-prefixes 0400` (~60 s, ~50 MB); (4) pass `--db <that sqlite> --region <County>` to `scaffold_town.py`/`bootstrap_town.py`. No generator or pipeline code changes — everything downstream (bustimes, OSM, generators) is nationwide already.
- **A geographically LARGE NaPTAN ATCO block over-captures the "core"** (Beaconsfield, 2026-07-21) → Buckinghamshire has no per-town locality letters like Cambridgeshire's `0500HSTIV`; all of central Bucks shares one numeric block `040000001`, so Beaconsfield, Gerrards Cross and Denham are ALL `040000001…`. `derive_intown`'s core = "prefix match", with no distance cap, so routes 102/104 pulled "core" stops out to **11 km** and the internal map wasn't clipped to the town. Fix: new **`intown_cfg.json` `coreMaxKm`** (2026-07-21) — a prefix stop farther than that from the anchor is demoted from core (still eligible as a buffer/edge stop under `maxEdgeKm`). Beaconsfield used `coreMaxKm:2.5`. **Absent ⇒ inert / byte-identical** (gated on St Neots + March). Diagnose by printing each drawn route's stops' distance-from-anchor — "core" stops several km out ⇒ the block is over-broad ⇒ set `coreMaxKm`. (105, which heads north into the `040000002` Amersham block, stayed tight on its own — the problem only bites routes whose neighbours share the prefix.)
- **A riverless town** (Beaconsfield, 2026-07-21) → `gen_internal.js` used to read `river_geo.json` unconditionally (line ~112) and crash `ENOENT` on a town with no river (every prior town happened to have one). **Fixed in-code** (2026-07-21): a missing `river_geo.json` now defaults to `[]`, so a riverless town just works — its real linear features come from `routes.json` `features[]` (Beaconsfield = railway + A355). No manual empty-file step needed. (Gated byte-identical: towns that DO have `river_geo.json` read it exactly as before.)
- **`gen_external.js` is the LOCAL S4 copy, not in `%SK%`** — the skill ships `gen_external_radial.js` / `gen_external_busway.js`; S3 copies the chosen one **as `gen_external.js`**. In S4 run it as `node ./gen_external.js` (or `node "%SK%\gen_external_radial.js"`), never `node "%SK%\gen_external.js"` (that file doesn't exist → `Cannot find module`).
- **A pale-blue road feature (motorway) reads as a RIVER** (Beaconsfield M40, 2026-07-21) → a `type:"road"` feature styled pale blue and drawn wide (the M40 pulled as 35 overlapping ways incl. J2 slip roads, at width 2.8 mm across 10 km) looks exactly like a watercourse, doubly misleading on a town with no real river. Either style a motorway thin and a clearly non-water colour, or (cleaner, done here) **drop it** — the railway + the main A-road were enough. A motorway skirting the town edge is rarely a useful in-town landmark; don't force a third feature.

- **A BIG town (30+ services) breaks two fixed layout budgets — plan for it before drawing** (High Wycombe, 2026-07-28 — the largest town built, ~125k, 46 verified services). (a) The **Services panel is one column**: 34 entries × 8 mm = 272 mm on a 210 mm page. (b) The **external radial frame perimeter is ~812 mm**, so at ~42 mm per destination lozenge it holds about **18–19 spokes** — High Wycombe wanted 23. Neither is a bug; they are budgets that a 7-route town never hits. The five opt-in keys that buy the room back (`panelCols`, `legendWrap`, `legendAt`+`box`, `badgeOffset`, `external[].routes`) are documented in [s3-config.md](s3-config.md). Work the external in this order: **merge co-terminating routes → one-word labels → hand-spread bearings → thin intermediate `stops` → place the legend last**. Two-line lozenges are nearly twice as wide as one-line ones and are the single biggest cause of collisions.
- **A 30-route town cannot have one colour per route — colour by OVERLAP, not by identity** (High Wycombe, 2026-07-28) → the locked "one colour per route" rule assumes a palette can cover the town; 31 drawn routes over a 12-colour colour-blind-safe set cannot. Do **not** hand-assign: build the conflict graph from `routes_paths.json` `edges` and greedy-colour it. Crucially, conflict must be **PROPORTIONAL** (shared edges ÷ the shorter route's edges ≥ ~0.28), not "share any road": nearly every route passes through the town centre, so "shares ≥3 edges" is almost a **clique** and produced 12 forced clashes. Proportional conflict means a colour is reused only between routes that never run *together* for long, which is exactly when a reader could confuse them; brief shared centre stretches are fanned apart by the internalRoads bundler anyway. Then load-balance so no colour carries a crowd, and re-check "same-colour pairs that share road" is empty. The numbered badge remains the unique identifier. (Script kept at `High Wycombe/S2-geometry/*/colour_routes.py`.)
- **The over-broad NaPTAN block bites the SERVICE SCOPE too, not just `derive_intown`'s core** (High Wycombe, 2026-07-28) → `coreMaxKm` fixes the drawn map, but `bootstrap_town.py`/`scaffold_town.py` still derive the service list **by prefix**, and Bucks block `040000003` holds **780 stops spread up to 49.6 km** (Aylesbury, Buckingham, Thame villages). The scaffold reported **78 "services"**, ~30 of which never enter High Wycombe (16, 18, 80, 111–113, 120, 133/134, 150–165, X4, X15 …). Re-scope with **`gtfs_query.py --near "lat,lon,km"`** (3.5 km here → 46 services, matching the bustimes locality list almost exactly) and register the town in `town_prefixes.json` with **`near`, not `prefixes`** — same fix Beaconsfield needed. Symptom: a service list full of routes whose termini are all in one *other* town.
- **BODS can publish a frequent service with NO `calendar` row at all — days come back "?"** (High Wycombe, Redline 130/300 and Carousel WW1, 2026-07-28) → those routes have 80+ trips each but are defined entirely by a handful of `calendar_dates` rows (130: **5 operating dates** across nine months), so `gtfs_query.py` can't derive days and reports `?`. Don't guess and don't leave `?` on the sheet. Resolve from the **bustimes timetable across the sampled S2 dates**: count time cells per fetched date (`>HH:MM<`) — 130/300 had journeys on all four including Sunday (**Daily**), WW1 had none on any and its BODS dates start 31 Aug (**matchdays only**, football season). Record the evidence on the service (`daysSource`) and log it as a disagreement — it is an operator feed-quality defect, not a route conflict.
- **A stale BODS registration for a service the operator no longer runs** (High Wycombe "route 20", 2026-07-28) → BODS listed a Thames Valley Buses (NOC CTNY) service 20, High Wycombe Bus Station → Maidenhead, Mon–Sat, 54 trips, current validity — a completely plausible-looking route with a real in-town stop chain. It does not exist: the operator's own site lists its Maidenhead-area services (1/1A, 2, 5, 6, 7, 16, 53, 108, 121–129, 150, 151, 156, 157/158, 171/172, 194, 227, 228, 703, 704) with **no 20**, bustimes has no such service, and the BODS stop it starts from (`040000002970`) shows **no services** on its bustimes stop page. Apply the standing rule — **the operator wins** — exclude it, and record the full evidence in the audit. A useful third test when GTFS and bustimes disagree about a service's *existence*: open the **bustimes stop page for its terminus stop** and see whether anything at all calls there.
- **Changing a generator DEFAULT is not "additive" — the gate exists to catch exactly this** (High Wycombe, 2026-07-28) → nudging the radial's terminus-badge offset from 8 mm to 13 mm to clear a wide lozenge looked like a harmless tweak; it silently moved the badge on **every** existing radial town and failed five gates at once. Any number you find yourself editing to make YOUR town look right is a **config key with the old value as its default** (`badgeOffset`), never a new default. Also: run the external gate with the generator each town actually uses — St Ives is **busway**, so gating it against `gen_external_radial.js` produces a meaningless DIFF at line 3.
- **Never split a complex town GEOGRAPHICALLY — measured, not assumed** (High Wycombe, 2026-07-28) → the obvious answer to "too much on one sheet" is to cut the town in half. It does not work on a radial town, because every route passes through the centre. Measured on High Wycombe's real S2 geometry: the whole town draws 31 lines, and the **north half still contains 31**, the **west half 31**, the **NW quadrant 31**; only the near-empty SW quadrant came out clean (8). An area split cuts the *extent* drawn but not the *number of colours*, so it leaves the actual failure (a ~12-hue palette carrying 31 routes) completely untouched, and whichever sheet holds the centre keeps the entire knot. **Split by route family / corridor group instead**, each sheet drawing its routes complete and town-wide. Same trap in miniature: a fisheye `lenses[]` fixes a compact knot but cannot fix a **trunk corridor** — check `complexity.json`'s `D5` (largest *connected* congested cluster) before reaching for a lens. High Wycombe's congestion is one 6.2 km connected network, not a blob at the bus station.
- **Run the complexity gate before you spend a day on a town that cannot work** (High Wycombe, 2026-07-28) → the build passed S1, S2 and S6 and still produced an unusable map, because nothing measured whether it *should* be drawn. `complexity_score.js` at the end of S2 costs about a second and would have said RED on all four metrics before a single line was styled. Its rung-1 "co-running families" are **candidates, not decisions** — the first implementation used union-find over pairwise overlap and chained `32 → 102 → M40 → …` into one bogus ten-route family purely because consecutive pairs shared the A40. It now requires **mutual** overlap and builds **cliques**; even so, confirm a family really co-runs over the drawn extent before bundling it, or the merged line asserts something false. Related: the "colour by OVERLAP" entry above is the *other* way to survive past 12 hues (reuse a colour between routes that never run together); corridor-colouring (rung 3) is the cleaner answer once routes are actually bundled.
- **Four towns had silently drifted from the engine template — "the gate passes" was measuring the wrong thing** (found + cleared 2026-07-28) → St Ives, March, Huntingdon **and Wisbech** no longer reproduced their committed `internal.svg` from `%SK%\gen_internal.js`. Every diff traced to a deliberate, already-accepted improvement (St Neots v2.0/v2.1 + High Wycombe): solo terminus label offset 2.6→3.7 mm, terminus badges clamped inside the frame, a destination shared by 2+ routes drawn black `#333`→`#111`, OS-style railway `#888888/1.6`→`#333333/1.5` **with `minSegLen` dropping short crossover stubs** (Huntingdon 69→39 railway segments), north arrow on by default, and the single-ended terminus self-correct. Nothing was an accidental regression. **Three lessons.** (1) **A build takes its generator from its S3 run, not from `%SK%`** — so an improved template reaches a town only when that town is re-rendered. Drift is the default, not the exception, and nothing warns you. (2) **Gating a town against its own frozen generator copy is circular** and always passes; the meaningful gate is *template + town data == committed SVG*. The earlier "all gates pass" claim was the circular version. Run both when diagnosing: own-copy PASS + template DIFF = shipped map intact, town just behind. (3) **`gotchas.md` under-claimed the blast radius.** The 2026-07-19 note predicted "the ONLY diffs are the 1.1 mm label shifts and Huntingdon's bottom badge 205.1→201.6"; in reality Huntingdon also got the whole railway restyle (1438 changed lines) and Wisbech *gained a destination and lost a POI label*, and the badge actually moved 205.125→205.1. When you write "this will intentionally break the gate", **list the towns and re-gate them at the end of that session** rather than predicting the diff from memory. **Always `comm` the label sets** (see `changing-the-engine.md` §2a) before shipping a re-render — a placement change can drop or surface a label, which is a content change wearing a cosmetic diff's clothing.
- **A terminus label that the new placer cannot fit disappears with no warning** (Huntingdon T1, 2026-07-28) → T1's drawn path is a 0.8 km stub ending **interior** at the bus station (see the busway/canonical-direction entry above), and its "to Cambridge" label used to be forced in at an interior point. The reserve-then-place path finds no free candidate there and drops it silently — you only notice by diffing the label sets. The right answer is not to widen the placer but to say what you mean in config: **`internalRoads.termini.<r>.end:false`** (Huntingdon now carries `T1:{end:false}`), so the arrow is absent *deliberately*. The route still draws, still carries its roundels and still appears in the Services panel. Freeing that reservation also let a 904 route badge place nearby — expect small knock-on gains, and check them.
- **A gained terminus label can cost a POI label** (Wisbech v1.1, 2026-07-28) → surfacing 43A's "to Sutton St James" and shifting the terminus labels 1.1 mm consumed the reservation box that "On Track Wisbech" was using, so that school keeps its icon but loses its name (a different POI, Leverington Sports & Social Club, gained one). This is the known unreserved-POI ordering issue two entries up, not a new bug. Trade accepted on Wisbech: a destination on a bus map beats a POI name. Fix by hand in the drag editor (nudge the icon) if it matters — **open item on Wisbech**.
- **The complexity gate's own arithmetic drifted from the drawn sheet the moment a town used the whole ladder** (High Wycombe P4, 2026-07-28 — three fixes in `complexity_score.js`, none in a generator). (1) **S counted routes the map no longer draws:** `routes_intown_atco.json` still lists every service S2 scoped, so a town that has taken rung 0 was charged for the stops of its curated-off lines (S=137 against a sheet showing 91). Measure S over the keys of `routes_paths.json`, which is written *after* `match_cfg.json skipRoutes`. (2) **A `corridorPalette` family whose lead is also an `internalCorridors` lead was silently dropped**, because `bundlePaths` renames the merged lane `<lead>+` and the existence filter then failed on the bare name — two of three corridors vanished and R read 13 instead of 11, i.e. AMBER instead of GREEN. Anything asking "is this route drawn?" after rung 1 must ask under the bundled key. (3) The colour-ambiguity line read S2's **draft** `palette.json` over every scoped route, so a town that had just fixed its palette was still told "COLOUR NO LONGER IDENTIFIES A ROUTE". Read the shipped `routes.json` palette, over drawn routes only, and say **by design** where `corridorPalette` is set. General lesson, the twin of the never-health-check-with-a-moved-metric one: **when a report and a sheet disagree, suspect the report** — and re-score every town after touching it (all six existing towns must reproduce their calibration values exactly).
- **`coreBox` leaves orphan stubs where a town-centre one-way loop pokes back out of the box** (High Wycombe v2.0 draft, 2026-07-28) → 102/103/104/105 each ended up with a 5 mm line fragment sitting *outside* the box, attached to nothing, carrying the family's six-badge terminus stack — because the matched path leaves the box, runs a few metres of the gyratory, and re-enters. `clipOutCore` now drops any run shorter than **`coreBox.minRun`** (default 2.5 mm) and `termBadge` refuses to plant a badge on an end whose run was dropped. Recognise it by a badge stack floating in white space beside the box.
- **A terminus badge ROW was clamped by its centre, not its width** (found by the first six-badge bundle, fixed 2026-07-28) → a cluster row spreads `(n-1)/2 × 6.6 mm` either side of `bx`, and only `bx` was kept inside `MX0/MX1`. A solo badge was fine (that is what the 2026-07-19 clamp was written for), but High Wycombe's six-badge "to Loudwater & Beaconsfield" row ran 13 mm past the frame and printed over the Services panel — and, once looked for, **St Ives' 2-badge row overflowed by 1.7 mm and two place leaflets' 5-badge rows by ~8 mm**, unnoticed since they were built. The clamp now uses the row's own half-width. Not config-gated, so it moved three shipped outputs: St Ives → **v6.8**, Beaconsfield Waitrose → **v1.1**, St Neots Town Centre → **v1.3**, all re-rendered the same day. **Lesson: a defect that only shows at n=6 is usually present and invisible at n=2** — when you fix a size-dependent placement bug, go looking for the small-n instances rather than assuming the old behaviour was right.
- **Colouring by corridor turns the sheet's TITLE the corridor's colour** (High Wycombe v2.0 draft, 2026-07-28) → the internal title has always been drawn in `C[orientationRoute]`, which is fine while colour identifies a route. Once 32A joined the Booker–Micklefield corridor the title came out red. New **`internalTitleColor`** key (absent ⇒ old behaviour). Watch for the same class of surprise anywhere a *route* colour is borrowed as a *page* colour.
- **A monthly monitoring script had the wrong town folder path and nothing ever complained** (`gtfs_refresh_report.py` / `gtfs_upcoming.py`, found + fixed 2026-08-04) → both looked for a town's data at `<Buses root>\<Town>` instead of `<Buses root>\Areas\<Town>` — a leftover from before the S1–S6 staging restructure moved every town under `Areas\`. The check was `if not os.path.isdir(town_dir): continue` — a silent skip, not an error, so the scripts ran fine, exited 0, and printed a perfectly formed "nothing to do" report **every single month** via the Windows Scheduled Task, for as long as the path had been wrong. Nobody noticed because a report saying "nothing to do" looks identical whether it's true or whether the script quietly checked zero towns. Fixed by pointing at `Areas\<Town>`; the first real run afterwards found 26 actionable items across 6 towns. **Lesson: a `continue`-on-missing-path guard is exactly the shape of bug that produces a clean, confident, wrong report** — when a monitoring script has been reporting "all clear" for a long time with zero variation, check that it is actually looking at the right files before trusting the silence. Grep for the same path-join pattern elsewhere before assuming one file's fix is the whole fix.
- **`internalRoads` is not an option — omit it and the map silently loses six features at once** (Tier-2 draft v1, Ramsey, 2026-08-04) → the first `draft_town.py` deliberately skipped `internalRoads` (and therefore `pull_roads.js`/`match_routes.js`) on the reasoning that the road-matching step needed human tuning and the "classic" straight-line model was more deterministic. It ran cleanly and produced a map that looked *plausible in isolation* but was far below standard: **no route-number badges, no "to X" terminus arrows, no road names, no north arrow, no version stamp, and no focus compression** — because `gen_internal.js` gates every one of them behind `if(IR ...)` (badges/arrows at ~:1104, north arrow :1643, version :1658, roads :149). All seven built towns use `internalRoads`; it *is* the standard. **Lesson: before deciding a stage is optional, check what the generator gates on it** — a single absent config key can silently subtract most of the map's information, and the output won't error, it will just quietly be a worse map. The "more deterministic" justification was also false: `pull_roads.js` and `match_routes.js` are as deterministic as the rest of S2.
- **The complexity gate must run AFTER `match_routes.js` or its verdict is not comparable to the calibration** (Ramsey, 2026-08-04) → scored on straight-line fallback geometry Ramsey read **GREEN**; scored on the real map-matched paths it read **AMBER** (`medianBuriedFraction` 0.988, four routes over half-buried — the 301/301S/301V/301X variants all on one road). s2-geometry.md already says to run it last "so it scores the real drawn geometry", and `complexity_score.js` prints its own warning that a GREEN without `routes_paths.json` is suspect. Both were easy to skim past. **A GREEN obtained without the road match is not a GREEN.**
- **GTFS cannot name a place, and the two obvious fixes each fail in an opposite direction** (Tier-2 draft, 2026-08-04) → the schema is `stop_id/stop_code/stop_name/stop_lat/stop_lon` — no locality column — and this region's operators publish **street-level headsigns**, so three different Ramsey routes all "terminate at **Bus Station**" (meaning Huntingdon, St Ives and Peterborough). Reverse-geocoding the stop coordinates via Nominatim works, cached per 9-char ATCO locality (one lookup names every stop in that locality), but the field-priority order is a trap **at both ends**: (a) UK Nominatim returns the **district** in `city`, so a rural stop between Ramsey and Warboys yields `city="Huntingdonshire"` and it prints as a real intermediate place — reject names matching `shire|District|Borough|County|Council`; (b) over-correcting by promoting `village`/`suburb` above `city` then degrades destinations to a *part* of themselves — Peterborough's Queensgate became **"Millfield"** and Huntingdon bus station **"Hartford"**. Correct order is `town, village, city(non-admin), suburb, hamlet`, and anything unresolved should be labelled `<check>` rather than shipped as a confident guess.
- **GTFS route variants drawn as separate routes overprint into nonsense** (Ramsey 301/301S/301V/301X, 2026-08-04) → four registered patterns of one route each got their own colour, their own external spoke and their own near-identical stop chain, so the radial drew four spokes on the same bearing with their place labels stacked on top of each other ("Warboys / Warboys", "Needingworth / Needingworth") and the panel listed four entries for one service. Merge them with `external[].routes` + `internalCorridors` keyed on the same lead. The safe signal is `gtfs_query.py`'s own **`possibleVariantOf`** — the operator has registered them under one number, which is a far narrower claim than the general rung-1 "these different services co-run" that complexity-triage.md insists a human confirms. Check `corridors_report.json`'s overlap figure afterwards regardless. **Also:** a merged-away variant has no `external[]` entry of its own, so anything deriving text from the spoke (e.g. `internalDesc`) must fall back to the **family lead**, or it drops straight back to the GTFS headsign and prints "Ramsey - Bus Station".
- **Overpass fails transiently often enough to strand an unattended run** (2026-08-04) → `pull_roads.js` died once mid-build under repeated use and succeeded immediately on a manual retry. Interactively that's a shrug; in an unattended script it leaves a half-built town and a committed-but-incomplete S2. Wrap Overpass-dependent steps in a retry with backoff.
- **`footerBand()`/`mapNotes` had no word-wrap at all — every wrapped note was hand-split, and one long string just ran off the page or collided with the footer** (Beaconsfield Simpson Centre + Waitrose place-internal maps, and every place-external map, found 2026-08-11) → `footerBand()` (footer.js) drew each array element as one `<text>` line with zero width-measuring, so `gen_external_places.js`'s single concatenated attribution string ran past the page edge and got clipped — while town-level external notes only looked fine because `gen_external_radial.js` pre-split its note by hand into two short strings before calling the same function. Separately, `gen_internal.js`'s `mapNotes` loop drew `n.text` verbatim with no wrap either; a town whose note happened to be short (High Wycombe) never noticed. **Fixed generically**: `footerBand()` now word-wraps every note to the available width (`footer.js`, shared `wrapNotes()` helper), and `mapNotes` wraps to the space available in its own anchor direction (`gen_internal.js`). Both are additive — an already-short note produces byte-identical output (verified against High Wycombe). **But word-wrap alone does not fix a note positioned too low**: Simpson Centre's/Waitrose's school-services note was short enough to stay one line even wrapped, and its `y:196` sat *inside* the footer's backing plate regardless of line count — that half of the fix was a **per-place `routes.json` edit** (moved to clear map space), not an engine change. The two are easy to conflate; word-wrap fixes overflow, not placement.
- **A footer-plate collision is now caught at build time, not by eyeballing a rendered JPG** (promoted 2026-08-11, after the same `y` mistake appeared in *two* place configs) → `footer.js` exports `footerPlateTop()` (factored out of `footerBand()`'s own line-count math) so a generator can ask, before drawing anything, "where does the footer's backing plate start?". `gen_internal.js` computes this once (`FOOTER_PLATE_TOP`, from the internal map's fixed two-line attribution) and checks every `mapNotes` entry's last line against it, printing a stderr warning naming the note if it lands inside/near the plate. **Does not auto-relocate** — the engine has no idea where a given town's route geometry leaves blank space (that's a judgement call; Simpson Centre's note went to the empty upper-left, which nothing in routes.json states explicitly), so silently moving a note risks dropping it on top of a route line instead. Warn-loud-don't-guess, same convention as the `panelCols` row-pitch check. If you're testing this by re-running `gen_internal.js` directly on an already-committed S4 folder: **don't** — it skips the `build_internal_place_roads.js`/wrapper's title-fix step and will silently overwrite the committed `internal.svg` with the wrong title ("Buses within X" instead of "Buses serving X"). Test in a scratch copy, or restore from the paired S5 folder if you slip.

- **A quality metric you have not checked against the artwork is worse than no metric — half of them were wrong on the first run** (`quality_metrics.js`, Phase 0, 2026-08-15) → the first full pass reported 30 of 31 sheets FAIL with figures that looked authoritative and were nonsense. **Label-over-icon said 291**: almost all were each label touching **its own** symbol, because placement puts a label 2.6 mm from a 4.2 mm-wide icon, so contact is the design — fixed by excluding the nearest icon to the label's anchor. **Whitespace balance said 412, 366, 264**: it divided the largest margin by the smallest, and routes are *meant* to run to the frame edge and be trimmed there, so the smallest margin is nearly always 0 — replaced with ink share across a 3×3 grid counting near-empty ninths. **Content-under-footer said 790**: it counted path *segments*, and one route is thousands of them — now measures mm² of ink under the band. **Silent dropped labels said 0**: it was comparing against the wrong file entirely (next entry). Each was caught by taking a suspicious number back to the 300 dpi print and looking. **Lesson: validate every measure against a defect you have already confirmed by eye BEFORE quoting any total.** The four that survived unchanged were the four that had been checked that way first (Waitrose under the library icon, the Flackwell Heath text under the footer, `River Wye` in the panel, the doubled `Fenstanton`) — and one of those corrected a diagnosis I had already written down and believed.
- **`pois.json` is not the list of things the engine labels** (found while trying to measure silent drops, 2026-08-15) → it is the raw OSM candidate list, with **display-name categories** (`Supermarket`, `Sports/Leisure`, `Doctors/GP`) not the engine's internal keys (`shop`, `leisure`, `gp`), captured **before** `excludeName`/`tidy`/`canon`, the `hide` overrides, the `inCore` test and the off-frame test. `gen_internal.js` classifies POIs from `osm.json`/`osm2.json` instead (`:381`). So a name in `pois.json` and absent from the sheet is usually *deliberate curation*, and any check that differences the two reports a confident, fabricated defect count. This is worth remembering generally: **the S2 human-readable artefact and the generator's actual input are different objects**, and it is easy to reach for the readable one.
- **Silent label drops cannot be measured from the output at all, and that is the strongest argument for fixing them** (2026-08-15) → `placeLabel()` ends `if(!chosen){ return false; }` (`gen_internal.js:662`) and emits nothing — no marker, no comment, no stderr unless `DBG_LABELS` is set. A dropped label leaves **zero trace in the SVG**, so no amount of analysing shipped sheets can count them. Every quality figure we have is therefore a **lower bound**: the maps may be failing to say things and we cannot see how much. The fix is the generator reporting what it dropped (`unplaced.json`, §1.9 of the plan), not cleverer analysis of the output.
- **The map frame overlaps the footer plate on EVERY sheet, and it only ever got noticed on one town** (2026-08-15) → the internal clip is `<rect x="6" y="30" width="190" height="175"/>` → the map is drawn down to **y=205**, while `footerBand()`'s backing plate starts at **y=195.16**. That 9.84 mm strip exists on all 8 towns and all 5 places: anything drawn in it is painted over by a 97 %-opaque white rect. 12 of 31 sheets have real route ink in it (**979 mm²** erased in total; worst St Neots internal 168 mm², St Ives internal 151 mm², St Ives schematic 126 mm²), and 9 have erased **text** — which is the only reason High Wycombe's half-vanished `to Flackwell Heath & Bourne End` was ever spotted. The 2026-08-11 `footerPlateTop()` warning covers `mapNotes` only; it does not know about route geometry or terminus labels. **Lesson: a frame constant and a plate constant that must agree, but are computed in different files, will drift and then hide the evidence** — the erasure is silent by construction, because the thing that would show you the bug is the thing being painted over. Fix belongs in the frame, not per-town.
- **Arial has two cmap subtables and taking the last one throws** (`font_metrics_build.js`, 2026-08-15) → `arial.ttf` ships both a format-4 `(3,1)` BMP table and a format-12 `(3,10)` full-Unicode one. A loop that assigns `best` on every `(3,1)|(3,10)` match ends up on the format-12 table and the format-4 parser then fails with "cmap subtable is not format 4". Every character these maps use is in the BMP, so prefer format 4 explicitly and keep a format-12 reader as fallback. Only matters if the baked width table is ever regenerated — which is exactly when nobody remembers the trap.
- **Stitching railway polylines by endpoint proximity chained four PARALLEL tracks into one path that doubled back on itself** (`gen_internal.js` chequer work, 2026-08-15) → the four tracks through St Neots station all begin and end at the same throat, so every endpoint sits within the 0.5 mm stitch tolerance of every other. The stitcher happily joined them A→B→A→B→A, producing one 291 mm path that retraced the same 73 mm four times. It looked *plausible* in the geometry stats (5 points, sensible length) and the failure only showed in the render: four superimposed dashed strokes at different phases fill each other's gaps, so the chequer came out as a **solid white core** — i.e. the symbol silently reverted to something else entirely. Fixed with `railStitchTurn` (default 60°): a real continuation carries on in roughly the same direction, a doubling-back does not, so reject any join that turns more than that. **Lesson: proximity is not adjacency.** Any "join these up" pass over map geometry needs a direction test as well as a distance test, and the tell was in the picture, not the numbers.
- **An all-or-nothing "is this line a duplicate?" test lets a 90 %-parallel siding survive whole** (same session) → the first `railMerge` dropped a polyline only if **every** point lay within tolerance of one already kept, so a track that runs alongside the main line and then diverges at one end failed the test and was drawn in full, re-doubling the very line the pass existed to de-duplicate. Rewritten to **trim rather than drop**: sample the line, keep only the maximal runs that are *not* already covered, and discard trimmed runs shorter than `railMinRun`. Sampling matters too — coverage judged at OSM vertices alone is wrong when the diagram engine has left 100 mm segments, so the line is densified to `tol/3` first and the collinear padding removed afterwards. **Lesson: for geometry de-duplication, the useful question is "which parts of this are new?", not "is this new?".**
- **The "content buried under the footer" metric only counted text STRADDLING the plate edge, so text sitting wholly inside the band scored zero** (`quality_metrics.js`, found 2026-08-15 while checking the railway-weight rollout) → the test required `b.y1 > footerTop && b.y0 < footerTop + 6 && t.y < footerTop + 2.5`, i.e. a window around the top edge. Anything placed a few mm lower was invisible to it *and* invisible on the sheet: six river/canal/railway labels at `y=196..200`, the "Only main stops…" diagram note on three towns, and a route badge at `y=197.7`. The scorecard read **"content buried under the footer: 12 sheets → 0"** while seven real ones were sitting there. The first fix — dropping the bounds and excluding the footer's own lines by their opening words — was worse: the external sheets' second note begins "Confirm live times…", so every external footer was reported as a buried label. The rule that actually works is **document order**: `footer.js` paints the plate and then its own text, so anything emitted *before* the plate is map content the plate is about to bury. **Lesson, and this is the fourth of its shape in this project: a metric that reports zero is not evidence of zero — check what it would have to see to report one.** Fixing it moved the tracked total 271 → 276 before any of the day's fixes were applied.
- **A terminus badge CLUSTER was clamped by its centre, not its height — the exact twin of the 2026-07-28 row-width bug, in the other axis** (found 2026-08-15) → the horizontal clamp had already been widened to `rowHalf` for multi-badge rows, and the vertical one was left at a flat `MY1-3.4`. But a cluster with several distinct "to X" texts draws one row per group at `by ± (n-1)/2 × RH`, so on Huntingdon a four-group cluster put route 9's badge at `y=197.7` against a frame ending at `192.2` — **5.5 mm inside the footer plate**, painted and then covered, so route 9 appeared in the Services panel and nowhere on the map. Nothing complained: badges are drawn outside the map's clip group, the byte gate only proves determinism, and the footer-text metric skipped badge digits because they carry `dominant-baseline="central"`. **Lesson: when you fix a size-dependent clamp in one axis, fix it in the other the same day** — the 2026-07-28 entry above even records the general form of this, and the y-axis half still sat there for three weeks.
- **A feature label sited outside the frame is drawn anyway, and there are three edges it can escape through, not two** (2026-08-15) → `drawFeatureLabel` already refused a `labelPos` inside `coreBox` and one right of the frame (the Wisbech "River Nene" case). It had no test for *below* the frame, and six sheets were shipping one: Huntingdon's "River Great Ouse", March's "River Nene (old course)", Ramsey's "Canal", St Neots' "East Coast Main Line" on both its sheets. Feature labels are drawn outside the map's clip group, so `design.footerSafe` — which shortens the frame — does not help them at all. March's was worse than a config mistake: it came from the engine's own **legacy fallback** for a town with no `features[]`, whose `labelPos` has always been `{x:40,y:200}`, fine against the old flat frame bottom of 205 and 4.8 mm inside the plate once `footerSafe` moved it to 192.16. The guard and the new default are both gated on `design.footerSafe`, so the five place sheets stay byte-identical until the Phase 8 re-vendor. **Lesson: an invariant worth enforcing on one edge is worth enumerating all the edges for.**
- **"Put a scale bar on the geographic sheets" — except none of them is to a single scale** (2026-08-15, building plan §4.6) → every town runs the radial fisheye in `compress()`: true scale inside `internalRoads.focus.coreKm`, then `focus.comp` beyond it. Across all eight towns `comp` is **0.30 to 0.50**, so the page scale steps by **2× to 3.3×** at the core boundary, and two towns carry a detail lens as well. A plain bar would have been right in the middle of the sheet and wrong by 3× at the edges — the difference between a fifteen-minute walk and a forty-five-minute one — and nothing in the build would have complained, because a scale bar is just a line and a number. The plan item was written by someone (me, the session before) who had read the drawing code and not the projection code. **Lesson: before drawing anything that makes a quantitative claim about the map, go and read what the projection actually does to distance.** What saved the device was a measurement rather than an assumption: the true-scale core is 34–71 mm in radius on a 190 × 162 mm frame, so it covers most of the *page* even though most of the *content* is outside it — which is what makes a bar captioned `town centre scale` honest and useful rather than a fudge.
- **The schematic workspace sets `focus:{comp:1}`, so a derived sheet looks perfectly uniform to the code that draws on it** (same session) → `schematize_internal.js` and `diagram_internal.js` hand `gen_internal.js` coordinates that have been solved onto a tube-map grid; distance is gone, but the *projection* the workspace declares is the identity, so any measurement `gen_internal.js` makes of its own page scale comes out clean and confident and means nothing. This is why `notToScale` is a flag the pre-stages set rather than something the engine infers. **Lesson: a pre-stage that rewrites the input can leave the downstream code with a perfectly consistent picture of a world that no longer exists** — when one stage synthesises coordinates for another, it owns telling the second stage what they are not.
- **The map was fitted to stops it does not draw, and the fit set asked the wrong question** (Ramsey, found 2026-08-15 doing plan §4.2) → under `internalRoads` the fit set is "core stops", selected by **ATCO prefix** — i.e. by which parish a stop is in. That is not the same question as *does this map draw anything there*: the route line comes from the matched road graph, and where the graph ends the line ends, so a served stop beyond that end sits in the fit with no ink anywhere near it. Ramsey had six on X31 out at Ramsey St Mary's, **2.7–3.6 km** from any drawn line. They stretched the fit box from 75 mm wide to 141 mm, so the town was drawn 8 % smaller than it needed to be and shoved into the right two-thirds of the frame — the entire left column measured **0.0 % route ink**. The sheet looked like a composition problem and was a projection one. Fixed as `fitMaxOffPath` (default 1500 m, and the measured separation is wide: six towns' worst core stop is ≤ 79 m, High Wycombe's is 929 m *by design*, Ramsey's six start at 2,701 m). **Lesson: when a sheet looks badly composed, check what the fit is computed from before rearranging anything** — and beware any set whose membership comes from an administrative code rather than from what the code actually drew.
- **`emptyNinths` had never been acted on, so nobody had checked it against a sheet that looks FINE** (2026-08-15) → it counts ninths of the frame with essentially no *route ink*. On an internal sheet the frame starts below the title and excludes the panel, so that is fair. On an **external** sheet there is no frame, so it measured from y=0 — counting the whole title band as empty map — and the operators box, which is composition and not route ink, made its column read bare. March external looks well balanced and scored 3. Both corrected; `share` (the 3×3 fractions) is now in `--json`, because "4 empty ninths" is not actionable without knowing which four. **Lesson: a warn-only metric nobody has acted on has never really been tested** — the first time you use one to justify a change, re-validate it against a good sheet as well as a bad one, or you will "fix" something that was right.
- **Two agents in one working tree is fine for editing and not fine for ROLLING OUT** (2026-08-15) → a second session was editing `gen_internal.js` (the `design.badgeFit` follow-up) while an 8-town `rollout.js --all` was running. Each town's build copies the generators fresh from `%SK%` at S4 time, so the eight towns picked up **five different snapshots of the file** and recorded five different engine hashes into their manifests. Nothing broke — the other session's feature was gated off and inert, so every sheet's output was identical and all 27 gates passed — but the provenance stamps are now wrong, and the only clue is `status.js` showing towns on hashes that no single commit contains. **The rollout is the step that freezes a hash into 31 manifests, so it wants a quiet tree.** Re-stamped 2026-08-16 — all 8 towns are on one hash again, and the two rollouts that day were both run into a quiet tree, which is the practice to copy. Related: `preview_design.js` writes to `assets/design-preview/` when `--out` is omitted, and a `git add` over the skill directory will sweep 3 MB of 300 dpi JPGs into the repo — now gitignored.

## Text that does not fit its shape (2026-08-16)

- **The bug is WIDTH, not character count — do not "fix" it with `size / len(text)`.** `design.badgeFit` was reported as "four-character route keys overflow the badge". Measured, the worst offenders were **three** characters: High Wycombe's `WW1` is 5.87 mm of Arial Bold in a 4.8 mm disc, against `301S`'s 5.61 mm. All-caps codes (`WW1` `LGW` `LHR` `OXF` `M40`) run far wider per character than digits, which are all 0.5562 em. A length-based rule would have missed five real cases and wrongly widened `X31`, which fits. **Ask `font_metrics.js`; never divide by string length.** The same applies to `measureNodeWidth` and the hub — see `design-quality.md` under `badgeFit` / `hubFit`.
- **Fit the shape to the text, not the text to the shape, once the type is near the legibility floor.** Shrinking a badge's font to fit its disc is the smaller change and it fails where it matters most: `301S` inside a 2.4 mm-radius stop badge needs **1.8 mm** type, under the 2.4 mm floor `quality_metrics.js` enforces. Hence the stadium.
- **Express a widened pitch as `<old literal> + <extra>`, never recompute it.** Every clamp, pitch and reserve box downstream of `badgeHalfW` is written that way, so a town with no wide key adds a floating zero and stays *bit-for-bit* identical rather than merely close. `2.6 + 0.2` is `2.8000000000000003` in JS; `x - 2.8 - 0` is `x - 2.8`. This is the difference between the byte gate passing and a day spent on a 1-byte diff.
- **A label's `own` box is its EXEMPTION, so widening it with the symbol makes things worse.** The terminus cluster's `reserve` box must grow with a wider badge row; its `own` box must not. Widened, Ramsey's `to St Ives` and `to Huntingdon` both came inside and printed over the ribbon (3 → 5 defects); left alone, one keeps a clean spot (3 → 4) and neither is dropped, because both are `mustPlace`.
- **`quality_metrics.js` cannot see this class of bug at all**, so do not read its total as a verdict on a fitting change. Badge glyphs are skipped by `t.central`, and nothing measures the inside of a symbol — `badgeFit` and `hubFit` together moved the 31-sheet total by −1 while fixing every overflow on the board. Render and look, and run the overflow check below.
- **Two traps in writing that overflow check**, both of which produced confident, wrong answers first time: (1) a bare `width="([0-9.]+)"` regex also matches **`stroke-width`**, which reported 1,375 of 1,376 lines overflowing a "0.70 mm" shape; (2) walking forward from a shape over consecutive `dominant-baseline="central"` text picks up **neighbours**, not just contents — the operator legend draws a badge and then the operator's name beside it, which yielded 49 confident false positives. Filter on `text-anchor="middle"` **and** on the text's `x` matching the shape's centre.
- **Check the whole class once you have found one instance.** The hub-node defect was invisible until the badge fix prompted a sweep of every shape-contained text on all 31 sheets. That sweep also proved the terminus lozenges did *not* need changing (tightest 0.88 mm a side across 312 lines), which saved moving every lozenge and every spoke badge offset to fix nothing.

## The panel, the corridor rule and the water (2026-08-16)

- **Do not pass a `--patch` / `--set` JSON containing en-dashes or middots through PowerShell 5.1's argv.** `preview_design.js` and `adopt_config.js` take their JSON as a command-line argument, and PowerShell converts it to the system ANSI codepage on the way to `node.exe`, so `Mon–Sat` and ` · ` arrive mangled — and they arrive mangled *silently*, straight into a committed S3. Every panel string in this system has both characters in it. Write the JSON to a **UTF-8 file** and spawn the tool from a tiny Node runner (`JSON.parse` the file, pass the re-stringified value), because `child_process` hands argv to Windows as UTF-16. The same applies to any commit message or note with typographic punctuation.
- **A defect the metric cannot see is not a defect you can skip rendering.** `design.panelCorridors` rebuilt the entire Services panel of two sheets and moved the 31-sheet total by **zero**, exactly as `panelScale` did before it, because no measure in `quality_metrics.js` looks at the panel. The only evidence that the work was right is the rendered panel and the label-set diff — and the label-set diff is the one to read carefully here: it lists 14 strings lost and 13 gained, and the question it answers is not "did the count go up" but **"is every service's destination still printed somewhere"**.
- **Regrouping rows is a chance to drop information silently.** Six services sharing one road through town have six different destinations beyond it, and a naive "one row per lane" writes one destination and loses five. That is why `corridorDesc` takes as many subtitle lines as the lane needs rather than a fixed `[title, subtitle]` pair — and why the check after the change is the *old* panel's content, not the new panel's tidiness.
- **A stack of badges in a panel column has to take its own line, and the arithmetic says so before the render does.** Six 5.2 mm discs are 34 mm across; a `panelCols` column is 49 mm and the title needs ~40 of it. The tidy-looking alternative — one fixed badge column wide enough for the widest stack — leaves every single-badge row floating 13 mm from its title. Measure the widest stack against the column before choosing a layout.
- **Pick a threshold from what it means, then look at the data — not the other way round.** Judging whether a `corridorPalette` group earns its shared hue needed a bar. "Half, because below that most of each line is going somewhere else" is an argument that survives the answer; the three High Wycombe groups then measured 0.55, 0.58 and 0.79, two of them within 0.05 of the line. Had the numbers been read first, any bar chosen would have been a rationalisation.
- **Plain colour distance flags a grey as a river.** The §5.2 check (route drawn in the water's colour) uses CIE76 dE **and** a hue test, with near-neutrals excluded on chroma: `#BBBBBB` sits 21.3 from the `#9ec9e8` river purely on lightness, and a grey line is not mistakable for water, while the real offender `#66CCEE` is 14.6 away *and* 8° of hue from it. A single-number colour test on a palette that contains greys will always fire on the wrong thing.
- **Score a replacement hue against every colour on the sheet, then render it anyway.** Ramsey's X31 came out of the scoring with teal as a strong second (worst separation 39.1), and teal's worst neighbour was the green 303 — which happens to run *beside* X31 for the length of Wood Lane. "Runs beside" is not in the numbers. Indigo (49.3) was the right answer and the render is what confirmed it.
